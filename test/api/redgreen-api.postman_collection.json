{
  "info": {
    "_postman_id": "20b614a5-6d93-43d7-8916-5bc93352b48e",
    "name": "RedGreen API Tests",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "TC-001 - Unregistered user cannot play",
      "item": [
        {
          "name": "TC-001 - POST attempt to start Slot game without JWT",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const ExpectedStatus = 401;",
                  "let ResponseJson;",
                  "",
                  "pm.test('TC-001 - request does not send Authorization header', function () {",
                  "  pm.expect(pm.request.headers.get('Authorization')).to.be.oneOf([undefined, null]);",
                  "});",
                  "",
                  "pm.test('TC-001 - backend rejects unauthenticated game start', function () {",
                  "  pm.response.to.have.status(ExpectedStatus);",
                  "});",
                  "",
                  "pm.test('TC-001 - error response matches implemented Unauthorized format', function () {",
                  "  pm.response.to.have.header('Content-Type');",
                  "  pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');",
                  "  ResponseJson = pm.response.json();",
                  "  pm.expect(ResponseJson).to.be.an('object');",
                  "  pm.expect(ResponseJson).to.deep.equal({ message: 'Unauthorized', statusCode: ExpectedStatus });",
                  "});",
                  "",
                  "pm.test('TC-001 - response contains no started session or game state', function () {",
                  "  ResponseJson = ResponseJson || pm.response.json();",
                  "  pm.expect(pm.response.code).to.not.be.oneOf([200, 201]);",
                  "  pm.expect(ResponseJson).to.not.have.property('session');",
                  "  pm.expect(ResponseJson).to.not.have.property('currentBalance');",
                  "  pm.expect(ResponseJson).to.not.have.property('SlotSessionId');",
                  "  pm.expect(ResponseJson).to.not.have.property('ActiveSessionId');",
                  "  pm.expect(ResponseJson).to.not.have.property('CurrentSpinResult');",
                  "  pm.expect(ResponseJson).to.not.have.property('CurrentRewardSnapshot');",
                  "});",
                  "",
                  "pm.test('TC-001 - response does not advertise a created resource', function () {",
                  "  pm.expect(pm.response.headers.get('Location')).to.be.oneOf([undefined, null]);",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/slot-machines/{{slotMachineId}}/sessions",
              "host": ["{{baseUrl}}"],
              "path": ["slot-machines", "{{slotMachineId}}", "sessions"]
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-002 - Regular user cannot use restricted functions",
      "item": [
        {
          "name": "TC-002 - Regular user cannot create gambit table",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function ResolveRequest(Serialized) {",
                  "  const Request = JSON.parse(pm.variables.replaceIn(Serialized));",
                  "  Request.url = Request.url.raw;",
                  "  return Request;",
                  "}",
                  "",
                  "const Suffix = `${Date.now()}${Math.floor(Math.random() * 100000)}`;",
                  "pm.environment.set('tc002UserName', `TC002 Regular User ${Suffix}`);",
                  "pm.environment.set('tc002UserEmail', `tc002.regular.${Suffix}@example.test`);",
                  "pm.environment.set('tc002UserNickname', `tc002regular${Suffix}`);",
                  "pm.environment.set('tc002UserPassword', 'Tc002Password123!');",
                  "pm.environment.set('tc002GambitTableName', `TC002 Unauthorized Gambit Table ${Suffix}`);",
                  "pm.environment.unset('tc002RegularUserToken');",
                  "",
                  "function StopSetup(Stage, ErrorObject) {",
                  "  pm.environment.unset('tc002RegularUserToken');",
                  "  pm.test('TC-002 - ' + Stage + ' setup succeeds', function () {",
                  "    throw new Error(ErrorObject.message || String(ErrorObject));",
                  "  });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "pm.sendRequest(ResolveRequest(\"{\\\"auth\\\":{\\\"type\\\":\\\"noauth\\\"},\\\"method\\\":\\\"POST\\\",\\\"header\\\":[{\\\"key\\\":\\\"Content-Type\\\",\\\"value\\\":\\\"application/json\\\"}],\\\"body\\\":{\\\"mode\\\":\\\"raw\\\",\\\"raw\\\":\\\"{\\\\n  \\\\\\\"Name\\\\\\\": \\\\\\\"{{tc002UserName}}\\\\\\\",\\\\n  \\\\\\\"BirthDate\\\\\\\": \\\\\\\"1995-01-01\\\\\\\",\\\\n  \\\\\\\"Nickname\\\\\\\": \\\\\\\"{{tc002UserNickname}}\\\\\\\",\\\\n  \\\\\\\"Email\\\\\\\": \\\\\\\"{{tc002UserEmail}}\\\\\\\",\\\\n  \\\\\\\"Password\\\\\\\": \\\\\\\"{{tc002UserPassword}}\\\\\\\"\\\\n}\\\",\\\"options\\\":{\\\"raw\\\":{\\\"language\\\":\\\"json\\\"}}},\\\"url\\\":{\\\"raw\\\":\\\"{{baseUrl}}/auth/register\\\",\\\"host\\\":[\\\"{{baseUrl}}\\\"],\\\"path\\\":[\\\"auth\\\",\\\"register\\\"]}}\"), function (ErrorObject, Response) {",
                  "  if (ErrorObject) { StopSetup('registration', ErrorObject); return; }",
                  "  let ResponseJson;",
                  "",
                  "pm.test('TC-002 - regular user registration succeeds', function () {",
                  "  pm.expect(Response.code).to.eql(201);",
                  "  ResponseJson = Response.json();",
                  "  pm.expect(ResponseJson).to.have.property('User');",
                  "  pm.expect(ResponseJson.User).to.have.property('Email', pm.environment.get('tc002UserEmail'));",
                  "});",
                  "",
                  "pm.test('TC-002 - created user is not an administrator', function () {",
                  "  ResponseJson = ResponseJson || Response.json();",
                  "  pm.expect(ResponseJson.User).to.have.property('UserType', 'User');",
                  "  pm.expect(ResponseJson.User.UserType).to.not.eql('Admin');",
                  "});",
                  "  let User;",
                  "  try { User = Response.json().User; } catch (ErrorObject) { StopSetup('registration', ErrorObject); return; }",
                  "  if (Response.code !== 201 || !User || User.UserType !== 'User' || User.Email !== pm.environment.get('tc002UserEmail')) {",
                  "    StopSetup('registration', new Error('Expected a newly registered regular user')); return;",
                  "  }",
                  "  pm.sendRequest(ResolveRequest(\"{\\\"auth\\\":{\\\"type\\\":\\\"noauth\\\"},\\\"method\\\":\\\"POST\\\",\\\"header\\\":[{\\\"key\\\":\\\"Content-Type\\\",\\\"value\\\":\\\"application/json\\\"}],\\\"body\\\":{\\\"mode\\\":\\\"raw\\\",\\\"raw\\\":\\\"{\\\\n  \\\\\\\"Email\\\\\\\": \\\\\\\"{{tc002UserEmail}}\\\\\\\",\\\\n  \\\\\\\"Password\\\\\\\": \\\\\\\"{{tc002UserPassword}}\\\\\\\"\\\\n}\\\",\\\"options\\\":{\\\"raw\\\":{\\\"language\\\":\\\"json\\\"}}},\\\"url\\\":{\\\"raw\\\":\\\"{{baseUrl}}/auth/login\\\",\\\"host\\\":[\\\"{{baseUrl}}\\\"],\\\"path\\\":[\\\"auth\\\",\\\"login\\\"]}}\"), function (ErrorObject, Response) {",
                  "    if (ErrorObject) { StopSetup('login', ErrorObject); return; }",
                  "    let ResponseJson;",
                  "",
                  "pm.test('TC-002 - regular user login succeeds', function () {",
                  "  pm.expect(Response.code).to.eql(200);",
                  "  ResponseJson = Response.json();",
                  "  pm.expect(ResponseJson).to.have.property('User');",
                  "  pm.expect(ResponseJson.User).to.have.property('Email', pm.environment.get('tc002UserEmail'));",
                  "  pm.expect(ResponseJson.User).to.have.property('UserType', 'User');",
                  "});",
                  "",
                  "pm.test('TC-002 - login returns a valid-looking JWT', function () {",
                  "  ResponseJson = ResponseJson || Response.json();",
                  "  pm.expect(ResponseJson).to.have.property('Token');",
                  "  pm.expect(ResponseJson.Token).to.be.a('string').and.not.empty;",
                  "  pm.expect(ResponseJson.Token.split('.')).to.have.length(3);",
                  "  pm.environment.set('tc002RegularUserToken', ResponseJson.Token);",
                  "});",
                  "    let Result;",
                  "    try { Result = Response.json(); } catch (ErrorObject) { StopSetup('login', ErrorObject); return; }",
                  "    if (Response.code !== 200 || !Result.User || Result.User.UserType !== 'User' || Result.User.Email !== pm.environment.get('tc002UserEmail') || typeof Result.Token !== 'string' || Result.Token.split('.').length !== 3) {",
                  "      StopSetup('login', new Error('Expected the regular user and a JWT')); return;",
                  "    }",
                  "    pm.environment.set('tc002RegularUserToken', Result.Token);",
                  "  });",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "function ResolveRequest(Serialized) {",
                  "  const Request = JSON.parse(pm.variables.replaceIn(Serialized));",
                  "  Request.url = Request.url.raw;",
                  "  return Request;",
                  "}",
                  "",
                  "const ExpectedStatus = 401;",
                  "let ResponseJson;",
                  "",
                  "pm.test('TC-002 - admin request is configured with the regular user JWT', function () {",
                  "  const Token = pm.environment.get('tc002RegularUserToken');",
                  "  pm.expect(Token).to.be.a('string').and.not.empty;",
                  "  pm.expect(Token.split('.')).to.have.length(3);",
                  "  pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + Token);",
                  "});",
                  "",
                  "pm.test('TC-002 - backend rejects regular user for admin-only operation', function () {",
                  "  pm.response.to.have.status(ExpectedStatus);",
                  "});",
                  "",
                  "pm.test('TC-002 - error response matches implemented AdminGuard format', function () {",
                  "  pm.response.to.have.header('Content-Type');",
                  "  pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');",
                  "  ResponseJson = pm.response.json();",
                  "  pm.expect(ResponseJson).to.be.an('object');",
                  "  pm.expect(ResponseJson).to.have.property('statusCode', ExpectedStatus);",
                  "  pm.expect(ResponseJson).to.have.property('message', 'Admin access required');",
                  "  pm.expect(ResponseJson).to.have.property('error', 'Unauthorized');",
                  "});",
                  "",
                  "pm.test('TC-002 - admin operation was not accepted as a created table', function () {",
                  "  ResponseJson = ResponseJson || pm.response.json();",
                  "  pm.expect(pm.response.code).to.not.be.oneOf([200, 201]);",
                  "  pm.expect(ResponseJson).to.not.have.property('GambitTableId');",
                  "  pm.expect(ResponseJson).to.not.have.property('Active');",
                  "});",
                  "",
                  "pm.sendRequest(ResolveRequest(\"{\\\"auth\\\":{\\\"type\\\":\\\"noauth\\\"},\\\"method\\\":\\\"GET\\\",\\\"header\\\":[],\\\"url\\\":{\\\"raw\\\":\\\"{{baseUrl}}/gambit-table\\\",\\\"host\\\":[\\\"{{baseUrl}}\\\"],\\\"path\\\":[\\\"gambit-table\\\"]}}\"), function (ErrorObject, Response) {",
                  "  pm.test('TC-002 - verification request completes', function () {",
                  "    pm.expect(ErrorObject).to.not.exist;",
                  "  });",
                  "  if (ErrorObject) return;",
                  "  pm.test('TC-002 - gambit table list is readable for verification', function () {",
                  "  pm.expect(Response.code).to.eql(200);",
                  "});",
                  "",
                  "pm.test('TC-002 - unauthorized admin operation did not create a table', function () {",
                  "  const ResponseJson = Response.json();",
                  "  const AttemptedName = pm.environment.get('tc002GambitTableName');",
                  "  pm.expect(ResponseJson).to.be.an('array');",
                  "  pm.expect(ResponseJson.some((Table) => Table.Name === AttemptedName)).to.eql(false);",
                  "});",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{tc002RegularUserToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Name\": \"{{tc002GambitTableName}}\",\n  \"Description\": \"TC-002 unauthorized admin operation attempt\",\n  \"MinimumChipsRequired\": 0,\n  \"CardPrice\": 10,\n  \"TableMultiplier\": 1,\n  \"MinimumCardsPurchased\": 1,\n  \"MaxCardsPurchased\": 5\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/gambit-table",
              "host": ["{{baseUrl}}"],
              "path": ["gambit-table"]
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-003 - Administrator can use restricted functions",
      "item": [
        {
          "name": "TC-003 - Administrator can create gambit table",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.variables.unset('tc003AdminToken');",
                  "pm.variables.unset('tc003ExpectedTable');",
                  "const Email = pm.environment.get('tc003AdminEmail');",
                  "const Password = pm.environment.get('tc003AdminPassword');",
                  "function StopSetup(ErrorObject) {",
                  "  pm.test('TC-003 - administrator setup succeeds', function () { throw ErrorObject; });",
                  "  pm.execution.skipRequest();",
                  "}",
                  "if (!Email || !Password) {",
                  "  StopSetup(new Error('Provide test administrator credentials or run npm run test:api:tc003 for automatic local preparation.'));",
                  "} else {",
                  "  const Name = pm.environment.get('tc003GambitTableName') || ('TC003 Admin Table ' + pm.variables.replaceIn('{{$guid}}'));",
                  "  const Table = { Name: Name, Description: 'TC-003 administrative access test', MinimumChipsRequired: 0, CardPrice: 10, TableMultiplier: 1, MinimumCardsPurchased: 5, MaxCardsPurchased: 20 };",
                  "  pm.variables.set('tc003ExpectedTable', JSON.stringify(Table));",
                  "  pm.sendRequest({",
                  "    url: pm.environment.get('baseUrl') + '/auth/login',",
                  "    method: 'POST',",
                  "    header: { 'Content-Type': 'application/json' },",
                  "    body: { mode: 'raw', raw: JSON.stringify({ Email: Email, Password: Password }) }",
                  "  }, function (ErrorObject, Response) {",
                  "    if (ErrorObject) { StopSetup(new Error('Administrator login request failed')); return; }",
                  "    pm.test('TC-003 - administrator login returns 200', function () { pm.expect(Response.code).to.eql(200); });",
                  "    let Data;",
                  "    try { Data = Response.json(); } catch (ErrorObject) { StopSetup(ErrorObject); return; }",
                  "    pm.test('TC-003 - authenticated user is the active administrator', function () {",
                  "      pm.expect(Data.User.Email).to.eql(Email);",
                  "      pm.expect(Data.User.UserType).to.eql('Admin');",
                  "      pm.expect(Data.User.Active).to.eql(true);",
                  "      pm.expect(Data.User.UserId).to.be.a('string').and.not.empty;",
                  "    });",
                  "    pm.test('TC-003 - login supplies a JWT', function () {",
                  "      pm.expect(Data.Token).to.be.a('string').and.not.empty;",
                  "      pm.expect(Data.Token.split('.')).to.have.length(3);",
                  "    });",
                  "    if (Response.code !== 200 || !Data.User || Data.User.Email !== Email || Data.User.UserType !== 'Admin' || !Data.User.Active || typeof Data.Token !== 'string' || Data.Token.split('.').length !== 3) {",
                  "      StopSetup(new Error('Login must authenticate an active administrator and return a JWT')); return;",
                  "    }",
                  "    pm.variables.set('tc003AdminToken', Data.Token);",
                  "  });",
                  "}"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const Expected = JSON.parse(pm.variables.get('tc003ExpectedTable'));",
                  "pm.test('TC-003 - request sends the administrator JWT', function () {",
                  "  const Token = pm.variables.get('tc003AdminToken');",
                  "  pm.expect(Token).to.be.a('string').and.not.empty;",
                  "  pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + Token);",
                  "});",
                  "pm.test('TC-003 - administrative creation returns 201', function () { pm.response.to.have.status(201); });",
                  "pm.test('TC-003 - creation returns JSON', function () {",
                  "  pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');",
                  "});",
                  "const Created = pm.response.json();",
                  "pm.test('TC-003 - created table matches submitted fields and is active', function () {",
                  "  pm.expect(Created).to.include(Expected);",
                  "  pm.expect(Created.Active).to.eql(true);",
                  "});",
                  "pm.test('TC-003 - created table has a generated positive integer ID', function () {",
                  "  pm.expect(Number.isInteger(Created.GambitTableId)).to.eql(true);",
                  "  pm.expect(Created.GambitTableId).to.be.above(0);",
                  "});",
                  "if (pm.response.code === 201 && Number.isInteger(Created.GambitTableId) && Created.GambitTableId > 0) {",
                  "  pm.sendRequest({ url: pm.environment.get('baseUrl') + '/gambit-table/' + Created.GambitTableId, method: 'GET' }, function (ErrorObject, Response) {",
                  "    pm.test('TC-003 - created table can be retrieved through the API', function () {",
                  "      pm.expect(ErrorObject).to.not.exist;",
                  "      pm.expect(Response.code).to.eql(200);",
                  "    });",
                  "    if (ErrorObject) return;",
                  "    pm.test('TC-003 - persisted table matches the created table', function () {",
                  "      const Persisted = Response.json();",
                  "      pm.expect(Persisted).to.include(Expected);",
                  "      pm.expect(Persisted.GambitTableId).to.eql(Created.GambitTableId);",
                  "      pm.expect(Persisted.Active).to.eql(true);",
                  "    });",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{tc003AdminToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{tc003ExpectedTable}}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": "{{baseUrl}}/gambit-table"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-004 - Daily bonus cannot be claimed more than once per day",
      "item": [
        {
          "name": "TC-004 - Daily bonus cannot be credited twice on the same day",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? Reject(ErrorObject) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-004 - ' + Name, function () {",
                  "    try { Assertion(); } catch (ErrorObject) { Failure = ErrorObject; throw ErrorObject; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc004Token', 'tc004InitialBalance', 'tc004FirstBalance', 'tc004LastLoginDate', 'tc004UserId']) pm.variables.unset(Key);",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}');",
                  "  const Email = 'tc004.' + Suffix + '@example.test';",
                  "  const Password = pm.variables.replaceIn('{{$guid}}') + 'Aa1!';",
                  "  const Registered = await CallApi('POST', '/auth/register', { Name: 'TC004 Daily Bonus User', BirthDate: '1995-01-01', Nickname: 'tc004' + Suffix, Email: Email, Password: Password });",
                  "  Check('fresh user is registered with no previous daily claim', () => {",
                  "    pm.expect(Registered.code).to.eql(201);",
                  "    const User = Registered.json().User;",
                  "    pm.expect(User.Email).to.eql(Email);",
                  "    pm.expect(User.UserType).to.eql('User');",
                  "    pm.expect(User.DailyLoginStreak).to.eql(0);",
                  "    pm.expect(User.LastLoginDate).to.be.oneOf([null, undefined]);",
                  "  });",
                  "  const Login = await CallApi('POST', '/auth/login', { Email: Email, Password: Password });",
                  "  Check('login authenticates the newly registered user', () => {",
                  "    pm.expect(Login.code).to.eql(200);",
                  "    pm.expect(Login.json().User.UserId).to.eql(Registered.json().User.UserId);",
                  "    pm.expect(Login.json().User.Email).to.eql(Email);",
                  "  });",
                  "  const Token = Login.json().Token;",
                  "  Check('login returns a JWT', () => {",
                  "    pm.expect(Token).to.be.a('string').and.not.empty;",
                  "    pm.expect(Token.split('.')).to.have.length(3);",
                  "  });",
                  "  pm.variables.set('tc004Token', Token);",
                  "  pm.variables.set('tc004UserId', Login.json().User.UserId);",
                  "  const InitialResponse = await CallApi('GET', '/user/chips', undefined, Token);",
                  "  Check('initial balance is read through the authenticated API', () => {",
                  "    pm.expect(InitialResponse.code).to.eql(200);",
                  "    pm.expect(InitialResponse.json().ChipBalance).to.eql(0);",
                  "  });",
                  "  const Initial = InitialResponse.json().ChipBalance;",
                  "  pm.variables.set('tc004InitialBalance', Initial);",
                  "  const First = await CallApi('POST', '/user/daily-login', undefined, Token);",
                  "  const ExpectedReward = 1 * 50;",
                  "  Check('first daily claim returns 201 and credits the day-one reward', () => {",
                  "    pm.expect(First.code).to.eql(201);",
                  "    pm.expect(First.json()).to.deep.equal({ FirstLoginToday: true, DailyStreak: 1, Reward: ExpectedReward, ChipBalance: Initial + ExpectedReward });",
                  "  });",
                  "  const AfterFirst = await CallApi('GET', '/user/chips', undefined, Token);",
                  "  Check('first reward is persisted in the balance', () => {",
                  "    pm.expect(AfterFirst.code).to.eql(200);",
                  "    pm.expect(AfterFirst.json().ChipBalance).to.eql(Initial + ExpectedReward);",
                  "  });",
                  "  pm.variables.set('tc004FirstBalance', AfterFirst.json().ChipBalance);",
                  "  const Profile = await CallApi('GET', '/user/profile', undefined, Token);",
                  "  Check('first claim persists its date and streak for the same user', () => {",
                  "    pm.expect(Profile.code).to.eql(200);",
                  "    pm.expect(Profile.json().UserId).to.eql(pm.variables.get('tc004UserId'));",
                  "    pm.expect(Profile.json().DailyLoginStreak).to.eql(1);",
                  "    pm.expect(Profile.json().LastLoginDate).to.be.a('string').and.not.empty;",
                  "    pm.expect(Number.isFinite(Date.parse(Profile.json().LastLoginDate))).to.eql(true);",
                  "  });",
                  "  pm.variables.set('tc004LastLoginDate', Profile.json().LastLoginDate);",
                  "})().catch(ErrorObject => {",
                  "  pm.test('TC-004 - preparation completes', () => { throw ErrorObject; });",
                  "  pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? Reject(ErrorObject) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-004 - ' + Name, function () {",
                  "    try { Assertion(); } catch (ErrorObject) { Failure = ErrorObject; throw ErrorObject; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "",
                  "(async function () {",
                  "  const Token = pm.variables.get('tc004Token');",
                  "  const FirstBalance = Number(pm.variables.get('tc004FirstBalance'));",
                  "  Check('second claim uses the same authenticated context', () => {",
                  "    pm.expect(Token).to.be.a('string').and.not.empty;",
                  "    pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + Token);",
                  "  });",
                  "  Check('second claim returns the implemented 201 status', () => { pm.response.to.have.status(201); });",
                  "  Check('backend recognizes a same-day repeat and grants zero reward', () => {",
                  "    pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');",
                  "    pm.expect(pm.response.json()).to.deep.equal({ FirstLoginToday: false, DailyStreak: 1, Reward: 0, ChipBalance: FirstBalance });",
                  "  });",
                  "  const FinalBalance = await CallApi('GET', '/user/chips', undefined, Token);",
                  "  Check('persisted balance contains exactly one daily reward', () => {",
                  "    pm.expect(FinalBalance.code).to.eql(200);",
                  "    pm.expect(FinalBalance.json().ChipBalance).to.eql(FirstBalance);",
                  "    pm.expect(FinalBalance.json().ChipBalance - Number(pm.variables.get('tc004InitialBalance'))).to.eql(50);",
                  "  });",
                  "  const FinalProfile = await CallApi('GET', '/user/profile', undefined, Token);",
                  "  Check('repeat leaves the persisted claim date and streak unchanged', () => {",
                  "    pm.expect(FinalProfile.code).to.eql(200);",
                  "    pm.expect(FinalProfile.json().UserId).to.eql(pm.variables.get('tc004UserId'));",
                  "    pm.expect(FinalProfile.json().LastLoginDate).to.eql(pm.variables.get('tc004LastLoginDate'));",
                  "    pm.expect(FinalProfile.json().DailyLoginStreak).to.eql(1);",
                  "    pm.expect(FinalProfile.json().ChipBalance).to.eql(FirstBalance);",
                  "  });",
                  "})().catch(ErrorObject => {",
                  "  pm.test('TC-004 - daily bonus verification completes', () => { throw ErrorObject; });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc004Token}}"
              }
            ],
            "url": "{{baseUrl}}/user/daily-login"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-005 - User cannot use a session owned by another user",
      "item": [
        {
          "name": "TC-005 - User B cannot cash out user A session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? Reject(ErrorObject) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-005 - ' + Name, function () {",
                  "    try { Assertion(); } catch (ErrorObject) { Failure = ErrorObject; throw ErrorObject; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.json();",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc005TokenA', 'tc005TokenB', 'tc005SessionId', 'tc005MachineId', 'tc005Before']) pm.variables.unset(Key);",
                  "  const Email = pm.environment.get('tc005AdminEmail');",
                  "  const Password = pm.environment.get('tc005AdminPassword');",
                  "  if (!Email || !Password) throw new Error('Run npm run test:api:tc005 for automatic setup, or provide test administrator credentials.');",
                  "  const AdminLogin = await CallApi('POST', '/auth/login', { Email: Email, Password: Password });",
                  "  Check('machine setup authenticates an administrator', () => {",
                  "    pm.expect(AdminLogin.code).to.eql(200);",
                  "    pm.expect(AdminLogin.json().User.Email).to.eql(Email);",
                  "    pm.expect(AdminLogin.json().User.UserType).to.eql('Admin');",
                  "    pm.expect(AdminLogin.json().Token).to.be.a('string').and.not.empty;",
                  "  });",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}');",
                  "  const MachineName = pm.environment.get('tc005SlotMachineName') || ('TC005 Slot Machine ' + Suffix);",
                  "  const Machine = await CallApi('POST', '/slot/machine', { Name: MachineName, Description: 'TC-005 isolated ownership test', MinimumSpinValue: 0, MinimumChipsRequired: 0, MinimumRerollValue: 0 }, AdminLogin.json().Token);",
                  "  Check('dedicated machine is created through the API', () => {",
                  "    pm.expect(Machine.code).to.eql(201);",
                  "    pm.expect(Machine.json().Name).to.eql(MachineName);",
                  "    pm.expect(Machine.json().Active).to.eql(true);",
                  "    pm.expect(Number.isInteger(Machine.json().SlotMachineId)).to.eql(true);",
                  "    pm.expect(Machine.json().SlotMachineId).to.be.above(0);",
                  "  });",
                  "  const MachineId = Machine.json().SlotMachineId;",
                  "  pm.variables.set('tc005MachineId', MachineId);",
                  "  async function CreateUser(Label) {",
                  "    const Email = pm.environment.get('tc005User' + Label + 'Email') || ('tc005.' + Label.toLowerCase() + '.' + Suffix + '@example.test');",
                  "    const Password = pm.variables.replaceIn('{{$guid}}') + 'Aa1!';",
                  "    const Registered = await CallApi('POST', '/auth/register', { Name: 'TC005 User ' + Label, BirthDate: '1995-01-01', Nickname: 'tc005' + Label + Suffix, Email: Email, Password: Password });",
                  "    Check('user ' + Label + ' is registered as a regular user', () => {",
                  "      pm.expect(Registered.code).to.eql(201);",
                  "      pm.expect(Registered.json().User.Email).to.eql(Email);",
                  "      pm.expect(Registered.json().User.UserType).to.eql('User');",
                  "    });",
                  "    const Login = await CallApi('POST', '/auth/login', { Email: Email, Password: Password });",
                  "    Check('user ' + Label + ' logs in and obtains its own JWT', () => {",
                  "      pm.expect(Login.code).to.eql(200);",
                  "      pm.expect(Login.json().User.UserId).to.eql(Registered.json().User.UserId);",
                  "      pm.expect(Login.json().User.UserType).to.eql('User');",
                  "      pm.expect(Login.json().Token).to.be.a('string').and.not.empty;",
                  "      pm.expect(Login.json().Token.split('.')).to.have.length(3);",
                  "    });",
                  "    return { id: Login.json().User.UserId, token: Login.json().Token };",
                  "  }",
                  "  const A = await CreateUser('A');",
                  "  pm.variables.set('tc005TokenA', A.token);",
                  "  const Created = await CallApi('POST', '/slot-machines/' + MachineId + '/sessions', {}, A.token);",
                  "  Check('A creates a real in-progress session with a generated ID', () => {",
                  "    pm.expect(Created.code).to.eql(201);",
                  "    const Session = Created.json().session;",
                  "    pm.expect(Session.UserId).to.eql(A.id);",
                  "    pm.expect(Session.SlotMachineId).to.eql(MachineId);",
                  "    pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Number.isInteger(Session.SlotSessionId)).to.eql(true);",
                  "    pm.expect(Session.SlotSessionId).to.be.above(0);",
                  "  });",
                  "  const SessionId = Created.json().session.SlotSessionId;",
                  "  pm.variables.set('tc005SessionId', SessionId);",
                  "  const B = await CreateUser('B');",
                  "  pm.variables.set('tc005TokenB', B.token);",
                  "  Check('A and B have different identities and tokens', () => {",
                  "    pm.expect(A.id).to.not.eql(B.id);",
                  "    pm.expect(A.token).to.not.eql(B.token);",
                  "  });",
                  "  const ProfileB = await Read('/user/profile', B.token, 'B authenticated profile');",
                  "  Check('B token authenticates B independently of the rejected action', () => {",
                  "    pm.expect(ProfileB.UserId).to.eql(B.id);",
                  "    pm.expect(ProfileB.UserType).to.eql('User');",
                  "  });",
                  "  const Session = await Read('/sessions/active', A.token, 'A session before attempt');",
                  "  const Registry = await Read('/sessions/me/active', A.token, 'A registry before attempt');",
                  "  const BalanceA = await Read('/user/chips', A.token, 'A balance before attempt');",
                  "  const BalanceB = await Read('/user/chips', B.token, 'B balance before attempt');",
                  "  Check('baseline identifies the actual session owned by A', () => {",
                  "    pm.expect(Session).to.deep.equal(Created.json().session);",
                  "    pm.expect(Session.UserId).to.eql(A.id);",
                  "    pm.expect(Registry.UserId).to.eql(A.id);",
                  "    pm.expect(Registry.ReferenceId).to.eql(SessionId);",
                  "    pm.expect(Registry.GameType).to.eql('Slot');",
                  "    pm.expect(BalanceA.ChipBalance).to.be.a('number');",
                  "    pm.expect(BalanceB.ChipBalance).to.be.a('number');",
                  "  });",
                  "  pm.variables.set('tc005Before', JSON.stringify({ session: Session, registry: Registry, balanceA: BalanceA, balanceB: BalanceB }));",
                  "})().catch(ErrorObject => {",
                  "  pm.test('TC-005 - preparation completes', () => { throw ErrorObject; });",
                  "  pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? Reject(ErrorObject) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-005 - ' + Name, function () {",
                  "    try { Assertion(); } catch (ErrorObject) { Failure = ErrorObject; throw ErrorObject; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.json();",
                  "}",
                  "",
                  "(async function () {",
                  "  const TokenA = pm.variables.get('tc005TokenA');",
                  "  const TokenB = pm.variables.get('tc005TokenB');",
                  "  const Before = JSON.parse(pm.variables.get('tc005Before'));",
                  "  const Path = '/slot-machines/' + Before.session.SlotMachineId + '/sessions/' + Before.session.SlotSessionId + '/cash-out';",
                  "  Check('B sends its own JWT to the exact session created by A', () => {",
                  "    pm.expect(TokenB).to.not.eql(TokenA);",
                  "    pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + TokenB);",
                  "    pm.expect(pm.variables.replaceIn(pm.request.url.toString())).to.eql(pm.environment.get('baseUrl') + Path);",
                  "    pm.expect(Number(pm.variables.get('tc005SessionId'))).to.eql(Before.session.SlotSessionId);",
                  "  });",
                  "  Check('foreign session cash-out returns the implemented 404 error', () => {",
                  "    pm.response.to.have.status(404);",
                  "    pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json');",
                  "    pm.expect(pm.response.json()).to.deep.equal({ message: 'Session not found', error: 'Not Found', statusCode: 404 });",
                  "  });",
                  "  const After = await Read('/sessions/active', TokenA, 'A session after attempt');",
                  "  Check('all publicly returned session fields remain unchanged', () => pm.expect(After).to.deep.equal(Before.session));",
                  "  const Registry = await Read('/sessions/me/active', TokenA, 'A registry after attempt');",
                  "  Check('A active-session registration remains unchanged', () => pm.expect(Registry).to.deep.equal(Before.registry));",
                  "  const BalanceA = await Read('/user/chips', TokenA, 'A balance after attempt');",
                  "  const BalanceB = await Read('/user/chips', TokenB, 'B balance after attempt');",
                  "  Check('neither user balance changed during the foreign cash-out', () => {",
                  "    pm.expect(BalanceA).to.deep.equal(Before.balanceA);",
                  "    pm.expect(BalanceB).to.deep.equal(Before.balanceB);",
                  "  });",
                  "  const SessionsB = await Read('/slot-machines/' + Before.session.SlotMachineId + '/sessions', TokenB, 'B session list');",
                  "  Check('B cannot see A session in its own session list', () => pm.expect(SessionsB).to.deep.equal([]));",
                  "  const OwnerCashOut = await CallApi('POST', Path, undefined, TokenA);",
                  "  Check('owner A can cash out the same session successfully', () => {",
                  "    pm.expect(OwnerCashOut.code).to.eql(201);",
                  "    pm.expect(OwnerCashOut.json()).to.deep.equal({ message: 'Cash out successful', finalBalance: Before.balanceA.ChipBalance + Before.session.CurrentRewardSnapshot });",
                  "  });",
                  "})().catch(ErrorObject => {",
                  "  pm.test('TC-005 - ownership verification completes', () => { throw ErrorObject; });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc005TokenB}}"
              }
            ],
            "url": "{{baseUrl}}/slot-machines/{{tc005MachineId}}/sessions/{{tc005SessionId}}/cash-out"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-011 - Daily bonus tops up the minimum balance and unlocks the slot session",
      "description": "Categoria: Dados Validos / Caminho Feliz.",
      "item": [
        {
          "name": "TC-011 - Slot session is accepted after the daily bonus covers the minimum",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "",
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-011 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Looks the seeded table up by name. It avoids a fixed id because the id",
                  "// depends on insertion order, which varies between databases.",
                  "async function findSeedTable(route, tableName) {",
                  "  const lista = await callApi('GET', route);",
                  "  expectCode(lista, 200, 'listagem de ' + route);",
                  "  const table = lista.json().filter(function (m) { return m.Name === tableName; })[0];",
                  "  if (!table) {",
                  "    throw new Error('Table \"' + tableName + '\" not found. It comes from the application seed.');",
                  "  }",
                  "  return table;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const key of ['tc011MachineId', 'tc011PlayerToken', 'tc011Expected']) pm.environment.unset(key);",
                  "",
                  "  // Slot 3 is the seeded table with the highest entry (30 chips). The reward",
                  "  // of the first daily bonus claim is 50, so it covers the entry with room",
                  "  // to spare, while a zero balance does not.",
                  "  const table = await findSeedTable('/slot/machine', 'Slot 3');",
                  "  pm.environment.set('tc011MachineId', table.SlotMachineId);",
                  "",
                  "  const entryCost = table.MinimumSpinValue;",
                  "  const reward = 50;",
                  "",
                  "  check('the seeded table charges an entry the daily bonus can cover', function () {",
                  "    pm.expect(entryCost).to.be.at.most(reward);",
                  "    pm.expect(entryCost).to.be.above(0);",
                  "  });",
                  "",
                  "  const playerToken = await registerPlayer('tc011', 'A');",
                  "  pm.environment.set('tc011PlayerToken', playerToken);",
                  "",
                  "  const profile = await callApi('GET', '/user/profile', undefined, playerToken);",
                  "  expectCode(profile, 200, 'player profile');",
                  "",
                  "  check('the player starts with no chips', function () {",
                  "    pm.expect(profile.json().ChipBalance).to.eql(0);",
                  "  });",
                  "",
                  "  // Eligible to claim: a brand new account never claimed, so LastLoginDate",
                  "  // is still null and the streak is zeroed.",
                  "  check('the player is eligible to claim the bonus at this point', function () {",
                  "    pm.expect(profile.json().LastLoginDate).to.be.null;",
                  "    pm.expect(profile.json().DailyLoginStreak).to.eql(0);",
                  "  });",
                  "",
                  "  const blocked = await callApi('POST', '/slot-machines/' + table.SlotMachineId + '/sessions', {}, playerToken);",
                  "  check('the session is refused while the balance does not cover the entry', function () {",
                  "    pm.expect(blocked.code).to.eql(400);",
                  "    pm.expect(JSON.stringify(blocked.json().message)).to.include('Insufficient chips');",
                  "  });",
                  "  check('the refusal creates no session', function () {",
                  "    pm.expect(blocked.json()).to.not.have.property('session');",
                  "  });",
                  "",
                  "  const bonus = await callApi('POST', '/user/daily-login', undefined, playerToken);",
                  "  expectCode(bonus, 201, 'daily bonus');",
                  "  check('the bonus is granted as the first claim of the day', function () {",
                  "    pm.expect(bonus.json().FirstLoginToday).to.eql(true);",
                  "    pm.expect(bonus.json().DailyStreak).to.eql(1);",
                  "    pm.expect(bonus.json().Reward).to.eql(reward);",
                  "  });",
                  "  check('the balance now covers the table entry', function () {",
                  "    pm.expect(bonus.json().ChipBalance).to.eql(reward);",
                  "    pm.expect(bonus.json().ChipBalance).to.be.at.least(entryCost);",
                  "  });",
                  "",
                  "  pm.environment.set('tc011Expected', String(reward - entryCost));",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "",
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-011 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Looks the seeded table up by name. It avoids a fixed id because the id",
                  "// depends on insertion order, which varies between databases.",
                  "async function findSeedTable(route, tableName) {",
                  "  const lista = await callApi('GET', route);",
                  "  expectCode(lista, 200, 'listagem de ' + route);",
                  "  const table = lista.json().filter(function (m) { return m.Name === tableName; })[0];",
                  "  if (!table) {",
                  "    throw new Error('Table \"' + tableName + '\" not found. It comes from the application seed.');",
                  "  }",
                  "  return table;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "check('the same request is now accepted', function () {",
                  "  pm.response.to.have.status(201);",
                  "});",
                  "",
                  "check('the response returns the created session', function () {",
                  "  pm.expect(body).to.have.property('session');",
                  "  pm.expect(body.session.Status).to.eql('InProgress');",
                  "});",
                  "",
                  "// What is left is the bonus reward minus the entry charged by the seeded table.",
                  "check('the table entry is debited from the balance', function () {",
                  "  pm.expect(body.currentBalance).to.eql(Number(pm.environment.get('tc011Expected')));",
                  "});",
                  "",
                  "(async function () {",
                  "  const token = pm.environment.get('tc011PlayerToken');",
                  "  const active = await callApi('GET', '/sessions/active', undefined, token);",
                  "  check('the session becomes the active one for the player', function () {",
                  "    pm.expect(active.code).to.eql(200);",
                  "    pm.expect(active.json().SlotSessionId).to.eql(body.session.SlotSessionId);",
                  "  });",
                  "",
                  "  // Close it so the seeded table is not left with a dangling session.",
                  "  await callApi('POST', '/sessions/active/cash-out', undefined, token);",
                  "})();"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{tc011PlayerToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/slot-machines/{{tc011MachineId}}/sessions",
              "host": ["{{baseUrl}}"],
              "path": ["slot-machines", "{{tc011MachineId}}", "sessions"]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        }
      ]
    },
    {
      "name": "TC-012 - Table can only be deleted after the administrative cash-out",
      "description": "Categoria: Dados Invalidos / Inoportunos.",
      "item": [
        {
          "name": "TC-012 - Slot machine is deleted once the administrator closed every session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  // Each case chains several calls, so a transport failure in the middle",
                  "  // must not fail the test. Retry before giving up.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-012 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "async function loginAdmin() {",
                  "  const email = pm.environment.get('adminEmail');",
                  "  const password = pm.environment.get('adminPassword');",
                  "  if (!email || !password) throw new Error('Run: npm run test:api:seed');",
                  "  const login = await callApi('POST', '/auth/login', { Email: email, Password: password });",
                  "  expectCode(login, 200, 'admin login');",
                  "  return login.json().Token;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const key of ['tc012MachineId', 'tc012AdminToken']) pm.environment.unset(key);",
                  "",
                  "  const adminToken = await loginAdmin();",
                  "  pm.environment.set('tc012AdminToken', adminToken);",
                  "",
                  "  const machine = await callApi('POST', '/slot/machine', {",
                  "    Name: 'TC012 Slot Machine ' + UNIQUE,",
                  "    Description: 'Table used by TC-012',",
                  "    MinimumSpinValue: 10,",
                  "    MinimumRerollValue: 10",
                  "  }, adminToken);",
                  "  expectCode(machine, 201, 'slot machine creation');",
                  "  const machineId = machine.json().SlotMachineId;",
                  "  pm.environment.set('tc012MachineId', machineId);",
                  "",
                  "  const playerToken = await registerPlayer('tc012', 'A', 1000);",
                  "",
                  "  const session = await callApi('POST', '/slot-machines/' + machineId + '/sessions', {}, playerToken);",
                  "  expectCode(session, 201, 'player session');",
                  "  check('a session is running on the machine', function () {",
                  "    pm.expect(session.json().session.Status).to.eql('InProgress');",
                  "  });",
                  "",
                  "  const blocked = await callApi('DELETE', '/slot/machine/' + machineId, undefined, adminToken);",
                  "  check('deleting the machine is refused while a session is running', function () {",
                  "    pm.expect(blocked.code).to.eql(400);",
                  "    pm.expect(JSON.stringify(blocked.json().message)).to.include('active sessions');",
                  "  });",
                  "",
                  "  const stillThere = await callApi('GET', '/slot/machine/' + machineId, undefined, adminToken);",
                  "  check('the refused delete leaves the machine untouched', function () {",
                  "    pm.expect(stillThere.code).to.eql(200);",
                  "    pm.expect(stillThere.json().SlotMachineId).to.eql(machineId);",
                  "  });",
                  "",
                  "  const deactivate = await callApi('POST', '/admin/slot-machines/' + machineId + '/deactivate', undefined, adminToken);",
                  "  expectCode(deactivate, 201, 'administrative cash-out');",
                  "  check('the administrative cash-out closes the running session', function () {",
                  "    pm.expect(deactivate.json().ClosedSessions).to.eql(1);",
                  "    pm.expect(deactivate.json()).to.have.property('ChipsReturned');",
                  "  });",
                  "",
                  "  const active = await callApi('GET', '/sessions/active', undefined, playerToken);",
                  "  check('the player is left without an active session', function () {",
                  "    pm.expect(active.code).to.eql(200);",
                  "    pm.expect(active.text()).to.be.oneOf(['', 'null']);",
                  "  });",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  // Each case chains several calls, so a transport failure in the middle",
                  "  // must not fail the test. Retry before giving up.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-012 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "async function loginAdmin() {",
                  "  const email = pm.environment.get('adminEmail');",
                  "  const password = pm.environment.get('adminPassword');",
                  "  if (!email || !password) throw new Error('Run: npm run test:api:seed');",
                  "  const login = await callApi('POST', '/auth/login', { Email: email, Password: password });",
                  "  expectCode(login, 200, 'admin login');",
                  "  return login.json().Token;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "check('the machine can be deleted once no session is running', function () {",
                  "  pm.response.to.have.status(200);",
                  "  pm.expect(pm.response.json().message).to.include('removed successfully');",
                  "});",
                  "",
                  "(async function () {",
                  "  const adminToken = pm.environment.get('tc012AdminToken');",
                  "  const gone = await callApi('GET', '/slot/machine/' + pm.environment.get('tc012MachineId'), undefined, adminToken);",
                  "  check('the machine is really gone after the delete', function () {",
                  "    pm.expect(gone.code).to.eql(404);",
                  "  });",
                  "})();"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc012AdminToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/slot/machine/{{tc012MachineId}}",
              "host": ["{{baseUrl}}"],
              "path": ["slot", "machine", "{{tc012MachineId}}"]
            }
          }
        }
      ]
    },
    {
      "name": "TC-013 - Reroll charges the table value and is refused without balance or rerolls",
      "description": "Categoria: Dados Invalidos / Inoportunos.",
      "item": [
        {
          "name": "TC-013 - Reroll is refused once the available rerolls run out",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "",
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-013 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Looks the seeded table up by name. It avoids a fixed id because the id",
                  "// depends on insertion order, which varies between databases.",
                  "async function findSeedTable(route, tableName) {",
                  "  const lista = await callApi('GET', route);",
                  "  expectCode(lista, 200, 'listagem de ' + route);",
                  "  const table = lista.json().filter(function (m) { return m.Name === tableName; })[0];",
                  "  if (!table) {",
                  "    throw new Error('Table \"' + tableName + '\" not found. It comes from the application seed.');",
                  "  }",
                  "  return table;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const key of ['tc013RichToken', 'tc013MachineId', 'tc013Reroll']) pm.environment.unset(key);",
                  "",
                  "  const table = await findSeedTable('/slot/machine', 'Slot 2');",
                  "  pm.environment.set('tc013MachineId', table.SlotMachineId);",
                  "",
                  "  const entryCost = table.MinimumSpinValue;",
                  "  const reroll = table.MinimumRerollValue;",
                  "  pm.environment.set('tc013Reroll', String(reroll));",
                  "",
                  "  check('the seeded table charges both entry and reroll', function () {",
                  "    pm.expect(entryCost).to.be.above(0);",
                  "    pm.expect(reroll).to.be.above(0);",
                  "  });",
                  "",
                  "  // --- Player A: balance sized to run out after exactly 2 rerolls.",
                  "  const balanceA = entryCost + 2 * reroll + 1;",
                  "  const brokeToken = await registerPlayer('tc013', 'A', balanceA);",
                  "  const brokeSession = await callApi('POST', '/slot-machines/' + table.SlotMachineId + '/sessions', {}, brokeToken);",
                  "  expectCode(brokeSession, 201, 'player A session');",
                  "",
                  "  check('the entry is debited when the session opens', function () {",
                  "    pm.expect(brokeSession.json().currentBalance).to.eql(balanceA - entryCost);",
                  "  });",
                  "  check('the session starts with 5 available rerolls', function () {",
                  "    pm.expect(brokeSession.json().session.CurrentRerollsSpent.Rerolls.Max).to.eql(5);",
                  "    pm.expect(brokeSession.json().session.CurrentRerollsSpent.Rerolls.Used).to.eql(0);",
                  "  });",
                  "",
                  "  let balance = balanceA - entryCost;",
                  "  for (let i = 1; i <= 2; i += 1) {",
                  "    const r = await callApi('POST', '/sessions/active/reroll/0', undefined, brokeToken);",
                  "    expectCode(r, 201, 'player A reroll ' + i);",
                  "    const charged = balance - r.json().currentBalance;",
                  "    check('reroll ' + i + ' charges the table value', function () {",
                  "      pm.expect(charged).to.eql(reroll);",
                  "      pm.expect(r.json().session.CurrentRerollsSpent.Rerolls.Used).to.eql(i);",
                  "    });",
                  "    balance = r.json().currentBalance;",
                  "  }",
                  "",
                  "  const semSaldo = await callApi('POST', '/sessions/active/reroll/0', undefined, brokeToken);",
                  "  check('a further reroll is refused once the balance no longer covers the cost', function () {",
                  "    pm.expect(balance).to.be.below(reroll);",
                  "    pm.expect(semSaldo.code).to.eql(400);",
                  "    pm.expect(JSON.stringify(semSaldo.json().message)).to.include('Insufficient chips to reroll');",
                  "  });",
                  "",
                  "  const aindaAtiva = await callApi('GET', '/sessions/active', undefined, brokeToken);",
                  "  check('the balance refusal leaves the session active', function () {",
                  "    pm.expect(aindaAtiva.code).to.eql(200);",
                  "    pm.expect(aindaAtiva.json().Status).to.eql('InProgress');",
                  "    pm.expect(aindaAtiva.json().CurrentRerollsSpent.Rerolls.Used).to.eql(2);",
                  "  });",
                  "",
                  "  // Close A session so the seeded table is not left busy.",
                  "  await callApi('POST', '/sessions/active/cash-out', undefined, brokeToken);",
                  "",
                  "  // --- Player B: balance to spare, spends all 5 available rerolls.",
                  "  const balanceB = entryCost + 5 * reroll + 1000;",
                  "  const richToken = await registerPlayer('tc013', 'B', balanceB);",
                  "  pm.environment.set('tc013RichToken', richToken);",
                  "",
                  "  const richSession = await callApi('POST', '/slot-machines/' + table.SlotMachineId + '/sessions', {}, richToken);",
                  "  expectCode(richSession, 201, 'player B session');",
                  "",
                  "  let ultimo;",
                  "  for (let i = 0; i < 5; i += 1) {",
                  "    ultimo = await callApi('POST', '/sessions/active/reroll/0', undefined, richToken);",
                  "    expectCode(ultimo, 201, 'player B reroll ' + (i + 1));",
                  "  }",
                  "",
                  "  check('the five available rerolls are spent', function () {",
                  "    pm.expect(ultimo.json().session.CurrentRerollsSpent.Rerolls.Used).to.eql(5);",
                  "    pm.expect(ultimo.json().session.CurrentRerollsSpent.Rerolls.Max).to.eql(5);",
                  "  });",
                  "  check('the player still has balance, so only the limit can block', function () {",
                  "    pm.expect(ultimo.json().currentBalance).to.be.above(reroll);",
                  "  });",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "",
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-013 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Looks the seeded table up by name. It avoids a fixed id because the id",
                  "// depends on insertion order, which varies between databases.",
                  "async function findSeedTable(route, tableName) {",
                  "  const lista = await callApi('GET', route);",
                  "  expectCode(lista, 200, 'listagem de ' + route);",
                  "  const table = lista.json().filter(function (m) { return m.Name === tableName; })[0];",
                  "  if (!table) {",
                  "    throw new Error('Table \"' + tableName + '\" not found. It comes from the application seed.');",
                  "  }",
                  "  return table;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "check('a further reroll is refused once the available rerolls run out', function () {",
                  "  pm.response.to.have.status(400);",
                  "  pm.expect(JSON.stringify(pm.response.json().message)).to.include('No rerolls left');",
                  "});",
                  "",
                  "(async function () {",
                  "  const token = pm.environment.get('tc013RichToken');",
                  "  const active = await callApi('GET', '/sessions/active', undefined, token);",
                  "",
                  "  check('the exhausted session stays active', function () {",
                  "    pm.expect(active.code).to.eql(200);",
                  "    pm.expect(active.json().Status).to.eql('InProgress');",
                  "  });",
                  "  check('no sixth reroll is counted', function () {",
                  "    pm.expect(active.json().CurrentRerollsSpent.Rerolls.Used).to.eql(5);",
                  "  });",
                  "",
                  "  // The session is active and playable: cash-out stays available.",
                  "  const cashOut = await callApi('POST', '/sessions/active/cash-out', undefined, token);",
                  "  check('the session can still be cashed out after the refusal', function () {",
                  "    pm.expect(cashOut.code).to.eql(201);",
                  "    pm.expect(cashOut.json().message).to.include('Cash out successful');",
                  "  });",
                  "})();"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc013RichToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/sessions/active/reroll/0",
              "host": ["{{baseUrl}}"],
              "path": ["sessions", "active", "reroll", "0"]
            }
          }
        }
      ]
    },
    {
      "name": "TC-014 - Gambit session never exposes cards that are not yet revealed",
      "description": "Categoria: Dados Validos / Caminho Feliz.",
      "item": [
        {
          "name": "TC-014 - Cash-out closes the session without ever leaking hidden cards",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "",
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-014 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Looks the seeded table up by name. It avoids a fixed id because the id",
                  "// depends on insertion order, which varies between databases.",
                  "async function findSeedTable(route, tableName) {",
                  "  const lista = await callApi('GET', route);",
                  "  expectCode(lista, 200, 'listagem de ' + route);",
                  "  const table = lista.json().filter(function (m) { return m.Name === tableName; })[0];",
                  "  if (!table) {",
                  "    throw new Error('Table \"' + tableName + '\" not found. It comes from the application seed.');",
                  "  }",
                  "  return table;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const key of ['tc014PlayerToken', 'tc014Violations', 'tc014Steps']) pm.environment.unset(key);",
                  "",
                  "  const table = await findSeedTable('/gambit-table', 'Gambit 1');",
                  "  const cards = Math.min(12, table.MaxCardsPurchased);",
                  "",
                  "  check('the seeded table accepts the card count this case buys', function () {",
                  "    pm.expect(cards).to.be.at.least(table.MinimumCardsPurchased);",
                  "    pm.expect(cards).to.be.at.most(table.MaxCardsPurchased);",
                  "  });",
                  "",
                  "  // Balance with room to spare: it must cover the table MinimumChipsRequired",
                  "  // and the cost of the purchased cards.",
                  "  const balance = (table.MinimumChipsRequired || 0) + cards * table.CardPrice + 1000;",
                  "  const playerToken = await registerPlayer('tc014', 'A', balance);",
                  "  pm.environment.set('tc014PlayerToken', playerToken);",
                  "",
                  "  const violations = [];",
                  "  let previous = null;",
                  "  let passos = 0;",
                  "",
                  "  // The API may only expose Position and Locked for cards not yet revealed.",
                  "  // Points and Effect stay hidden until the card is burned.",
                  "  function auditar(view, step) {",
                  "    passos += 1;",
                  "",
                  "    for (const card of view.Grid.Unrevealed) {",
                  "      const keys = Object.keys(card).sort();",
                  "      if (keys.join(',') !== 'Locked,Position') {",
                  "        violations.push(step + ': position ' + card.Position + ' exposed ' + keys.join(','));",
                  "      }",
                  "    }",
                  "",
                  "    const revealed = view.Grid.Revealed.map(function (c) { return c.Position; });",
                  "    const hidden = view.Grid.Unrevealed.map(function (c) { return c.Position; });",
                  "",
                  "    if (revealed.length + hidden.length !== 25) {",
                  "      violations.push(step + ': the grid no longer adds up to 25 positions');",
                  "    }",
                  "    if (hidden.some(function (p) { return revealed.indexOf(p) !== -1; })) {",
                  "      violations.push(step + ': a position appears revealed and hidden at the same time');",
                  "    }",
                  "    if (view.BurnsRemaining !== Math.max(0, view.BurnSlotsAvailable - view.ManualFlipsCount)) {",
                  "      violations.push(step + ': BurnsRemaining does not match the burns performed');",
                  "    }",
                  "",
                  "    if (previous) {",
                  "      if (view.GambitSessionId !== previous.GambitSessionId) violations.push(step + ': the session changed id');",
                  "      if (view.CardsPurchased !== previous.CardsPurchased) violations.push(step + ': CardsPurchased mudou no meio da partida');",
                  "      if (view.Grid.Revealed.length < previous.Grid.Revealed.length) violations.push(step + ': a revealed card became hidden again');",
                  "    }",
                  "    previous = view;",
                  "    return view;",
                  "  }",
                  "",
                  "  const session = await callApi('POST', '/gambit-tables/' + table.GambitTableId + '/sessions', { CardsPurchased: cards }, playerToken);",
                  "  expectCode(session, 201, 'gambit session');",
                  "  let view = auditar(session.json().session, 'criacao');",
                  "",
                  "  let guard = 0;",
                  "  while (view.Status === 'InProgress' && guard < 40) {",
                  "    guard += 1;",
                  "",
                  "    if (view.Grid.PendingInteraction) {",
                  "      const needed = view.Grid.PendingInteraction.RequiredSelections;",
                  "      const positions = view.Grid.Unrevealed.filter(function (c) { return !c.Locked; })",
                  "        .slice(0, needed).map(function (c) { return c.Position; });",
                  "      const resolved = await callApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: positions }, playerToken);",
                  "      expectCode(resolved, 201, 'resolve effect');",
                  "      view = auditar(resolved.json(), 'resolve-effect');",
                  "      continue;",
                  "    }",
                  "",
                  "    if (view.Grid.PendingEvent) {",
                  "      const resolved = await callApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: 0, BadIndex: 0 }, playerToken);",
                  "      expectCode(resolved, 201, 'resolve event');",
                  "      view = auditar(resolved.json(), 'resolve-event');",
                  "      continue;",
                  "    }",
                  "",
                  "    const target = view.Grid.Unrevealed.filter(function (c) { return !c.Locked; })[0];",
                  "    if (!target) break;",
                  "    const burned = await callApi('POST', '/gambit/sessions/active/burn/' + target.Position, undefined, playerToken);",
                  "    expectCode(burned, 201, 'burn at position ' + target.Position);",
                  "    view = auditar(burned.json(), 'burn');",
                  "  }",
                  "",
                  "  check('the session runs to the end within the purchased burns', function () {",
                  "    pm.expect(view.Status, 'the session did not finish in ' + guard + ' actions').to.eql('Finished');",
                  "  });",
                  "  check('real play happened, with revealed cards', function () {",
                  "    pm.expect(view.Grid.PendingEvent).to.be.null;",
                  "    pm.expect(view.Grid.Revealed.length).to.be.above(0);",
                  "  });",
                  "",
                  "  pm.environment.set('tc014Violations', JSON.stringify(violations));",
                  "  pm.environment.set('tc014Steps', String(passos));",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "",
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-014 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Looks the seeded table up by name. It avoids a fixed id because the id",
                  "// depends on insertion order, which varies between databases.",
                  "async function findSeedTable(route, tableName) {",
                  "  const lista = await callApi('GET', route);",
                  "  expectCode(lista, 200, 'listagem de ' + route);",
                  "  const table = lista.json().filter(function (m) { return m.Name === tableName; })[0];",
                  "  if (!table) {",
                  "    throw new Error('Table \"' + tableName + '\" not found. It comes from the application seed.');",
                  "  }",
                  "  return table;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "const violations = JSON.parse(pm.environment.get('tc014Violations'));",
                  "const passos = Number(pm.environment.get('tc014Steps'));",
                  "",
                  "check('every session step was audited', function () {",
                  "  pm.expect(passos).to.be.above(5);",
                  "});",
                  "",
                  "check('no hidden card exposed anything beyond position and lock', function () {",
                  "  pm.expect(violations, violations.join(' | ')).to.be.empty;",
                  "});",
                  "",
                  "check('the cash-out of the finished session is accepted', function () {",
                  "  pm.response.to.have.status(201);",
                  "  pm.expect(pm.response.json().message).to.include('Cash out successful');",
                  "});",
                  "",
                  "check('the cash-out reports the reward and the final balance', function () {",
                  "  const body = pm.response.json();",
                  "  pm.expect(body.reward).to.be.at.least(0);",
                  "  pm.expect(body).to.have.property('finalBalance');",
                  "});",
                  "",
                  "// Not even the closing call may return grid data.",
                  "check('the cash-out response does not contain the grid', function () {",
                  "  pm.expect(pm.response.json()).to.not.have.property('Grid');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc014PlayerToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/gambit/sessions/active/cash-out",
              "host": ["{{baseUrl}}"],
              "path": ["gambit", "sessions", "active", "cash-out"]
            }
          }
        }
      ]
    },
    {
      "name": "TC-015 - Table changes do not affect the running session, only later ones",
      "description": "Categoria: Dados Validos / Caminho Feliz.",
      "item": [
        {
          "name": "TC-015 - A session opened after the change pays the updated entry cost",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  // Each case chains several calls, so a transport failure in the middle",
                  "  // must not fail the test. Retry before giving up.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-015 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "async function loginAdmin() {",
                  "  const email = pm.environment.get('adminEmail');",
                  "  const password = pm.environment.get('adminPassword');",
                  "  if (!email || !password) throw new Error('Run: npm run test:api:seed');",
                  "  const login = await callApi('POST', '/auth/login', { Email: email, Password: password });",
                  "  expectCode(login, 200, 'admin login');",
                  "  return login.json().Token;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const key of ['tc015MachineId', 'tc015RichToken']) pm.environment.unset(key);",
                  "",
                  "  const adminToken = await loginAdmin();",
                  "  pm.environment.set('tc015AdminToken', adminToken);",
                  "",
                  "  const machine = await callApi('POST', '/slot/machine', {",
                  "    Name: 'TC015 Slot Machine ' + UNIQUE,",
                  "    Description: 'Table used by TC-015',",
                  "    MinimumSpinValue: 50,",
                  "    MinimumRerollValue: 10",
                  "  }, adminToken);",
                  "  expectCode(machine, 201, 'slot machine creation');",
                  "  const machineId = machine.json().SlotMachineId;",
                  "  pm.environment.set('tc015MachineId', machineId);",
                  "",
                  "  // --- Player A joins BEFORE the change, paying the entry of 50. ---",
                  "  const veteranToken = await registerPlayer('tc015', 'A', 1000);",
                  "  const session = await callApi('POST', '/slot-machines/' + machineId + '/sessions', {}, veteranToken);",
                  "  expectCode(session, 201, 'player A session');",
                  "  check('the session opens under the original entry cost', function () {",
                  "    pm.expect(session.json().currentBalance).to.eql(950);",
                  "  });",
                  "",
                  "  // --- The admin raises the entry in the middle of the game. ---",
                  "  const updated = await callApi('PUT', '/slot/machine/' + machineId, { MinimumSpinValue: 900 }, adminToken);",
                  "  expectCode(updated, 200, 'machine update');",
                  "  check('the administrator changes the entry cost while the session runs', function () {",
                  "    pm.expect(updated.json().MinimumSpinValue).to.eql(900);",
                  "    pm.expect(updated.json().Active).to.eql(true);",
                  "  });",
                  "",
                  "  // --- Player A keeps playing normally. ---",
                  "  const reroll = await callApi('POST', '/sessions/active/reroll/0', undefined, veteranToken);",
                  "  check('the running session keeps accepting rerolls after the change', function () {",
                  "    pm.expect(reroll.code).to.eql(201);",
                  "    pm.expect(reroll.json().session.CurrentRerollsSpent.Rerolls.Used).to.eql(1);",
                  "  });",
                  "  check('the running session is not charged the new entry cost', function () {",
                  "    pm.expect(reroll.json().currentBalance).to.eql(940);",
                  "  });",
                  "",
                  "  const cashOut = await callApi('POST', '/sessions/active/cash-out', undefined, veteranToken);",
                  "  check('the running session can still be cashed out after the change', function () {",
                  "    pm.expect(cashOut.code).to.eql(201);",
                  "    pm.expect(cashOut.json().message).to.include('Cash out successful');",
                  "  });",
                  "",
                  "  // --- Player B opens AFTER, without balance for the new entry. ---",
                  "  const poorToken = await registerPlayer('tc015', 'B', 500);",
                  "  const refused = await callApi('POST', '/slot-machines/' + machineId + '/sessions', {}, poorToken);",
                  "  check('a session opened after the change is measured by the new entry cost', function () {",
                  "    pm.expect(refused.code).to.eql(400);",
                  "    pm.expect(JSON.stringify(refused.json().message)).to.include('Insufficient chips');",
                  "  });",
                  "",
                  "  // --- Player C opens AFTER, with enough balance for the new entry. ---",
                  "  const richToken = await registerPlayer('tc015', 'C', 1000);",
                  "  pm.environment.set('tc015RichToken', richToken);",
                  "})();"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function callApi(method, path, body, token, tentativa) {",
                  "  const header = { 'Content-Type': 'application/json' };",
                  "  if (token) header.Authorization = 'Bearer ' + token;",
                  "  const request = { method, url: pm.environment.get('baseUrl') + path, header };",
                  "  if (body !== undefined) request.body = { mode: 'raw', raw: JSON.stringify(body) };",
                  "  const apiResponse = await new Promise((resolve, reject) => pm.sendRequest(request, (error, response) => error ? reject(error) : resolve(response)));",
                  "  // This API never returns HTML: that means a tunnel or proxy error page.",
                  "  // Each case chains several calls, so a transport failure in the middle",
                  "  // must not fail the test. Retry before giving up.",
                  "  const contentType = apiResponse.headers.get('Content-Type') || '';",
                  "  const usadas = tentativa || 0;",
                  "  if (contentType.indexOf('text/html') !== -1 && usadas < 2) {",
                  "    return callApi(method, path, body, token, usadas + 1);",
                  "  }",
                  "  return apiResponse;",
                  "}",
                  "function check(name, assertion) {",
                  "  let failure;",
                  "  pm.test('TC-015 - ' + name, function () {",
                  "    try { assertion(); } catch (error) { failure = error; throw error; }",
                  "  });",
                  "  if (failure) throw failure;",
                  "}",
                  "function expectCode(response, code, label) {",
                  "  if (response.code !== code) {",
                  "    throw new Error(label + ' returned ' + response.code + ': ' + response.text());",
                  "  }",
                  "}",
                  "// Unique id for this run. Everything the case creates carries this value,",
                  "// so the same collection can run as many times as needed without colliding",
                  "// on the UNIQUE indexes of Email and Nickname.",
                  "const UNIQUE = pm.variables.replaceIn('{{$guid}}');",
                  "async function loginAdmin() {",
                  "  const email = pm.environment.get('adminEmail');",
                  "  const password = pm.environment.get('adminPassword');",
                  "  if (!email || !password) throw new Error('Run: npm run test:api:seed');",
                  "  const login = await callApi('POST', '/auth/login', { Email: email, Password: password });",
                  "  expectCode(login, 200, 'admin login');",
                  "  return login.json().Token;",
                  "}",
                  "async function registerPlayer(prefix, suffix, chipBalance) {",
                  "  const body = {",
                  "    Name: prefix.toUpperCase() + ' Player ' + suffix,",
                  "    BirthDate: '1995-01-01',",
                  "    Nickname: prefix + 'player' + suffix.toLowerCase() + UNIQUE.replace(/-/g, ''),",
                  "    Email: prefix + '.player' + suffix.toLowerCase() + '.' + UNIQUE + '@example.test',",
                  "    Password: 'SenhaDeTeste123'",
                  "  };",
                  "  if (chipBalance !== undefined) body.ChipBalance = chipBalance;",
                  "  const registration = await callApi('POST', '/auth/register', body);",
                  "  expectCode(registration, 201, 'player ' + suffix + ' registration');",
                  "  return registration.json().Token;",
                  "}",
                  "",
                  "const body = pm.response.json();",
                  "",
                  "check('a new session is accepted when the balance covers the new entry cost', function () {",
                  "  pm.response.to.have.status(201);",
                  "  pm.expect(body.session.Status).to.eql('InProgress');",
                  "});",
                  "",
                  "// 1000 - 900: the new session pays the changed value, not the original 50.",
                  "check('the new session is charged the updated entry cost', function () {",
                  "  pm.expect(body.currentBalance).to.eql(100);",
                  "});",
                  "",
                  "(async function () {",
                  "  const token = pm.environment.get('tc015RichToken');",
                  "  const active = await callApi('GET', '/sessions/active', undefined, token);",
                  "  check('the new session becomes the active one for its player', function () {",
                  "    pm.expect(active.code).to.eql(200);",
                  "    pm.expect(active.json().SlotSessionId).to.eql(body.session.SlotSessionId);",
                  "  });",
                  "",
                  "  await callApi('POST', '/sessions/active/cash-out', undefined, token);",
                  "",
                  "  // This table was created by the case itself and had its config changed.",
                  "  // Deleting it here avoids leaving residue in the database on every run.",
                  "  const apagada = await callApi('DELETE', '/slot/machine/' + pm.environment.get('tc015MachineId'), undefined, pm.environment.get('tc015AdminToken'));",
                  "  check('the table created by this case is removed at the end', function () {",
                  "    pm.expect(apagada.code).to.eql(200);",
                  "  });",
                  "})();"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{tc015RichToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/slot-machines/{{tc015MachineId}}/sessions",
              "host": ["{{baseUrl}}"],
              "path": ["slot-machines", "{{tc015MachineId}}", "sessions"]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        }
      ]
    },
    {
      "name": "TC-016 - Reauthenticated user recovers and continues active Slot session",
      "item": [
        {
          "name": "TC-016 - Reauthenticated user recovers and continues active Slot session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-016 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a structurally valid Slot state', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Session.StartedAt).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.EndedAt).to.eql(null);",
                  "    pm.expect(Session.CurrentRewardSnapshot).to.be.a('number');",
                  "    pm.expect(Session.CurrentSpinResult).to.be.an('object');",
                  "    pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4);",
                  "    Session.CurrentSpinResult.Reels.forEach((Reel, Index) => {",
                  "      pm.expect(Reel.ReelIndex).to.eql(Index);",
                  "      pm.expect(Reel.SymbolId).to.be.a('string').and.not.empty;",
                  "    });",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Max).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session).to.have.property('DeletedAt');",
                  "    pm.expect(Session.DeletedAt).to.eql(null);",
                  "  });",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc016InitialToken', 'tc016SecondToken', 'tc016CleanupToken', 'tc016Machine', 'tc016MachineId', 'tc016Snapshot', 'tc016Registry', 'tc016BalanceBeforeReauth', 'tc016RerollCost']) pm.variables.unset(Key);",
                  "",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "  const Credentials = {",
                  "    Name: 'TC016 Slot Recovery User',",
                  "    Email: 'tc016.' + Suffix + '@example.test',",
                  "    Nickname: 'tc016' + Suffix,",
                  "    Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!',",
                  "  };",
                  "",
                  "  const MachinesResponse = await CallApi('GET', '/slot/machine');",
                  "  Check('Slot Machine list is available through the API', () => {",
                  "    pm.expect(MachinesResponse.code).to.eql(200);",
                  "    pm.expect(MachinesResponse.json()).to.be.an('array');",
                  "  });",
                  "  const Machines = MachinesResponse.json();",
                  "  const Machine = Machines.find((Candidate) => Candidate.Name === 'Slot 1');",
                  "  Check('official Slot 1 fixture is available', () => {",
                  "    if (!Machine) throw new Error('TC-016 depende da fixture oficial \"Slot 1\", ainda não disponível neste ambiente.');",
                  "  });",
                  "  Check('official Slot 1 fixture is active and has valid numeric configuration', () => {",
                  "    pm.expect(Machine.SlotMachineId).to.be.a('number').and.above(0);",
                  "    pm.expect(Machine.Active).to.eql(true);",
                  "    for (const Field of ['MinimumSpinValue', 'MinimumChipsRequired', 'MinimumRerollValue']) pm.expect(typeof Machine[Field] === 'number' && Number.isFinite(Machine[Field]) && Machine[Field] >= 0).to.eql(true);",
                  "  });",
                  "  pm.variables.set('tc016Machine', JSON.stringify(Machine));",
                  "  pm.variables.set('tc016MachineId', Machine.SlotMachineId);",
                  "",
                  "  const InitialChipBalance = 200;",
                  "  const RegistrationPayload = { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialChipBalance };",
                  "  Check('registration payload assigns the agreed deterministic initial balance', () => pm.expect(RegistrationPayload.ChipBalance).to.eql(200));",
                  "  const Registration = await CallApi('POST', '/auth/register', RegistrationPayload);",
                  "  Check('temporary regular user is registered with the agreed initial balance', () => {",
                  "    pm.expect(Registration.code).to.eql(201);",
                  "    pm.expect(Registration.json().User.Email).to.eql(Credentials.Email);",
                  "    pm.expect(Registration.json().User.Nickname).to.eql(Credentials.Nickname);",
                  "    pm.expect(Registration.json().User.UserType).to.eql('User');",
                  "    pm.expect(Registration.json().User.ChipBalance).to.eql(InitialChipBalance);",
                  "  });",
                  "  const UserId = Registration.json().User.UserId;",
                  "  pm.variables.set('tc016CleanupToken', Registration.json().Token);",
                  "",
                  "  const InitialLogin = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "  Check('initial login authenticates the temporary user', () => {",
                  "    pm.expect(InitialLogin.code).to.eql(200);",
                  "    pm.expect(InitialLogin.json().User.UserId).to.eql(UserId);",
                  "    pm.expect(InitialLogin.json().User.Email).to.eql(Credentials.Email);",
                  "    pm.expect(InitialLogin.json().User.ChipBalance).to.eql(InitialChipBalance);",
                  "  });",
                  "  const InitialToken = InitialLogin.json().Token;",
                  "  Check('initial login returns a valid-looking JWT', () => {",
                  "    pm.expect(InitialToken).to.be.a('string').and.not.empty;",
                  "    pm.expect(InitialToken.split('.')).to.have.length(3);",
                  "  });",
                  "  pm.variables.set('tc016InitialToken', InitialToken);",
                  "  pm.variables.set('tc016CleanupToken', InitialToken);",
                  "",
                  "  const InitialBalance = await Read('/user/chips', InitialToken, 'registered user initial balance');",
                  "  Check('registered initial balance is persisted before starting the official Slot 1 fixture', () => pm.expect(InitialBalance.ChipBalance).to.eql(InitialChipBalance));",
                  "",
                  "  const SpinCost = Machine.MinimumSpinValue;",
                  "  const MinimumAccess = Machine.MinimumChipsRequired;",
                  "  const RerollCost = Machine.MinimumRerollValue;",
                  "  const RequiredBalance = Math.max(MinimumAccess, SpinCost + (2 * RerollCost));",
                  "  Check('initial balance covers official Slot 1 creation and both rerolls', () => {",
                  "    if (InitialBalance.ChipBalance < RequiredBalance) {",
                  "      const Missing = RequiredBalance - InitialBalance.ChipBalance;",
                  "      throw new Error('Initial balance is insufficient for TC-016: available ' + InitialBalance.ChipBalance + ', required ' + RequiredBalance + ' (MinimumChipsRequired=' + MinimumAccess + ', MinimumSpinValue=' + SpinCost + ', two MinimumRerollValue=' + RerollCost + '), missing ' + Missing + '.');",
                  "    }",
                  "  });",
                  "  pm.variables.set('tc016RerollCost', RerollCost);",
                  "",
                  "  const Creation = await CallApi('POST', '/slot-machines/' + Machine.SlotMachineId + '/sessions', {}, InitialToken);",
                  "  Check('real session is created on the official Slot 1 fixture', () => pm.expect(Creation.code).to.eql(201));",
                  "  const CreatedSession = Creation.json().session;",
                  "  AssertSessionShape(CreatedSession, 'created session');",
                  "  Check('created session belongs to the temporary user and official Slot 1 fixture', () => {",
                  "    pm.expect(CreatedSession.UserId).to.eql(UserId);",
                  "    pm.expect(CreatedSession.SlotMachineId).to.eql(Machine.SlotMachineId);",
                  "    pm.expect(Creation.json().currentBalance).to.eql(InitialBalance.ChipBalance - SpinCost);",
                  "  });",
                  "",
                  "  const Registry = await Read('/sessions/me/active', InitialToken, 'active-session registry before reauthentication');",
                  "  Check('registry references the created Slot session', () => {",
                  "    pm.expect(Registry.UserId).to.eql(UserId);",
                  "    pm.expect(Registry.GameType).to.eql('Slot');",
                  "    pm.expect(Registry.ReferenceId).to.eql(CreatedSession.SlotSessionId);",
                  "  });",
                  "  pm.variables.set('tc016Registry', JSON.stringify(Registry));",
                  "",
                  "  let ExpectedSession = CreatedSession;",
                  "  let ExpectedBalance = Creation.json().currentBalance;",
                  "  const CanSafelyRerollBeforeReauthentication = ExpectedBalance >= RerollCost * 2;",
                  "  if (CanSafelyRerollBeforeReauthentication) {",
                  "    const FirstReroll = await CallApi('POST', '/sessions/active/reroll/0', undefined, InitialToken);",
                  "    Check('pre-reauthentication reroll succeeds when enough balance remains', () => pm.expect(FirstReroll.code).to.eql(201));",
                  "    const Rerolled = FirstReroll.json().session;",
                  "    AssertSessionShape(Rerolled, 'pre-reauthentication rerolled session');",
                  "    Check('pre-reauthentication reroll changes only valid state invariants', () => {",
                  "      pm.expect(Rerolled.SlotSessionId).to.eql(CreatedSession.SlotSessionId);",
                  "      pm.expect(Rerolled.CurrentRerollsSpent.Rerolls.Used).to.eql(CreatedSession.CurrentRerollsSpent.Rerolls.Used + 1);",
                  "      for (let Index = 1; Index < CreatedSession.CurrentSpinResult.Reels.length; Index += 1) pm.expect(Rerolled.CurrentSpinResult.Reels[Index]).to.deep.equal(CreatedSession.CurrentSpinResult.Reels[Index]);",
                  "      pm.expect(FirstReroll.json().currentBalance).to.eql(ExpectedBalance - RerollCost);",
                  "    });",
                  "    ExpectedSession = Rerolled;",
                  "    ExpectedBalance = FirstReroll.json().currentBalance;",
                  "  } else {",
                  "    Check('pre-reauthentication reroll is safely reserved for continuation', () => pm.expect(ExpectedBalance).to.be.at.least(RerollCost));",
                  "  }",
                  "",
                  "  const ActiveBeforeReauthentication = await Read('/sessions/active', InitialToken, 'active Slot session before reauthentication');",
                  "  Check('active endpoint persists the complete prepared Slot state', () => pm.expect(ActiveBeforeReauthentication).to.deep.equal(ExpectedSession));",
                  "  const BalanceBeforeReauthentication = await Read('/user/chips', InitialToken, 'balance before reauthentication');",
                  "  Check('balance before reauthentication matches the prepared game state', () => pm.expect(BalanceBeforeReauthentication.ChipBalance).to.eql(ExpectedBalance));",
                  "  pm.variables.set('tc016Snapshot', JSON.stringify(ActiveBeforeReauthentication));",
                  "  pm.variables.set('tc016BalanceBeforeReauth', ExpectedBalance);",
                  "",
                  "  const SecondLogin = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "  Check('second login succeeds for the same user', () => {",
                  "    pm.expect(SecondLogin.code).to.eql(200);",
                  "    pm.expect(SecondLogin.json().User.UserId).to.eql(UserId);",
                  "    pm.expect(SecondLogin.json().User.Email).to.eql(Credentials.Email);",
                  "  });",
                  "  const SecondToken = SecondLogin.json().Token;",
                  "  Check('second login returns a valid-looking JWT without requiring token inequality', () => {",
                  "    pm.expect(SecondToken).to.be.a('string').and.not.empty;",
                  "    pm.expect(SecondToken.split('.')).to.have.length(3);",
                  "  });",
                  "  pm.variables.set('tc016SecondToken', SecondToken);",
                  "  pm.variables.set('tc016CleanupToken', SecondToken);",
                  "})().catch(async (Error) => {",
                  "  const CleanupToken = pm.variables.get('tc016CleanupToken');",
                  "  if (CleanupToken) {",
                  "    try {",
                  "      const Active = await CallApi('GET', '/sessions/active', undefined, CleanupToken);",
                  "      if (Active.code === 200 && Active.text().trim() !== '') {",
                  "        const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, CleanupToken);",
                  "        if (CashOut.code !== 201) throw new Error('API cleanup cash-out returned ' + CashOut.code);",
                  "      }",
                  "      const Deactivation = await CallApi('DELETE', '/user', undefined, CleanupToken);",
                  "      pm.test('TC-016 - API cleanup after preparation failure deactivates the temporary user', () => pm.expect(Deactivation.code).to.eql(200));",
                  "    } catch (CleanupError) {",
                  "      pm.test('TC-016 - API cleanup after preparation failure completes', () => { throw CleanupError; });",
                  "    }",
                  "  }",
                  "  pm.test('TC-016 - preparation completes', () => { throw Error; });",
                  "  pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-016 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "",
                  "(async function () {",
                  "  const Token = pm.variables.get('tc016SecondToken');",
                  "  const Snapshot = JSON.parse(pm.variables.get('tc016Snapshot'));",
                  "  const RegistryBefore = JSON.parse(pm.variables.get('tc016Registry'));",
                  "  const BalanceBefore = Number(pm.variables.get('tc016BalanceBeforeReauth'));",
                  "  const RerollCost = Number(pm.variables.get('tc016RerollCost'));",
                  "  let FlowError;",
                  "",
                  "  try {",
                  "  Check('recovery request uses the JWT returned by the second login', () => {",
                  "    pm.expect(Token).to.be.a('string').and.not.empty;",
                  "    pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + Token);",
                  "  });",
                  "  Check('active Slot recovery after reauthentication returns 200', () => pm.response.to.have.status(200));",
                  "  Check('active Slot recovery returns JSON', () => pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json'));",
                  "  const Recovered = pm.response.json();",
                  "  Check('recovered session keeps its persisted identity', () => {",
                  "    pm.expect(Recovered.SlotSessionId).to.eql(Snapshot.SlotSessionId);",
                  "    pm.expect(Recovered.UserId).to.eql(Snapshot.UserId);",
                  "    pm.expect(Recovered.SlotMachineId).to.eql(Snapshot.SlotMachineId);",
                  "  });",
                  "  Check('recovered session keeps its lifecycle state', () => {",
                  "    pm.expect(Recovered.Status).to.eql(Snapshot.Status);",
                  "    pm.expect(Recovered.StartedAt).to.eql(Snapshot.StartedAt);",
                  "    pm.expect(Recovered.EndedAt).to.eql(Snapshot.EndedAt);",
                  "    pm.expect(Recovered.DeletedAt).to.eql(Snapshot.DeletedAt);",
                  "  });",
                  "  Check('recovered session keeps reward and all reel state', () => {",
                  "    pm.expect(Recovered.CurrentRewardSnapshot).to.eql(Snapshot.CurrentRewardSnapshot);",
                  "    pm.expect(Recovered.CurrentSpinResult.Reels).to.deep.equal(Snapshot.CurrentSpinResult.Reels);",
                  "  });",
                  "  Check('recovered session keeps the complete reroll state', () => pm.expect(Recovered.CurrentRerollsSpent.Rerolls).to.deep.equal(Snapshot.CurrentRerollsSpent.Rerolls));",
                  "  Check('recovered response is the same complete persisted session snapshot', () => pm.expect(Recovered).to.deep.equal(Snapshot));",
                  "",
                  "  const RegistryAfter = await Read('/sessions/me/active', Token, 'registry through the second authentication context');",
                  "  Check('active-session registry is preserved across reauthentication', () => {",
                  "    pm.expect(RegistryAfter).to.deep.equal(RegistryBefore);",
                  "    pm.expect(RegistryAfter.GameType).to.eql('Slot');",
                  "    pm.expect(RegistryAfter.ReferenceId).to.eql(Recovered.SlotSessionId);",
                  "  });",
                  "  const BalanceAfterRecovery = await Read('/user/chips', Token, 'balance through the second authentication context');",
                  "  Check('reauthentication does not alter the user balance', () => pm.expect(BalanceAfterRecovery.ChipBalance).to.eql(BalanceBefore));",
                  "",
                  "  const SelectedReel = 1;",
                  "  const Continuation = await CallApi('POST', '/sessions/active/reroll/' + SelectedReel, undefined, Token);",
                  "  Check('second authentication context can continue the active game', () => pm.expect(Continuation.code).to.eql(201));",
                  "  const Continued = Continuation.json().session;",
                  "  Check('continuation keeps the same session active', () => {",
                  "    pm.expect(Continued.SlotSessionId).to.eql(Snapshot.SlotSessionId);",
                  "    pm.expect(Continued.UserId).to.eql(Snapshot.UserId);",
                  "    pm.expect(Continued.SlotMachineId).to.eql(Snapshot.SlotMachineId);",
                  "    pm.expect(Continued.Status).to.eql('InProgress');",
                  "    pm.expect(Continued.EndedAt).to.eql(null);",
                  "  });",
                  "  Check('continuation consumes exactly one reroll', () => {",
                  "    pm.expect(Continued.CurrentRerollsSpent.Rerolls.Max).to.eql(Snapshot.CurrentRerollsSpent.Rerolls.Max);",
                  "    pm.expect(Continued.CurrentRerollsSpent.Rerolls.Used).to.eql(Snapshot.CurrentRerollsSpent.Rerolls.Used + 1);",
                  "  });",
                  "  Check('continuation preserves every non-selected reel', () => {",
                  "    Snapshot.CurrentSpinResult.Reels.forEach((BeforeReel) => {",
                  "      const AfterReel = Continued.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex);",
                  "      pm.expect(AfterReel).to.exist;",
                  "      if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel);",
                  "    });",
                  "  });",
                  "  Check('selected reel and recalculated reward remain structurally valid without assuming RNG output', () => {",
                  "    const Selected = Continued.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel);",
                  "    pm.expect(Selected.SymbolId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Continued.CurrentRewardSnapshot).to.be.a('number').and.at.least(0);",
                  "  });",
                  "  const ExpectedBalanceAfterContinuation = BalanceBefore - RerollCost;",
                  "  Check('continuation debits exactly the configured reroll cost', () => pm.expect(Continuation.json().currentBalance).to.eql(ExpectedBalanceAfterContinuation));",
                  "",
                  "  const PersistedAfterContinuation = await Read('/sessions/active', Token, 'continued session persisted state');",
                  "  Check('continued state is persisted and immediately recoverable', () => pm.expect(PersistedAfterContinuation).to.deep.equal(Continued));",
                  "  const PersistedBalance = await Read('/user/chips', Token, 'balance after continuation');",
                  "  Check('continued balance debit is persisted', () => pm.expect(PersistedBalance.ChipBalance).to.eql(ExpectedBalanceAfterContinuation));",
                  "",
                  "  const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token);",
                  "  const ExpectedFinalBalance = ExpectedBalanceAfterContinuation + Continued.CurrentRewardSnapshot;",
                  "  Check('temporary session is cashed out successfully', () => {",
                  "    pm.expect(CashOut.code).to.eql(201);",
                  "    pm.expect(CashOut.json()).to.deep.equal({ message: 'Cash out successful', finalBalance: ExpectedFinalBalance });",
                  "  });",
                  "  const ActiveAfterCashOut = await Read('/sessions/active', Token, 'active session after cash-out');",
                  "  Check('cash-out leaves no active Slot session', () => pm.expect(ActiveAfterCashOut).to.eql(null));",
                  "  const RegistryAfterCashOut = await Read('/sessions/me/active', Token, 'registry after cash-out');",
                  "  Check('cash-out releases the active-session registry', () => pm.expect(RegistryAfterCashOut).to.eql(null));",
                  "  const FinalBalance = await Read('/user/chips', Token, 'final balance after cash-out');",
                  "  Check('cash-out reward is persisted in the final balance', () => pm.expect(FinalBalance.ChipBalance).to.eql(ExpectedFinalBalance));",
                  "  } catch (Error) {",
                  "    FlowError = Error;",
                  "  } finally {",
                  "    try {",
                  "      const ActiveForCleanup = await CallApi('GET', '/sessions/active', undefined, Token);",
                  "      pm.test('TC-016 - API cleanup can inspect the temporary user active session', () => pm.expect(ActiveForCleanup.code).to.eql(200));",
                  "      if (ActiveForCleanup.code === 200 && ActiveForCleanup.text().trim() !== '') {",
                  "        const CleanupCashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token);",
                  "        pm.test('TC-016 - API cleanup cashes out any remaining temporary session', () => pm.expect(CleanupCashOut.code).to.eql(201));",
                  "      }",
                  "      const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "      pm.test('TC-016 - temporary user is deactivated through the authenticated API', () => {",
                  "        pm.expect(Deactivation.code).to.eql(200);",
                  "        pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' });",
                  "      });",
                  "    } catch (CleanupError) {",
                  "      pm.test('TC-016 - API-only final cleanup completes', () => { throw CleanupError; });",
                  "    }",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((Error) => {",
                  "  pm.test('TC-016 - recovery, continuation and cleanup flow completes', () => { throw Error; });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc016SecondToken}}"
              }
            ],
            "url": "{{baseUrl}}/sessions/active"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-017 - Reauthenticated user recovers and continues active Gambit session",
      "item": [
        {
          "name": "TC-017 - Reauthenticated user recovers and continues active Gambit session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-017 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function SessionView(Payload) {",
                  "  if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload;",
                  "  const View = Object.assign({}, Payload);",
                  "  delete View.PeekResult;",
                  "  return View;",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a complete valid Gambit view', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.GambitTableId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount));",
                  "    pm.expect(Session.AccumulatedPoints).to.be.a('number');",
                  "    pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true);",
                  "    pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']);",
                  "    pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true);",
                  "    pm.expect(Session.Grid).to.be.an('object');",
                  "    pm.expect(Session.Grid.Revealed).to.be.an('array');",
                  "    pm.expect(Session.Grid.Unrevealed).to.be.an('array');",
                  "    pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25);",
                  "    const Positions = [];",
                  "    Session.Grid.Revealed.forEach((Card) => {",
                  "      pm.expect(Card.Position).to.be.a('number').and.within(0, 24);",
                  "      pm.expect(Card.Points).to.be.a('number');",
                  "      pm.expect(Card.Effect === null || typeof Card.Effect === 'string').to.eql(true);",
                  "      pm.expect(Card.Locked).to.be.a('boolean');",
                  "      Positions.push(Card.Position);",
                  "    });",
                  "    Session.Grid.Unrevealed.forEach((Card) => {",
                  "      pm.expect(Card.Position).to.be.a('number').and.within(0, 24);",
                  "      pm.expect(Card.Locked).to.be.a('boolean');",
                  "      pm.expect(Card).not.to.have.property('Points');",
                  "      pm.expect(Card).not.to.have.property('Effect');",
                  "      Positions.push(Card.Position);",
                  "    });",
                  "    pm.expect(new Set(Positions).size).to.eql(25);",
                  "    pm.expect(Session.Grid).to.have.property('PendingEvent');",
                  "    pm.expect(Session.Grid).to.have.property('PendingInteraction');",
                  "  });",
                  "}",
                  "async function ResolveBlockingState(Session, Token, Label, RecordAssertions) {",
                  "  let Current = SessionView(Session);",
                  "  let Steps = 0;",
                  "  while (Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent)) {",
                  "    Steps += 1;",
                  "    if (Steps > 20) throw new Error(Label + ': pending Gambit state did not converge after 20 API resolutions.');",
                  "    let Response;",
                  "    if (Current.Grid.PendingInteraction) {",
                  "      const Pending = Current.Grid.PendingInteraction;",
                  "      const Required = Pending.RequiredSelections;",
                  "      const Candidates = Current.Grid.Unrevealed",
                  "        .filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0)",
                  "        .filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index);",
                  "      if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error(Label + ': pending interaction cannot be resolved with the required number of distinct unrevealed positions.');",
                  "      const Positions = Candidates.slice(0, Required).map((Card) => Card.Position);",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: Positions }, Token);",
                  "    } else {",
                  "      const Pending = Current.Grid.PendingEvent;",
                  "      const GoodIndex = Array.isArray(Pending.GoodOptions) ? Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      const BadIndex = Array.isArray(Pending.BadOptions) ? Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error(Label + ': pending event does not expose selectable good and bad options accepted by ResolveEventDto.');",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: GoodIndex, BadIndex: BadIndex }, Token);",
                  "    }",
                  "    if (RecordAssertions) Check(Label + ' resolves pending random state #' + Steps, () => pm.expect(Response.code).to.eql(201));",
                  "    else if (Response.code !== 201) throw new Error(Label + ': pending-state endpoint returned ' + Response.code + '.');",
                  "    Current = SessionView(Response.json());",
                  "  }",
                  "  return Current;",
                  "}",
                  "async function BurnOne(Session, Token, Label) {",
                  "  const Ready = await ResolveBlockingState(Session, Token, Label, true);",
                  "  const Target = Ready.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "  Check(Label + ' has a valid card available to burn', () => {",
                  "    pm.expect(Ready.Status).to.eql('InProgress');",
                  "    pm.expect(Target).to.exist;",
                  "  });",
                  "  const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "  Check(Label + ' burn succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const Burned = SessionView(Response.json());",
                  "  AssertSessionShape(Burned, Label + ' result');",
                  "  Check(Label + ' changes only deterministic burn invariants', () => {",
                  "    pm.expect(Burned.GambitSessionId).to.eql(Ready.GambitSessionId);",
                  "    pm.expect(Burned.UserId).to.eql(Ready.UserId);",
                  "    pm.expect(Burned.GambitTableId).to.eql(Ready.GambitTableId);",
                  "    pm.expect(Burned.ManualFlipsCount).to.eql(Ready.ManualFlipsCount + 1);",
                  "    pm.expect(Burned.Grid.Revealed.length).to.eql(Ready.Grid.Revealed.length + 1);",
                  "    pm.expect(Burned.Grid.Unrevealed.length).to.eql(Ready.Grid.Unrevealed.length - 1);",
                  "    pm.expect(Burned.Grid.Revealed.some((Card) => Card.Position === Target.Position)).to.eql(true);",
                  "  });",
                  "  return Burned;",
                  "}",
                  "async function FinishAndCashOut(Token) {",
                  "  let Response = await CallApi('GET', '/gambit/sessions/active', undefined, Token);",
                  "  if (Response.code !== 200) throw new Error('Gambit cleanup active-session lookup returned ' + Response.code + '.');",
                  "  let Current = Response.text().trim() === '' ? null : SessionView(Response.json());",
                  "  for (let Steps = 0; Current && Steps < 100; Steps += 1) {",
                  "    if (Current.Status === 'Finished') {",
                  "      const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token);",
                  "      if (CashOut.code !== 201) throw new Error('Gambit cleanup cash-out returned ' + CashOut.code + '.');",
                  "      return { Finished: Current, CashOut };",
                  "    }",
                  "    if (Current.Status !== 'InProgress') return { Finished: Current, CashOut: null };",
                  "    if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) {",
                  "      Current = await ResolveBlockingState(Current, Token, 'Gambit cleanup', false);",
                  "      continue;",
                  "    }",
                  "    const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "    if (!Target) throw new Error('Gambit cleanup found no burnable card in an InProgress session.');",
                  "    Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "    if (Response.code !== 201) throw new Error('Gambit cleanup burn returned ' + Response.code + '.');",
                  "    Current = SessionView(Response.json());",
                  "  }",
                  "  if (Current) throw new Error('Gambit cleanup did not finish the session within 100 API actions.');",
                  "  return { Finished: null, CashOut: null };",
                  "}",
                  "async function CleanupTemporaryUser(Token) {",
                  "  await FinishAndCashOut(Token);",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error('Temporary-user deactivation returned ' + Deactivation.code + '.');",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc017SecondToken', 'tc017CleanupToken', 'tc017Snapshot', 'tc017Registry', 'tc017BalanceBeforeReauth', 'tc017UserId', 'tc017TableId']) pm.variables.unset(Key);",
                  "",
                  "  const TablesResponse = await CallApi('GET', '/gambit-table');",
                  "  Check('Gambit Table list is available through the API', () => {",
                  "    pm.expect(TablesResponse.code).to.eql(200);",
                  "    pm.expect(TablesResponse.json()).to.be.an('array');",
                  "  });",
                  "  const Table = TablesResponse.json().find((Candidate) => Candidate.Name === 'Gambit 1');",
                  "  Check('official Gambit 1 fixture is available', () => {",
                  "    if (!Table) throw new Error('TC-017 depende da fixture oficial \"Gambit 1\", ainda não disponível neste ambiente.');",
                  "  });",
                  "  Check('official Gambit 1 fixture is active and has valid numeric configuration', () => {",
                  "    pm.expect(Table.GambitTableId).to.be.a('number').and.above(0);",
                  "    pm.expect(Table.Active).to.eql(true);",
                  "    for (const Field of ['MinimumChipsRequired', 'CardPrice', 'TableMultiplier', 'MinimumCardsPurchased', 'MaxCardsPurchased']) pm.expect(typeof Table[Field] === 'number' && Number.isFinite(Table[Field]) && Table[Field] >= 0).to.eql(true);",
                  "    pm.expect(Table.MinimumCardsPurchased).to.be.at.least(1);",
                  "    pm.expect(Table.MaxCardsPurchased).to.be.at.least(Table.MinimumCardsPurchased);",
                  "  });",
                  "  pm.variables.set('tc017TableId', Table.GambitTableId);",
                  "",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "  const Credentials = { Name: 'TC017 Gambit Recovery User', Email: 'tc017.' + Suffix + '@example.test', Nickname: 'tc017' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "  const InitialChipBalance = 200;",
                  "  const RegistrationPayload = { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialChipBalance };",
                  "  Check('registration payload assigns the agreed deterministic initial balance', () => pm.expect(RegistrationPayload.ChipBalance).to.eql(200));",
                  "  const Registration = await CallApi('POST', '/auth/register', RegistrationPayload);",
                  "  Check('temporary regular user is registered through the API', () => {",
                  "    pm.expect(Registration.code).to.eql(201);",
                  "    pm.expect(Registration.json().User.Email).to.eql(Credentials.Email);",
                  "    pm.expect(Registration.json().User.Nickname).to.eql(Credentials.Nickname);",
                  "    pm.expect(Registration.json().User.UserType).to.eql('User');",
                  "    pm.expect(Registration.json().User.ChipBalance).to.eql(InitialChipBalance);",
                  "  });",
                  "  const UserId = Registration.json().User.UserId;",
                  "  pm.variables.set('tc017UserId', UserId);",
                  "  pm.variables.set('tc017CleanupToken', Registration.json().Token);",
                  "",
                  "  const InitialLogin = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "  Check('initial login authenticates the temporary user', () => {",
                  "    pm.expect(InitialLogin.code).to.eql(200);",
                  "    pm.expect(InitialLogin.json().User.UserId).to.eql(UserId);",
                  "    pm.expect(InitialLogin.json().User.Email).to.eql(Credentials.Email);",
                  "    pm.expect(InitialLogin.json().User.ChipBalance).to.eql(InitialChipBalance);",
                  "  });",
                  "  const InitialToken = InitialLogin.json().Token;",
                  "  Check('initial login returns a valid-looking JWT', () => {",
                  "    pm.expect(InitialToken).to.be.a('string').and.not.empty;",
                  "    pm.expect(InitialToken.split('.')).to.have.length(3);",
                  "  });",
                  "  pm.variables.set('tc017CleanupToken', InitialToken);",
                  "",
                  "  const InitialBalance = await Read('/user/chips', InitialToken, 'registered user initial balance');",
                  "  Check('registered initial balance is persisted before starting Gambit 1', () => pm.expect(InitialBalance.ChipBalance).to.eql(InitialChipBalance));",
                  "  const CardsPurchased = Table.MinimumCardsPurchased;",
                  "  const SessionCost = Table.CardPrice * CardsPurchased;",
                  "  const RequiredBalance = Math.max(Table.MinimumChipsRequired, SessionCost);",
                  "  Check('initial balance covers Gambit 1 access and the minimum valid card purchase', () => {",
                  "    if (InitialBalance.ChipBalance < RequiredBalance) {",
                  "      throw new Error('Initial balance is insufficient for TC-017: available ' + InitialBalance.ChipBalance + ', required ' + RequiredBalance + ' (MinimumChipsRequired=' + Table.MinimumChipsRequired + ', CardPrice=' + Table.CardPrice + ', CardsPurchased=' + CardsPurchased + ', purchase cost=' + SessionCost + '), missing ' + (RequiredBalance - InitialBalance.ChipBalance) + '.');",
                  "    }",
                  "  });",
                  "",
                  "  const CreationPayload = { CardsPurchased: CardsPurchased };",
                  "  const Creation = await CallApi('POST', '/gambit-tables/' + Table.GambitTableId + '/sessions', CreationPayload, InitialToken);",
                  "  Check('real session is created on the official Gambit 1 fixture', () => pm.expect(Creation.code).to.eql(201));",
                  "  const Created = SessionView(Creation.json().session);",
                  "  AssertSessionShape(Created, 'created session');",
                  "  Check('created session has the expected identity, card quantity, lifecycle and debit', () => {",
                  "    pm.expect(Created.UserId).to.eql(UserId);",
                  "    pm.expect(Created.GambitTableId).to.eql(Table.GambitTableId);",
                  "    pm.expect(Created.CardsPurchased).to.eql(CardsPurchased);",
                  "    pm.expect(Created.Status).to.eql('InProgress');",
                  "    pm.expect(Created.ManualFlipsCount).to.eql(0);",
                  "    pm.expect(Creation.json().currentBalance).to.eql(InitialBalance.ChipBalance - SessionCost);",
                  "  });",
                  "",
                  "  const Registry = await Read('/sessions/me/active', InitialToken, 'active-session registry before reauthentication');",
                  "  Check('registry references the created Gambit session', () => {",
                  "    pm.expect(Registry.UserId).to.eql(UserId);",
                  "    pm.expect(Registry.GameType).to.eql('Gambit');",
                  "    pm.expect(Registry.ReferenceId).to.eql(Created.GambitSessionId);",
                  "  });",
                  "",
                  "  const FirstBurn = await BurnOne(Created, InitialToken, 'pre-reauthentication action');",
                  "  const ActiveBeforeReauth = await Read('/gambit/sessions/active', InitialToken, 'active Gambit session before reauthentication');",
                  "  Check('active endpoint persists the complete non-initial Gambit state including pending state', () => pm.expect(ActiveBeforeReauth).to.deep.equal(FirstBurn));",
                  "  const BalanceBeforeReauth = await Read('/user/chips', InitialToken, 'balance before reauthentication');",
                  "  Check('Gambit actions do not create an unconfigured chip debit', () => pm.expect(BalanceBeforeReauth.ChipBalance).to.eql(Creation.json().currentBalance));",
                  "",
                  "  const SecondLogin = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "  Check('second login succeeds for the same user', () => {",
                  "    pm.expect(SecondLogin.code).to.eql(200);",
                  "    pm.expect(SecondLogin.json().User.UserId).to.eql(UserId);",
                  "    pm.expect(SecondLogin.json().User.Email).to.eql(Credentials.Email);",
                  "  });",
                  "  const SecondToken = SecondLogin.json().Token;",
                  "  Check('second login returns a valid-looking JWT without requiring token inequality', () => {",
                  "    pm.expect(SecondToken).to.be.a('string').and.not.empty;",
                  "    pm.expect(SecondToken.split('.')).to.have.length(3);",
                  "  });",
                  "  const BalanceThroughSecondLogin = await Read('/user/chips', SecondToken, 'balance through the second authentication context');",
                  "  Check('second authentication does not alter the persisted balance', () => pm.expect(BalanceThroughSecondLogin.ChipBalance).to.eql(BalanceBeforeReauth.ChipBalance));",
                  "  const RegistryThroughSecondLogin = await Read('/sessions/me/active', SecondToken, 'registry through the second authentication context');",
                  "  Check('registry is preserved through the second authentication context', () => pm.expect(RegistryThroughSecondLogin).to.deep.equal(Registry));",
                  "",
                  "  pm.variables.set('tc017SecondToken', SecondToken);",
                  "  pm.variables.set('tc017CleanupToken', SecondToken);",
                  "  pm.variables.set('tc017Snapshot', JSON.stringify(ActiveBeforeReauth));",
                  "  pm.variables.set('tc017Registry', JSON.stringify(Registry));",
                  "  pm.variables.set('tc017BalanceBeforeReauth', BalanceBeforeReauth.ChipBalance);",
                  "})().catch(async (Error) => {",
                  "  const CleanupToken = pm.variables.get('tc017CleanupToken');",
                  "  if (CleanupToken) {",
                  "    try {",
                  "      await CleanupTemporaryUser(CleanupToken);",
                  "      pm.test('TC-017 - API cleanup after preparation failure deactivates the temporary user', () => pm.expect(true).to.eql(true));",
                  "    } catch (CleanupError) {",
                  "      pm.test('TC-017 - API cleanup after preparation failure completes', () => { throw CleanupError; });",
                  "    }",
                  "  }",
                  "  pm.test('TC-017 - preparation completes', () => { throw Error; });",
                  "  pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-017 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function SessionView(Payload) {",
                  "  if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload;",
                  "  const View = Object.assign({}, Payload);",
                  "  delete View.PeekResult;",
                  "  return View;",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a complete valid Gambit view', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.GambitTableId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount));",
                  "    pm.expect(Session.AccumulatedPoints).to.be.a('number');",
                  "    pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true);",
                  "    pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']);",
                  "    pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true);",
                  "    pm.expect(Session.Grid.Revealed).to.be.an('array');",
                  "    pm.expect(Session.Grid.Unrevealed).to.be.an('array');",
                  "    pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25);",
                  "    pm.expect(Session.Grid).to.have.property('PendingEvent');",
                  "    pm.expect(Session.Grid).to.have.property('PendingInteraction');",
                  "  });",
                  "}",
                  "async function ResolveBlockingState(Session, Token, Label, RecordAssertions) {",
                  "  let Current = SessionView(Session);",
                  "  let Steps = 0;",
                  "  while (Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent)) {",
                  "    Steps += 1;",
                  "    if (Steps > 20) throw new Error(Label + ': pending Gambit state did not converge after 20 API resolutions.');",
                  "    let Response;",
                  "    if (Current.Grid.PendingInteraction) {",
                  "      const Pending = Current.Grid.PendingInteraction;",
                  "      const Required = Pending.RequiredSelections;",
                  "      const Candidates = Current.Grid.Unrevealed",
                  "        .filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0)",
                  "        .filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index);",
                  "      if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error(Label + ': pending interaction cannot be resolved with the required number of distinct unrevealed positions.');",
                  "      const Positions = Candidates.slice(0, Required).map((Card) => Card.Position);",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: Positions }, Token);",
                  "    } else {",
                  "      const Pending = Current.Grid.PendingEvent;",
                  "      const GoodIndex = Array.isArray(Pending.GoodOptions) ? Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      const BadIndex = Array.isArray(Pending.BadOptions) ? Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error(Label + ': pending event does not expose selectable good and bad options accepted by ResolveEventDto.');",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: GoodIndex, BadIndex: BadIndex }, Token);",
                  "    }",
                  "    if (RecordAssertions) Check(Label + ' resolves pending random state #' + Steps, () => pm.expect(Response.code).to.eql(201));",
                  "    else if (Response.code !== 201) throw new Error(Label + ': pending-state endpoint returned ' + Response.code + '.');",
                  "    Current = SessionView(Response.json());",
                  "  }",
                  "  return Current;",
                  "}",
                  "async function BurnOne(Session, Token, Label) {",
                  "  const Ready = await ResolveBlockingState(Session, Token, Label, true);",
                  "  const Target = Ready.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "  Check(Label + ' has a valid card available to burn', () => {",
                  "    pm.expect(Ready.Status).to.eql('InProgress');",
                  "    pm.expect(Target).to.exist;",
                  "  });",
                  "  const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "  Check(Label + ' burn succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const Burned = SessionView(Response.json());",
                  "  AssertSessionShape(Burned, Label + ' result');",
                  "  Check(Label + ' changes deterministic burn invariants', () => {",
                  "    pm.expect(Burned.GambitSessionId).to.eql(Ready.GambitSessionId);",
                  "    pm.expect(Burned.UserId).to.eql(Ready.UserId);",
                  "    pm.expect(Burned.GambitTableId).to.eql(Ready.GambitTableId);",
                  "    pm.expect(Burned.ManualFlipsCount).to.eql(Ready.ManualFlipsCount + 1);",
                  "    pm.expect(Burned.Grid.Revealed.length).to.eql(Ready.Grid.Revealed.length + 1);",
                  "    pm.expect(Burned.Grid.Unrevealed.length).to.eql(Ready.Grid.Unrevealed.length - 1);",
                  "    pm.expect(Burned.Grid.Revealed.some((Card) => Card.Position === Target.Position)).to.eql(true);",
                  "  });",
                  "  return Burned;",
                  "}",
                  "async function FinishAndCashOut(Token) {",
                  "  let Response = await CallApi('GET', '/gambit/sessions/active', undefined, Token);",
                  "  if (Response.code !== 200) throw new Error('Gambit cleanup active-session lookup returned ' + Response.code + '.');",
                  "  let Current = Response.text().trim() === '' ? null : SessionView(Response.json());",
                  "  for (let Steps = 0; Current && Steps < 100; Steps += 1) {",
                  "    if (Current.Status === 'Finished') {",
                  "      const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token);",
                  "      if (CashOut.code !== 201) throw new Error('Gambit cash-out returned ' + CashOut.code + '.');",
                  "      return { Finished: Current, CashOut };",
                  "    }",
                  "    if (Current.Status !== 'InProgress') return { Finished: Current, CashOut: null };",
                  "    if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) {",
                  "      Current = await ResolveBlockingState(Current, Token, 'Gambit finalization', false);",
                  "      continue;",
                  "    }",
                  "    const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "    if (!Target) throw new Error('Gambit finalization found no burnable card in an InProgress session.');",
                  "    Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "    if (Response.code !== 201) throw new Error('Gambit finalization burn returned ' + Response.code + '.');",
                  "    Current = SessionView(Response.json());",
                  "  }",
                  "  if (Current) throw new Error('Gambit finalization did not finish the session within 100 API actions.');",
                  "  return { Finished: null, CashOut: null };",
                  "}",
                  "",
                  "(async function () {",
                  "  const Token = pm.variables.get('tc017SecondToken');",
                  "  const Snapshot = JSON.parse(pm.variables.get('tc017Snapshot'));",
                  "  const RegistryBefore = JSON.parse(pm.variables.get('tc017Registry'));",
                  "  const BalanceBefore = Number(pm.variables.get('tc017BalanceBeforeReauth'));",
                  "  let FlowError;",
                  "  try {",
                  "    Check('recovery request uses the JWT returned by the second login', () => {",
                  "      pm.expect(Token).to.be.a('string').and.not.empty;",
                  "      pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + Token);",
                  "    });",
                  "    Check('active Gambit recovery after reauthentication returns 200', () => pm.response.to.have.status(200));",
                  "    Check('active Gambit recovery returns JSON', () => pm.expect(pm.response.headers.get('Content-Type')).to.include('application/json'));",
                  "    const Recovered = pm.response.json();",
                  "    AssertSessionShape(Recovered, 'recovered session');",
                  "    Check('recovered session keeps its persisted identity and lifecycle', () => {",
                  "      pm.expect(Recovered.GambitSessionId).to.eql(Snapshot.GambitSessionId);",
                  "      pm.expect(Recovered.UserId).to.eql(Snapshot.UserId);",
                  "      pm.expect(Recovered.GambitTableId).to.eql(Snapshot.GambitTableId);",
                  "      pm.expect(Recovered.Status).to.eql(Snapshot.Status);",
                  "      pm.expect(Recovered.Result).to.eql(Snapshot.Result);",
                  "    });",
                  "    Check('recovered session preserves purchase, burn counters, points and next effect', () => {",
                  "      for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining', 'AccumulatedPoints', 'NextEffect']) pm.expect(Recovered[Field]).to.deep.equal(Snapshot[Field]);",
                  "    });",
                  "    Check('recovered session preserves revealed, hidden and pending board state', () => pm.expect(Recovered.Grid).to.deep.equal(Snapshot.Grid));",
                  "    Check('recovered response is the same complete persisted Gambit snapshot', () => pm.expect(Recovered).to.deep.equal(Snapshot));",
                  "",
                  "    const RegistryAfter = await Read('/sessions/me/active', Token, 'registry after reauthentication');",
                  "    Check('active-session registry remains tied to the recovered Gambit session', () => {",
                  "      pm.expect(RegistryAfter).to.deep.equal(RegistryBefore);",
                  "      pm.expect(RegistryAfter.GameType).to.eql('Gambit');",
                  "      pm.expect(RegistryAfter.ReferenceId).to.eql(Recovered.GambitSessionId);",
                  "    });",
                  "    const BalanceAfterRecovery = await Read('/user/chips', Token, 'balance after recovery');",
                  "    Check('reauthentication and recovery preserve the chip balance', () => pm.expect(BalanceAfterRecovery.ChipBalance).to.eql(BalanceBefore));",
                  "",
                  "    const ReadyForContinuation = await ResolveBlockingState(Recovered, Token, 'recovered pending state', true);",
                  "    const SecondBurn = await BurnOne(ReadyForContinuation, Token, 'post-reauthentication continuation');",
                  "    const Continued = await ResolveBlockingState(SecondBurn, Token, 'post-reauthentication continuation', true);",
                  "    const PersistedContinuation = await Read('/gambit/sessions/active', Token, 'continued Gambit session persisted state');",
                  "    Check('continued state is persisted and immediately recoverable', () => pm.expect(PersistedContinuation).to.deep.equal(Continued));",
                  "    Check('continued action remains on the same recovered session', () => {",
                  "      pm.expect(PersistedContinuation.GambitSessionId).to.eql(Snapshot.GambitSessionId);",
                  "      pm.expect(PersistedContinuation.ManualFlipsCount).to.eql(Snapshot.ManualFlipsCount + 1);",
                  "    });",
                  "    const BalanceAfterContinuation = await Read('/user/chips', Token, 'balance after continuation');",
                  "    Check('continued Gambit action does not create an unconfigured chip debit', () => pm.expect(BalanceAfterContinuation.ChipBalance).to.eql(BalanceBefore));",
                  "",
                  "  } catch (Error) {",
                  "    FlowError = Error;",
                  "  } finally {",
                  "    try {",
                  "      const Cleanup = await FinishAndCashOut(Token);",
                  "      pm.test('TC-017 - API cleanup leaves no temporary Gambit session open', () => pm.expect(Cleanup.CashOut === null || Cleanup.CashOut.code === 201).to.eql(true));",
                  "      const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "      pm.test('TC-017 - temporary user is deactivated through the authenticated API', () => {",
                  "        pm.expect(Deactivation.code).to.eql(200);",
                  "        pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' });",
                  "      });",
                  "    } catch (CleanupError) {",
                  "      pm.test('TC-017 - API-only final cleanup completes', () => { throw CleanupError; });",
                  "    }",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((Error) => {",
                  "  pm.test('TC-017 - recovery, continuation and cleanup flow completes', () => { throw Error; });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc017SecondToken}}"
              }
            ],
            "url": "{{baseUrl}}/gambit/sessions/active"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-018 - Two users can play the same Slot independently",
      "item": [
        {
          "name": "TC-018 - Two users keep independent sessions on the same Slot Machine",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-018 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a structurally valid Slot state', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Session.StartedAt).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.EndedAt).to.eql(null);",
                  "    pm.expect(Session.CurrentRewardSnapshot).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4);",
                  "    Session.CurrentSpinResult.Reels.forEach((Reel, Index) => {",
                  "      pm.expect(Reel.ReelIndex).to.eql(Index);",
                  "      pm.expect(Reel.SymbolId).to.be.a('string').and.not.empty;",
                  "    });",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Max).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.DeletedAt).to.eql(null);",
                  "  });",
                  "}",
                  "function AssertReroll(Before, Response, SelectedReel, ExpectedBalance, Label) {",
                  "  Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const After = Response.json().session;",
                  "  AssertSessionShape(After, Label + ' result');",
                  "  Check(Label + ' updates only deterministic invariants of its own session', () => {",
                  "    pm.expect(After.SlotSessionId).to.eql(Before.SlotSessionId);",
                  "    pm.expect(After.UserId).to.eql(Before.UserId);",
                  "    pm.expect(After.SlotMachineId).to.eql(Before.SlotMachineId);",
                  "    pm.expect(After.CurrentRerollsSpent.Rerolls.Used).to.eql(Before.CurrentRerollsSpent.Rerolls.Used + 1);",
                  "    Before.CurrentSpinResult.Reels.forEach((BeforeReel) => {",
                  "      const AfterReel = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex);",
                  "      pm.expect(AfterReel).to.exist;",
                  "      if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel);",
                  "    });",
                  "    const Selected = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel);",
                  "    pm.expect(Selected.SymbolId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Response.json().currentBalance).to.eql(ExpectedBalance);",
                  "  });",
                  "  return After;",
                  "}",
                  "async function CleanupUser(Token) {",
                  "  if (!Token) return;",
                  "  const Active = await CallApi('GET', '/sessions/active', undefined, Token);",
                  "  if (Active.code !== 200) throw new Error('Cleanup active-session lookup returned ' + Active.code + '.');",
                  "  if (Active.text().trim() !== '' && Active.json() !== null) {",
                  "    const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token);",
                  "    if (CashOut.code !== 201) throw new Error('Cleanup cash-out returned ' + CashOut.code + '.');",
                  "  }",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error('Cleanup user deactivation returned ' + Deactivation.code + '.');",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc018TokenA', 'tc018TokenB', 'tc018CleanupTokenA', 'tc018CleanupTokenB', 'tc018SnapshotB', 'tc018SessionAAfterAction', 'tc018BalanceAAfterAction', 'tc018BalanceBBeforeAAction', 'tc018RegistryA', 'tc018RegistryB', 'tc018RerollCost']) pm.variables.unset(Key);",
                  "",
                  "  const MachinesResponse = await CallApi('GET', '/slot/machine');",
                  "  Check('Slot Machine list is available through the API', () => {",
                  "    pm.expect(MachinesResponse.code).to.eql(200);",
                  "    pm.expect(MachinesResponse.json()).to.be.an('array');",
                  "  });",
                  "  const Machine = MachinesResponse.json().find((Candidate) => Candidate.Name === 'Slot 1');",
                  "  Check('official Slot 1 fixture is available before temporary users are registered', () => {",
                  "    if (!Machine) throw new Error('TC-018 depende da fixture oficial \"Slot 1\", ainda não disponível neste ambiente.');",
                  "  });",
                  "  Check('official Slot 1 fixture is active and has valid numeric configuration', () => {",
                  "    pm.expect(Machine.SlotMachineId).to.be.a('number').and.above(0);",
                  "    pm.expect(Machine.Active).to.eql(true);",
                  "    for (const Field of ['MinimumSpinValue', 'MinimumChipsRequired', 'MinimumRerollValue']) pm.expect(typeof Machine[Field] === 'number' && Number.isFinite(Machine[Field]) && Machine[Field] >= 0).to.eql(true);",
                  "  });",
                  "",
                  "  const InitialBalance = 200;",
                  "  const RequiredBalance = Math.max(Machine.MinimumChipsRequired, Machine.MinimumSpinValue + (2 * Machine.MinimumRerollValue));",
                  "  Check('each user initial balance covers creation and all planned independent actions', () => {",
                  "    if (InitialBalance < RequiredBalance) throw new Error('TC-018 requires ' + RequiredBalance + ' chips per user but registration provides ' + InitialBalance + '.');",
                  "  });",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "  const CredentialsA = { Name: 'TC018 User A', Email: 'tc018.a.' + Suffix + '@example.test', Nickname: 'tc018a' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "  const CredentialsB = { Name: 'TC018 User B', Email: 'tc018.b.' + Suffix + '@example.test', Nickname: 'tc018b' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Bb1!' };",
                  "  Check('temporary users use different identities and credentials', () => {",
                  "    pm.expect(CredentialsA.Email).not.to.eql(CredentialsB.Email);",
                  "    pm.expect(CredentialsA.Nickname).not.to.eql(CredentialsB.Nickname);",
                  "    pm.expect(CredentialsA.Password).not.to.eql(CredentialsB.Password);",
                  "  });",
                  "",
                  "  const Register = async (Credentials, Label) => {",
                  "    const Payload = { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance };",
                  "    const Response = await CallApi('POST', '/auth/register', Payload);",
                  "    Check(Label + ' is registered through the API with 200 chips', () => {",
                  "      pm.expect(Response.code).to.eql(201);",
                  "      pm.expect(Response.json().User.Email).to.eql(Credentials.Email);",
                  "      pm.expect(Response.json().User.Nickname).to.eql(Credentials.Nickname);",
                  "      pm.expect(Response.json().User.ChipBalance).to.eql(InitialBalance);",
                  "    });",
                  "    return Response.json();",
                  "  };",
                  "  const RegisteredA = await Register(CredentialsA, 'user A');",
                  "  pm.variables.set('tc018CleanupTokenA', RegisteredA.Token);",
                  "  const RegisteredB = await Register(CredentialsB, 'user B');",
                  "  pm.variables.set('tc018CleanupTokenB', RegisteredB.Token);",
                  "  Check('users A and B have different persisted UserIds', () => pm.expect(RegisteredA.User.UserId).not.to.eql(RegisteredB.User.UserId));",
                  "",
                  "  const Login = async (Credentials, UserId, Label) => {",
                  "    const Response = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "    Check(Label + ' login returns its own valid JWT', () => {",
                  "      pm.expect(Response.code).to.eql(200);",
                  "      pm.expect(Response.json().User.UserId).to.eql(UserId);",
                  "      pm.expect(Response.json().Token).to.be.a('string').and.not.empty;",
                  "      pm.expect(Response.json().Token.split('.')).to.have.length(3);",
                  "    });",
                  "    return Response.json().Token;",
                  "  };",
                  "  const TokenA = await Login(CredentialsA, RegisteredA.User.UserId, 'user A');",
                  "  const TokenB = await Login(CredentialsB, RegisteredB.User.UserId, 'user B');",
                  "  pm.variables.set('tc018TokenA', TokenA);",
                  "  pm.variables.set('tc018TokenB', TokenB);",
                  "  pm.variables.set('tc018CleanupTokenA', TokenA);",
                  "  pm.variables.set('tc018CleanupTokenB', TokenB);",
                  "  Check('tokens remain separately associated with users A and B', () => {",
                  "    pm.expect(TokenA).to.be.a('string').and.not.empty;",
                  "    pm.expect(TokenB).to.be.a('string').and.not.empty;",
                  "    pm.expect(RegisteredA.User.UserId).not.to.eql(RegisteredB.User.UserId);",
                  "  });",
                  "",
                  "  const BalanceA = await Read('/user/chips', TokenA, 'user A initial balance');",
                  "  const BalanceB = await Read('/user/chips', TokenB, 'user B initial balance');",
                  "  Check('both users independently persist the agreed initial balance', () => {",
                  "    pm.expect(BalanceA.ChipBalance).to.eql(InitialBalance);",
                  "    pm.expect(BalanceB.ChipBalance).to.eql(InitialBalance);",
                  "  });",
                  "",
                  "  const CreateA = await CallApi('POST', '/slot-machines/' + Machine.SlotMachineId + '/sessions', {}, TokenA);",
                  "  const CreateB = await CallApi('POST', '/slot-machines/' + Machine.SlotMachineId + '/sessions', {}, TokenB);",
                  "  Check('both users create sessions on the same official Slot 1 fixture', () => {",
                  "    pm.expect(CreateA.code).to.eql(201);",
                  "    pm.expect(CreateB.code).to.eql(201);",
                  "  });",
                  "  const SessionA = CreateA.json().session;",
                  "  const SessionB = CreateB.json().session;",
                  "  console.log('TC-018 session IDs: A=' + SessionA.SlotSessionId + ', B=' + SessionB.SlotSessionId);",
                  "  AssertSessionShape(SessionA, 'user A created session');",
                  "  AssertSessionShape(SessionB, 'user B created session');",
                  "  Check('sessions are distinct, owned correctly and use the same Slot Machine', () => {",
                  "    pm.expect(SessionA.UserId).to.eql(RegisteredA.User.UserId);",
                  "    pm.expect(SessionB.UserId).to.eql(RegisteredB.User.UserId);",
                  "    pm.expect(SessionA.UserId).not.to.eql(SessionB.UserId);",
                  "    pm.expect(SessionA.SlotSessionId).not.to.eql(SessionB.SlotSessionId);",
                  "    pm.expect(SessionA.SlotMachineId).to.eql(Machine.SlotMachineId);",
                  "    pm.expect(SessionB.SlotMachineId).to.eql(Machine.SlotMachineId);",
                  "  });",
                  "  const BalanceAfterCreation = InitialBalance - Machine.MinimumSpinValue;",
                  "  Check('each creation debits only its owner by the configured spin cost', () => {",
                  "    pm.expect(CreateA.json().currentBalance).to.eql(BalanceAfterCreation);",
                  "    pm.expect(CreateB.json().currentBalance).to.eql(BalanceAfterCreation);",
                  "  });",
                  "",
                  "  const RegistryA = await Read('/sessions/me/active', TokenA, 'user A active-session registry');",
                  "  const RegistryB = await Read('/sessions/me/active', TokenB, 'user B active-session registry');",
                  "  Check('both users simultaneously keep separate Slot registry entries', () => {",
                  "    pm.expect(RegistryA.UserId).to.eql(RegisteredA.User.UserId);",
                  "    pm.expect(RegistryB.UserId).to.eql(RegisteredB.User.UserId);",
                  "    pm.expect(RegistryA.GameType).to.eql('Slot');",
                  "    pm.expect(RegistryB.GameType).to.eql('Slot');",
                  "    pm.expect(RegistryA.ReferenceId).to.eql(SessionA.SlotSessionId);",
                  "    pm.expect(RegistryB.ReferenceId).to.eql(SessionB.SlotSessionId);",
                  "    pm.expect(RegistryA.ReferenceId).not.to.eql(RegistryB.ReferenceId);",
                  "  });",
                  "",
                  "  const SnapshotB = await Read('/sessions/active', TokenB, 'user B snapshot before user A action');",
                  "  const BalanceBBeforeA = await Read('/user/chips', TokenB, 'user B balance before user A action');",
                  "  Check('user B baseline matches its untouched created state', () => {",
                  "    pm.expect(SnapshotB).to.deep.equal(SessionB);",
                  "    pm.expect(BalanceBBeforeA.ChipBalance).to.eql(BalanceAfterCreation);",
                  "  });",
                  "",
                  "  const RerollCost = Machine.MinimumRerollValue;",
                  "  const RerollAResponse = await CallApi('POST', '/sessions/active/reroll/0', undefined, TokenA);",
                  "  const SessionAAfterAction = AssertReroll(SessionA, RerollAResponse, 0, BalanceAfterCreation - RerollCost, 'user A reroll');",
                  "  const PersistedA = await Read('/sessions/active', TokenA, 'user A state after its own action');",
                  "  Check('user A action is persisted only in user A session', () => pm.expect(PersistedA).to.deep.equal(SessionAAfterAction));",
                  "  const BalanceAAfterAction = await Read('/user/chips', TokenA, 'user A balance after its own action');",
                  "  Check('user A alone pays its configured reroll cost', () => pm.expect(BalanceAAfterAction.ChipBalance).to.eql(BalanceAfterCreation - RerollCost));",
                  "",
                  "  pm.variables.set('tc018SnapshotB', JSON.stringify(SnapshotB));",
                  "  pm.variables.set('tc018SessionAAfterAction', JSON.stringify(SessionAAfterAction));",
                  "  pm.variables.set('tc018BalanceAAfterAction', BalanceAAfterAction.ChipBalance);",
                  "  pm.variables.set('tc018BalanceBBeforeAAction', BalanceBBeforeA.ChipBalance);",
                  "  pm.variables.set('tc018RegistryA', JSON.stringify(RegistryA));",
                  "  pm.variables.set('tc018RegistryB', JSON.stringify(RegistryB));",
                  "  pm.variables.set('tc018RerollCost', RerollCost);",
                  "})().catch(async (Error) => {",
                  "  const CleanupErrors = [];",
                  "  for (const Token of [pm.variables.get('tc018CleanupTokenA'), pm.variables.get('tc018CleanupTokenB')]) {",
                  "    try { await CleanupUser(Token); } catch (CleanupError) { CleanupErrors.push(CleanupError); }",
                  "  }",
                  "  if (CleanupErrors.length) pm.test('TC-018 - API cleanup after preparation failure completes for both users', () => { throw CleanupErrors[0]; });",
                  "  pm.test('TC-018 - preparation completes', () => { throw Error; });",
                  "  pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-018 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a structurally valid Slot state', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Session.CurrentRewardSnapshot).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4);",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0);",
                  "  });",
                  "}",
                  "function AssertReroll(Before, Response, SelectedReel, ExpectedBalance, Label) {",
                  "  Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const After = Response.json().session;",
                  "  AssertSessionShape(After, Label + ' result');",
                  "  Check(Label + ' updates only deterministic invariants of its own session', () => {",
                  "    pm.expect(After.SlotSessionId).to.eql(Before.SlotSessionId);",
                  "    pm.expect(After.UserId).to.eql(Before.UserId);",
                  "    pm.expect(After.SlotMachineId).to.eql(Before.SlotMachineId);",
                  "    pm.expect(After.CurrentRerollsSpent.Rerolls.Used).to.eql(Before.CurrentRerollsSpent.Rerolls.Used + 1);",
                  "    Before.CurrentSpinResult.Reels.forEach((BeforeReel) => {",
                  "      const AfterReel = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex);",
                  "      pm.expect(AfterReel).to.exist;",
                  "      if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel);",
                  "    });",
                  "    pm.expect(After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel).SymbolId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Response.json().currentBalance).to.eql(ExpectedBalance);",
                  "  });",
                  "  return After;",
                  "}",
                  "async function CleanupUser(Token, Label) {",
                  "  const Active = await CallApi('GET', '/sessions/active', undefined, Token);",
                  "  if (Active.code !== 200) throw new Error(Label + ' cleanup active-session lookup returned ' + Active.code + '.');",
                  "  if (Active.text().trim() !== '' && Active.json() !== null) {",
                  "    const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token);",
                  "    if (CashOut.code !== 201) throw new Error(Label + ' cleanup cash-out returned ' + CashOut.code + '.');",
                  "  }",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error(Label + ' cleanup user deactivation returned ' + Deactivation.code + '.');",
                  "  return Deactivation;",
                  "}",
                  "",
                  "(async function () {",
                  "  const TokenA = pm.variables.get('tc018TokenA');",
                  "  const TokenB = pm.variables.get('tc018TokenB');",
                  "  const SnapshotB = JSON.parse(pm.variables.get('tc018SnapshotB'));",
                  "  const PreparedA = JSON.parse(pm.variables.get('tc018SessionAAfterAction'));",
                  "  const RegistryAInitial = JSON.parse(pm.variables.get('tc018RegistryA'));",
                  "  const RegistryBInitial = JSON.parse(pm.variables.get('tc018RegistryB'));",
                  "  const BalanceAStored = Number(pm.variables.get('tc018BalanceAAfterAction'));",
                  "  const BalanceBStored = Number(pm.variables.get('tc018BalanceBBeforeAAction'));",
                  "  const RerollCost = Number(pm.variables.get('tc018RerollCost'));",
                  "  let FlowError;",
                  "  try {",
                  "    Check('main isolation request uses only user B token', () => {",
                  "      pm.expect(TokenA).to.be.a('string').and.not.empty;",
                  "      pm.expect(TokenB).to.be.a('string').and.not.empty;",
                  "      pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + TokenB);",
                  "    });",
                  "    Check('user B session lookup after user A action returns 200', () => pm.response.to.have.status(200));",
                  "    const BAfterAAction = pm.response.json();",
                  "    AssertSessionShape(BAfterAAction, 'user B session after user A action');",
                  "    Check('user A action leaves the complete user B session unchanged', () => pm.expect(BAfterAAction).to.deep.equal(SnapshotB));",
                  "    Check('user A action preserves every user B gameplay and lifecycle field', () => {",
                  "      pm.expect(BAfterAAction.SlotSessionId).to.eql(SnapshotB.SlotSessionId);",
                  "      pm.expect(BAfterAAction.CurrentSpinResult.Reels).to.deep.equal(SnapshotB.CurrentSpinResult.Reels);",
                  "      pm.expect(BAfterAAction.CurrentRerollsSpent.Rerolls).to.deep.equal(SnapshotB.CurrentRerollsSpent.Rerolls);",
                  "      pm.expect(BAfterAAction.CurrentRewardSnapshot).to.eql(SnapshotB.CurrentRewardSnapshot);",
                  "      for (const Field of ['Status', 'StartedAt', 'EndedAt', 'DeletedAt']) pm.expect(BAfterAAction[Field]).to.eql(SnapshotB[Field]);",
                  "    });",
                  "    const BalanceBAfterA = await Read('/user/chips', TokenB, 'user B balance after user A action');",
                  "    Check('user A action leaves user B balance unchanged', () => pm.expect(BalanceBAfterA.ChipBalance).to.eql(BalanceBStored));",
                  "",
                  "    const SnapshotABeforeB = await Read('/sessions/active', TokenA, 'user A snapshot before user B action');",
                  "    Check('user A prepared snapshot matches its persisted post-reroll state', () => pm.expect(SnapshotABeforeB).to.deep.equal(PreparedA));",
                  "    const BalanceABeforeB = await Read('/user/chips', TokenA, 'user A balance before user B action');",
                  "    Check('user A balance baseline includes only user A own costs', () => pm.expect(BalanceABeforeB.ChipBalance).to.eql(BalanceAStored));",
                  "",
                  "    const RerollBResponse = await CallApi('POST', '/sessions/active/reroll/1', undefined, TokenB);",
                  "    const SessionBAfterAction = AssertReroll(BAfterAAction, RerollBResponse, 1, BalanceBStored - RerollCost, 'user B reroll');",
                  "    const PersistedB = await Read('/sessions/active', TokenB, 'user B state after its own action');",
                  "    Check('user B action is persisted only in user B session', () => pm.expect(PersistedB).to.deep.equal(SessionBAfterAction));",
                  "    const BalanceBAfterOwnAction = await Read('/user/chips', TokenB, 'user B balance after its own action');",
                  "    Check('user B alone pays its configured reroll cost', () => pm.expect(BalanceBAfterOwnAction.ChipBalance).to.eql(BalanceBStored - RerollCost));",
                  "",
                  "    const AAfterBAction = await Read('/sessions/active', TokenA, 'user A session after user B action');",
                  "    Check('user B action leaves the complete user A session unchanged', () => pm.expect(AAfterBAction).to.deep.equal(SnapshotABeforeB));",
                  "    const BalanceAAfterB = await Read('/user/chips', TokenA, 'user A balance after user B action');",
                  "    Check('user B action leaves user A balance unchanged', () => pm.expect(BalanceAAfterB.ChipBalance).to.eql(BalanceABeforeB.ChipBalance));",
                  "    const RegistryAAfterB = await Read('/sessions/me/active', TokenA, 'user A registry after user B action');",
                  "    Check('user B action leaves user A registry tied only to session A', () => {",
                  "      pm.expect(RegistryAAfterB).to.deep.equal(RegistryAInitial);",
                  "      pm.expect(RegistryAAfterB.ReferenceId).to.eql(SnapshotABeforeB.SlotSessionId);",
                  "    });",
                  "",
                  "    const CashOutA = await CallApi('POST', '/sessions/active/cash-out', undefined, TokenA);",
                  "    Check('user A independently cashes out only session A', () => {",
                  "      pm.expect(CashOutA.code).to.eql(201);",
                  "      pm.expect(CashOutA.json()).to.deep.equal({ message: 'Cash out successful', finalBalance: BalanceABeforeB.ChipBalance + SnapshotABeforeB.CurrentRewardSnapshot });",
                  "    });",
                  "    const ActiveAAfterCashOut = await Read('/sessions/active', TokenA, 'user A active session after cash-out');",
                  "    Check('user A has no active Slot session after its cash-out', () => pm.expect(ActiveAAfterCashOut).to.eql(null));",
                  "",
                  "    const BAfterACashOut = await Read('/sessions/active', TokenB, 'user B session after user A cash-out');",
                  "    Check('user A cash-out does not close or alter user B session', () => {",
                  "      pm.expect(BAfterACashOut).to.deep.equal(SessionBAfterAction);",
                  "      pm.expect(BAfterACashOut.SlotSessionId).to.eql(SnapshotB.SlotSessionId);",
                  "      pm.expect(BAfterACashOut.Status).to.eql('InProgress');",
                  "    });",
                  "    const RegistryBAfterACashOut = await Read('/sessions/me/active', TokenB, 'user B registry after user A cash-out');",
                  "    Check('user A cash-out leaves user B registry untouched', () => pm.expect(RegistryBAfterACashOut).to.deep.equal(RegistryBInitial));",
                  "    const BalanceBAfterACashOut = await Read('/user/chips', TokenB, 'user B balance after user A cash-out');",
                  "    Check('user A cash-out leaves user B balance untouched', () => pm.expect(BalanceBAfterACashOut.ChipBalance).to.eql(BalanceBAfterOwnAction.ChipBalance));",
                  "",
                  "    const ContinuationBResponse = await CallApi('POST', '/sessions/active/reroll/2', undefined, TokenB);",
                  "    const ContinuedB = AssertReroll(BAfterACashOut, ContinuationBResponse, 2, BalanceBAfterACashOut.ChipBalance - RerollCost, 'user B continuation after user A cash-out');",
                  "    const PersistedContinuedB = await Read('/sessions/active', TokenB, 'user B continued state after user A cash-out');",
                  "    Check('user B remains playable and persists its action after user A ends', () => pm.expect(PersistedContinuedB).to.deep.equal(ContinuedB));",
                  "",
                  "    const CashOutB = await CallApi('POST', '/sessions/active/cash-out', undefined, TokenB);",
                  "    Check('user B cleanup cashes out its own remaining session', () => {",
                  "      pm.expect(CashOutB.code).to.eql(201);",
                  "      pm.expect(CashOutB.json()).to.deep.equal({ message: 'Cash out successful', finalBalance: ContinuationBResponse.json().currentBalance + ContinuedB.CurrentRewardSnapshot });",
                  "    });",
                  "    const ActiveBAfterCashOut = await Read('/sessions/active', TokenB, 'user B active session after cleanup cash-out');",
                  "    Check('user B has no active Slot session after cleanup', () => pm.expect(ActiveBAfterCashOut).to.eql(null));",
                  "    const RegistryBAfterCashOut = await Read('/sessions/me/active', TokenB, 'user B registry after cleanup cash-out');",
                  "    Check('user B cleanup releases only user B registry', () => pm.expect(RegistryBAfterCashOut).to.eql(null));",
                  "  } catch (Error) {",
                  "    FlowError = Error;",
                  "  } finally {",
                  "    const CleanupErrors = [];",
                  "    for (const [Token, Label] of [[TokenA, 'user A'], [TokenB, 'user B']]) {",
                  "      try {",
                  "        const Deactivation = await CleanupUser(Token, Label);",
                  "        pm.test('TC-018 - ' + Label + ' is deactivated through API-only cleanup', () => pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' }));",
                  "      } catch (CleanupError) { CleanupErrors.push(CleanupError); }",
                  "    }",
                  "    if (CleanupErrors.length) pm.test('TC-018 - API-only final cleanup completes for both users', () => { throw CleanupErrors[0]; });",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((Error) => {",
                  "  pm.test('TC-018 - simultaneous isolation and cleanup flow completes', () => { throw Error; });",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc018TokenB}}"
              }
            ],
            "url": "{{baseUrl}}/sessions/active"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-019 - Two users can play the same Gambit table independently",
      "item": [
        {
          "name": "TC-019 - Two users keep independent sessions on the same Gambit Table",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-019 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function SessionView(Payload) {",
                  "  if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload;",
                  "  const View = Object.assign({}, Payload);",
                  "  delete View.PeekResult;",
                  "  return View;",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a complete valid Gambit view', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.GambitTableId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount));",
                  "    pm.expect(Session.AccumulatedPoints).to.be.a('number');",
                  "    pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true);",
                  "    pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']);",
                  "    pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true);",
                  "    pm.expect(Session.Grid.Revealed).to.be.an('array');",
                  "    pm.expect(Session.Grid.Unrevealed).to.be.an('array');",
                  "    pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25);",
                  "    pm.expect(Session.Grid).to.have.property('PendingEvent');",
                  "    pm.expect(Session.Grid).to.have.property('PendingInteraction');",
                  "  });",
                  "}",
                  "async function ResolveBlockingState(Session, Token) {",
                  "  let Current = SessionView(Session);",
                  "  for (let Steps = 0; Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent); Steps += 1) {",
                  "    if (Steps >= 20) throw new Error('Pending Gambit state did not converge after 20 API resolutions.');",
                  "    let Response;",
                  "    if (Current.Grid.PendingInteraction) {",
                  "      const Required = Current.Grid.PendingInteraction.RequiredSelections;",
                  "      const Candidates = Current.Grid.Unrevealed.filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0).filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index);",
                  "      if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error('Pending interaction lacks enough distinct unrevealed positions.');",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: Candidates.slice(0, Required).map((Card) => Card.Position) }, Token);",
                  "    } else {",
                  "      const Event = Current.Grid.PendingEvent;",
                  "      const GoodIndex = Array.isArray(Event.GoodOptions) ? Event.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      const BadIndex = Array.isArray(Event.BadOptions) ? Event.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error('Pending event lacks selectable options accepted by ResolveEventDto.');",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: GoodIndex, BadIndex: BadIndex }, Token);",
                  "    }",
                  "    if (Response.code !== 201) throw new Error('Pending-state resolution returned ' + Response.code + '.');",
                  "    Current = SessionView(Response.json());",
                  "  }",
                  "  return Current;",
                  "}",
                  "async function FinishAndCashOut(Token) {",
                  "  const Response = await CallApi('GET', '/gambit/sessions/active', undefined, Token);",
                  "  if (Response.code !== 200) throw new Error('Cleanup active-session lookup returned ' + Response.code + '.');",
                  "  let Current = Response.text().trim() === '' ? null : SessionView(Response.json());",
                  "  for (let Steps = 0; Current && Steps < 100; Steps += 1) {",
                  "    if (Current.Status === 'Finished') {",
                  "      const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token);",
                  "      if (CashOut.code !== 201) throw new Error('Cleanup cash-out returned ' + CashOut.code + '.');",
                  "      return { Finished: Current, CashOut };",
                  "    }",
                  "    if (Current.Status !== 'InProgress') return { Finished: Current, CashOut: null };",
                  "    if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) { Current = await ResolveBlockingState(Current, Token); continue; }",
                  "    const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "    if (!Target) throw new Error('Cleanup found no burnable card in an InProgress session.');",
                  "    const Burn = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "    if (Burn.code !== 201) throw new Error('Cleanup burn returned ' + Burn.code + '.');",
                  "    Current = SessionView(Burn.json());",
                  "  }",
                  "  if (Current) throw new Error('Cleanup did not finish the Gambit session within 100 API actions.');",
                  "  return { Finished: null, CashOut: null };",
                  "}",
                  "async function CleanupUser(Token) {",
                  "  if (!Token) return;",
                  "  await FinishAndCashOut(Token);",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error('Cleanup user deactivation returned ' + Deactivation.code + '.');",
                  "}",
                  "async function BurnOne(Session, Token, Label) {",
                  "  if (Session.Status !== 'InProgress' || Session.Grid.PendingEvent || Session.Grid.PendingInteraction) throw new Error(Label + ' requires an unblocked InProgress session.');",
                  "  const Target = Session.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "  Check(Label + ' has a valid card available to burn', () => pm.expect(Target).to.exist);",
                  "  const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "  Check(Label + ' burn succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const Burned = SessionView(Response.json());",
                  "  AssertSessionShape(Burned, Label + ' result');",
                  "  Check(Label + ' changes only deterministic burn invariants', () => {",
                  "    pm.expect(Burned.GambitSessionId).to.eql(Session.GambitSessionId);",
                  "    pm.expect(Burned.UserId).to.eql(Session.UserId);",
                  "    pm.expect(Burned.GambitTableId).to.eql(Session.GambitTableId);",
                  "    pm.expect(Burned.ManualFlipsCount).to.eql(Session.ManualFlipsCount + 1);",
                  "    pm.expect(Burned.Grid.Revealed.length).to.eql(Session.Grid.Revealed.length + 1);",
                  "    pm.expect(Burned.Grid.Unrevealed.length).to.eql(Session.Grid.Unrevealed.length - 1);",
                  "    pm.expect(Burned.Grid.Revealed.some((Card) => Card.Position === Target.Position)).to.eql(true);",
                  "  });",
                  "  return Burned;",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc019TokenA', 'tc019TokenB', 'tc019CleanupTokenA', 'tc019CleanupTokenB', 'tc019SnapshotB', 'tc019SessionAAfterAction', 'tc019BalanceB', 'tc019RegistryB', 'tc019BalanceAfterCreation']) pm.variables.unset(Key);",
                  "  const TablesResponse = await CallApi('GET', '/gambit-table');",
                  "  Check('Gambit Table list is available through the API', () => { pm.expect(TablesResponse.code).to.eql(200); pm.expect(TablesResponse.json()).to.be.an('array'); });",
                  "  const Table = TablesResponse.json().find((Candidate) => Candidate.Name === 'Gambit 1');",
                  "  Check('official Gambit 1 fixture is available before temporary users are registered', () => { if (!Table) throw new Error('TC-019 depende da fixture oficial \"Gambit 1\", ainda não disponível neste ambiente.'); });",
                  "  Check('official Gambit 1 fixture is active and has valid numeric configuration', () => {",
                  "    pm.expect(Table.GambitTableId).to.be.a('number').and.above(0);",
                  "    pm.expect(Table.Active).to.eql(true);",
                  "    for (const Field of ['MinimumChipsRequired', 'CardPrice', 'TableMultiplier', 'MinimumCardsPurchased', 'MaxCardsPurchased']) pm.expect(typeof Table[Field] === 'number' && Number.isFinite(Table[Field]) && Table[Field] >= 0).to.eql(true);",
                  "    pm.expect(Table.MinimumCardsPurchased).to.be.at.least(1);",
                  "    pm.expect(Table.MaxCardsPurchased).to.be.at.least(Table.MinimumCardsPurchased);",
                  "  });",
                  "  const CardsPurchased = Table.MinimumCardsPurchased;",
                  "  const SessionCost = CardsPurchased * Table.CardPrice;",
                  "  const InitialBalance = 200;",
                  "  const RequiredBalance = Math.max(Table.MinimumChipsRequired, SessionCost);",
                  "  Check('each user initial balance covers Gambit 1 access and minimum card purchase', () => { if (InitialBalance < RequiredBalance) throw new Error('TC-019 requires ' + RequiredBalance + ' chips per user but registration provides ' + InitialBalance + '.'); });",
                  "",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "  const CredentialsA = { Name: 'TC019 User A', Email: 'tc019.a.' + Suffix + '@example.test', Nickname: 'tc019a' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "  const CredentialsB = { Name: 'TC019 User B', Email: 'tc019.b.' + Suffix + '@example.test', Nickname: 'tc019b' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Bb1!' };",
                  "  Check('temporary users use different identities and credentials', () => { pm.expect(CredentialsA.Email).not.to.eql(CredentialsB.Email); pm.expect(CredentialsA.Nickname).not.to.eql(CredentialsB.Nickname); pm.expect(CredentialsA.Password).not.to.eql(CredentialsB.Password); });",
                  "  const Register = async (Credentials, Label) => {",
                  "    const Response = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance });",
                  "    Check(Label + ' is registered through the API with 200 chips', () => { pm.expect(Response.code).to.eql(201); pm.expect(Response.json().User.Email).to.eql(Credentials.Email); pm.expect(Response.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "    return Response.json();",
                  "  };",
                  "  const RegisteredA = await Register(CredentialsA, 'user A');",
                  "  pm.variables.set('tc019CleanupTokenA', RegisteredA.Token);",
                  "  const RegisteredB = await Register(CredentialsB, 'user B');",
                  "  pm.variables.set('tc019CleanupTokenB', RegisteredB.Token);",
                  "  Check('users A and B have different persisted UserIds', () => pm.expect(RegisteredA.User.UserId).not.to.eql(RegisteredB.User.UserId));",
                  "  const Login = async (Credentials, UserId, Label) => {",
                  "    const Response = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "    Check(Label + ' login returns its own valid JWT', () => { pm.expect(Response.code).to.eql(200); pm.expect(Response.json().User.UserId).to.eql(UserId); pm.expect(Response.json().Token.split('.')).to.have.length(3); });",
                  "    return Response.json().Token;",
                  "  };",
                  "  const TokenA = await Login(CredentialsA, RegisteredA.User.UserId, 'user A');",
                  "  const TokenB = await Login(CredentialsB, RegisteredB.User.UserId, 'user B');",
                  "  pm.variables.set('tc019TokenA', TokenA); pm.variables.set('tc019TokenB', TokenB); pm.variables.set('tc019CleanupTokenA', TokenA); pm.variables.set('tc019CleanupTokenB', TokenB);",
                  "  Check('tokens remain separately associated with users A and B', () => { pm.expect(TokenA).to.be.a('string').and.not.empty; pm.expect(TokenB).to.be.a('string').and.not.empty; pm.expect(RegisteredA.User.UserId).not.to.eql(RegisteredB.User.UserId); });",
                  "  const BalanceA = await Read('/user/chips', TokenA, 'user A initial balance');",
                  "  const BalanceB = await Read('/user/chips', TokenB, 'user B initial balance');",
                  "  Check('both users independently persist 200 initial chips', () => { pm.expect(BalanceA.ChipBalance).to.eql(InitialBalance); pm.expect(BalanceB.ChipBalance).to.eql(InitialBalance); });",
                  "",
                  "  const CreationA = await CallApi('POST', '/gambit-tables/' + Table.GambitTableId + '/sessions', { CardsPurchased: CardsPurchased }, TokenA);",
                  "  const CreationB = await CallApi('POST', '/gambit-tables/' + Table.GambitTableId + '/sessions', { CardsPurchased: CardsPurchased }, TokenB);",
                  "  Check('both users create sessions on the same official Gambit 1 fixture', () => { pm.expect(CreationA.code).to.eql(201); pm.expect(CreationB.code).to.eql(201); });",
                  "  const SessionA = SessionView(CreationA.json().session);",
                  "  const SessionB = SessionView(CreationB.json().session);",
                  "  console.log('TC-019 session IDs: A=' + SessionA.GambitSessionId + ', B=' + SessionB.GambitSessionId);",
                  "  AssertSessionShape(SessionA, 'user A created session'); AssertSessionShape(SessionB, 'user B created session');",
                  "  Check('sessions are distinct, owned correctly and use the same Gambit Table', () => {",
                  "    pm.expect(SessionA.UserId).to.eql(RegisteredA.User.UserId); pm.expect(SessionB.UserId).to.eql(RegisteredB.User.UserId);",
                  "    pm.expect(SessionA.UserId).not.to.eql(SessionB.UserId); pm.expect(SessionA.GambitSessionId).not.to.eql(SessionB.GambitSessionId);",
                  "    pm.expect(SessionA.GambitTableId).to.eql(Table.GambitTableId); pm.expect(SessionB.GambitTableId).to.eql(Table.GambitTableId);",
                  "    pm.expect(SessionA.CardsPurchased).to.eql(CardsPurchased); pm.expect(SessionB.CardsPurchased).to.eql(CardsPurchased);",
                  "  });",
                  "  const BalanceAfterCreation = InitialBalance - SessionCost;",
                  "  Check('each session creation debits only its owner by CardsPurchased times CardPrice', () => { pm.expect(CreationA.json().currentBalance).to.eql(BalanceAfterCreation); pm.expect(CreationB.json().currentBalance).to.eql(BalanceAfterCreation); });",
                  "  const RegistryA = await Read('/sessions/me/active', TokenA, 'user A active-session registry');",
                  "  const RegistryB = await Read('/sessions/me/active', TokenB, 'user B active-session registry');",
                  "  Check('both users simultaneously keep separate Gambit registry entries', () => {",
                  "    pm.expect(RegistryA.UserId).to.eql(SessionA.UserId); pm.expect(RegistryB.UserId).to.eql(SessionB.UserId);",
                  "    pm.expect(RegistryA.GameType).to.eql('Gambit'); pm.expect(RegistryB.GameType).to.eql('Gambit');",
                  "    pm.expect(RegistryA.ReferenceId).to.eql(SessionA.GambitSessionId); pm.expect(RegistryB.ReferenceId).to.eql(SessionB.GambitSessionId);",
                  "  });",
                  "  const SnapshotB = await Read('/gambit/sessions/active', TokenB, 'user B snapshot before user A action');",
                  "  const BalanceBBeforeA = await Read('/user/chips', TokenB, 'user B balance before user A action');",
                  "  const RegistryBBeforeA = await Read('/sessions/me/active', TokenB, 'user B registry before user A action');",
                  "  Check('user B baseline matches its untouched created state', () => { pm.expect(SnapshotB).to.deep.equal(SessionB); pm.expect(BalanceBBeforeA.ChipBalance).to.eql(BalanceAfterCreation); pm.expect(RegistryBBeforeA).to.deep.equal(RegistryB); });",
                  "  const BurnedA = await BurnOne(SessionA, TokenA, 'user A first action');",
                  "  const PersistedA = await Read('/gambit/sessions/active', TokenA, 'user A state after its own action');",
                  "  Check('user A action including any pending state is persisted without normalization', () => pm.expect(PersistedA).to.deep.equal(BurnedA));",
                  "  pm.variables.set('tc019SnapshotB', JSON.stringify(SnapshotB)); pm.variables.set('tc019SessionAAfterAction', JSON.stringify(PersistedA));",
                  "  pm.variables.set('tc019BalanceB', BalanceBBeforeA.ChipBalance); pm.variables.set('tc019RegistryB', JSON.stringify(RegistryBBeforeA)); pm.variables.set('tc019BalanceAfterCreation', BalanceAfterCreation);",
                  "})().catch(async (Error) => {",
                  "  const CleanupErrors = [];",
                  "  for (const Token of [pm.variables.get('tc019CleanupTokenA'), pm.variables.get('tc019CleanupTokenB')]) { try { await CleanupUser(Token); } catch (CleanupError) { CleanupErrors.push(CleanupError); } }",
                  "  if (CleanupErrors.length) pm.test('TC-019 - API cleanup after preparation failure completes for both users', () => { throw CleanupErrors[0]; });",
                  "  pm.test('TC-019 - preparation completes', () => { throw Error; }); pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-019 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function SessionView(Payload) { if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload; const View = Object.assign({}, Payload); delete View.PeekResult; return View; }",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a complete valid Gambit view', () => {",
                  "    pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0); pm.expect(Session.GambitTableId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount)); pm.expect(Session.AccumulatedPoints).to.be.a('number');",
                  "    pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true); pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']); pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true);",
                  "    pm.expect(Session.Grid.Revealed).to.be.an('array'); pm.expect(Session.Grid.Unrevealed).to.be.an('array'); pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25);",
                  "    pm.expect(Session.Grid).to.have.property('PendingEvent'); pm.expect(Session.Grid).to.have.property('PendingInteraction');",
                  "  });",
                  "}",
                  "async function ResolveBlockingState(Session, Token, Label, RecordAssertions) {",
                  "  let Current = SessionView(Session); let Steps = 0;",
                  "  while (Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent)) {",
                  "    Steps += 1; if (Steps > 20) throw new Error(Label + ': pending state did not converge.'); let Response;",
                  "    if (Current.Grid.PendingInteraction) {",
                  "      const Required = Current.Grid.PendingInteraction.RequiredSelections;",
                  "      const Candidates = Current.Grid.Unrevealed.filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0).filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index);",
                  "      if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error(Label + ': pending interaction lacks valid targets.');",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: Candidates.slice(0, Required).map((Card) => Card.Position) }, Token);",
                  "    } else {",
                  "      const Event = Current.Grid.PendingEvent; const GoodIndex = Array.isArray(Event.GoodOptions) ? Event.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; const BadIndex = Array.isArray(Event.BadOptions) ? Event.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1;",
                  "      if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error(Label + ': pending event lacks valid choices.');",
                  "      Response = await CallApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: GoodIndex, BadIndex: BadIndex }, Token);",
                  "    }",
                  "    if (RecordAssertions) Check(Label + ' resolves its own pending state #' + Steps, () => pm.expect(Response.code).to.eql(201)); else if (Response.code !== 201) throw new Error(Label + ': resolution returned ' + Response.code + '.');",
                  "    Current = SessionView(Response.json());",
                  "  } return Current;",
                  "}",
                  "async function BurnOne(Session, Token, Label) {",
                  "  if (Session.Status !== 'InProgress' || Session.Grid.PendingEvent || Session.Grid.PendingInteraction) throw new Error(Label + ' requires an unblocked InProgress session.');",
                  "  const Target = Session.Grid.Unrevealed.find((Card) => Card.Locked === false); Check(Label + ' has a valid card available to burn', () => pm.expect(Target).to.exist);",
                  "  const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); Check(Label + ' burn succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const Burned = SessionView(Response.json()); AssertSessionShape(Burned, Label + ' result');",
                  "  Check(Label + ' changes deterministic invariants only in its owner session', () => { pm.expect(Burned.GambitSessionId).to.eql(Session.GambitSessionId); pm.expect(Burned.UserId).to.eql(Session.UserId); pm.expect(Burned.ManualFlipsCount).to.eql(Session.ManualFlipsCount + 1); pm.expect(Burned.Grid.Revealed.length).to.eql(Session.Grid.Revealed.length + 1); pm.expect(Burned.Grid.Unrevealed.length).to.eql(Session.Grid.Unrevealed.length - 1); });",
                  "  return Burned;",
                  "}",
                  "async function FinishAndCashOut(Token) {",
                  "  let Response = await CallApi('GET', '/gambit/sessions/active', undefined, Token); if (Response.code !== 200) throw new Error('Active lookup returned ' + Response.code + '.');",
                  "  let Current = Response.text().trim() === '' ? null : SessionView(Response.json());",
                  "  for (let Steps = 0; Current && Steps < 100; Steps += 1) {",
                  "    if (Current.Status === 'Finished') { const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('Cash-out returned ' + CashOut.code + '.'); return { Finished: Current, CashOut }; }",
                  "    if (Current.Status !== 'InProgress') return { Finished: Current, CashOut: null };",
                  "    if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) { Current = await ResolveBlockingState(Current, Token, 'cleanup', false); continue; }",
                  "    const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false); if (!Target) throw new Error('No burnable cleanup card.');",
                  "    Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); if (Response.code !== 201) throw new Error('Cleanup burn returned ' + Response.code + '.'); Current = SessionView(Response.json());",
                  "  } if (Current) throw new Error('Cleanup exceeded 100 actions.'); return { Finished: null, CashOut: null };",
                  "}",
                  "async function CleanupUser(Token, Label) { await FinishAndCashOut(Token); const Response = await CallApi('DELETE', '/user', undefined, Token); if (Response.code !== 200) throw new Error(Label + ' deactivation returned ' + Response.code + '.'); return Response; }",
                  "",
                  "(async function () {",
                  "  const TokenA = pm.variables.get('tc019TokenA'); const TokenB = pm.variables.get('tc019TokenB');",
                  "  const SnapshotB = JSON.parse(pm.variables.get('tc019SnapshotB')); const PreparedA = JSON.parse(pm.variables.get('tc019SessionAAfterAction'));",
                  "  const BalanceBStored = Number(pm.variables.get('tc019BalanceB')); const RegistryBStored = JSON.parse(pm.variables.get('tc019RegistryB')); const BalanceAfterCreation = Number(pm.variables.get('tc019BalanceAfterCreation'));",
                  "  let FlowError;",
                  "  try {",
                  "    Check('main isolation request uses only user B token', () => { pm.expect(TokenA).to.be.a('string').and.not.empty; pm.expect(TokenB).to.be.a('string').and.not.empty; pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + TokenB); });",
                  "    Check('user B session lookup after user A action returns 200', () => pm.response.to.have.status(200));",
                  "    const BAfterA = pm.response.json(); AssertSessionShape(BAfterA, 'user B session after user A action');",
                  "    Check('user A action leaves the complete user B Gambit state unchanged', () => pm.expect(BAfterA).to.deep.equal(SnapshotB));",
                  "    Check('user A pending event, interaction or next effect cannot alter user B', () => { pm.expect(BAfterA.Grid.PendingEvent).to.deep.equal(SnapshotB.Grid.PendingEvent); pm.expect(BAfterA.Grid.PendingInteraction).to.deep.equal(SnapshotB.Grid.PendingInteraction); pm.expect(BAfterA.NextEffect).to.eql(SnapshotB.NextEffect); pm.expect(BAfterA.AccumulatedPoints).to.eql(SnapshotB.AccumulatedPoints); });",
                  "    const BalanceBAfterA = await Read('/user/chips', TokenB, 'user B balance after user A action'); Check('user A action leaves user B balance unchanged', () => pm.expect(BalanceBAfterA.ChipBalance).to.eql(BalanceBStored));",
                  "    const RegistryBAfterA = await Read('/sessions/me/active', TokenB, 'user B registry after user A action'); Check('user A action leaves user B registry unchanged', () => pm.expect(RegistryBAfterA).to.deep.equal(RegistryBStored));",
                  "",
                  "    const SnapshotABeforeB = await Read('/gambit/sessions/active', TokenA, 'user A snapshot before user B action'); Check('user A snapshot preserves its own pending state', () => pm.expect(SnapshotABeforeB).to.deep.equal(PreparedA));",
                  "    const BalanceABeforeB = await Read('/user/chips', TokenA, 'user A balance before user B action'); Check('user A balance contains only its own session cost', () => pm.expect(BalanceABeforeB.ChipBalance).to.eql(BalanceAfterCreation));",
                  "    const RegistryABeforeB = await Read('/sessions/me/active', TokenA, 'user A registry before user B action');",
                  "    const BurnedB = await BurnOne(BAfterA, TokenB, 'user B first action');",
                  "    const PersistedB = await Read('/gambit/sessions/active', TokenB, 'user B state after its own action'); Check('user B action including any pending state is persisted without normalization', () => pm.expect(PersistedB).to.deep.equal(BurnedB));",
                  "    const AAfterB = await Read('/gambit/sessions/active', TokenA, 'user A session after user B action'); Check('user B action leaves the complete user A Gambit state unchanged', () => pm.expect(AAfterB).to.deep.equal(SnapshotABeforeB));",
                  "    Check('user B pending event, interaction or next effect cannot alter user A', () => { pm.expect(AAfterB.Grid.PendingEvent).to.deep.equal(SnapshotABeforeB.Grid.PendingEvent); pm.expect(AAfterB.Grid.PendingInteraction).to.deep.equal(SnapshotABeforeB.Grid.PendingInteraction); pm.expect(AAfterB.NextEffect).to.eql(SnapshotABeforeB.NextEffect); pm.expect(AAfterB.AccumulatedPoints).to.eql(SnapshotABeforeB.AccumulatedPoints); });",
                  "    const BalanceAAfterB = await Read('/user/chips', TokenA, 'user A balance after user B action'); Check('user B action leaves user A balance unchanged', () => pm.expect(BalanceAAfterB).to.deep.equal(BalanceABeforeB));",
                  "    const RegistryAAfterB = await Read('/sessions/me/active', TokenA, 'user A registry after user B action'); Check('user B action leaves user A registry unchanged', () => pm.expect(RegistryAAfterB).to.deep.equal(RegistryABeforeB));",
                  "",
                  "    const CompletionA = await FinishAndCashOut(TokenA); Check('user A independently reaches cash-out through its own API context', () => { pm.expect(CompletionA.Finished.GambitSessionId).to.eql(SnapshotABeforeB.GambitSessionId); pm.expect(CompletionA.CashOut.code).to.eql(201); });",
                  "    const ActiveAAfterCashOut = await Read('/gambit/sessions/active', TokenA, 'user A session after cash-out'); Check('user A has no active Gambit session after cash-out', () => pm.expect(ActiveAAfterCashOut).to.eql(null));",
                  "    const BalanceAAfterCashOut = await Read('/user/chips', TokenA, 'user A balance after cash-out');",
                  "    const BAfterACashOut = await Read('/gambit/sessions/active', TokenB, 'user B session after user A cash-out');",
                  "    Check('user A finalization, pending resolutions and cash-out leave user B state intact', () => { pm.expect(BAfterACashOut).to.deep.equal(PersistedB); pm.expect(BAfterACashOut.GambitSessionId).to.eql(SnapshotB.GambitSessionId); pm.expect(BAfterACashOut.Status).to.eql('InProgress'); });",
                  "    const RegistryBAfterCashOut = await Read('/sessions/me/active', TokenB, 'user B registry after user A cash-out'); Check('user A cash-out leaves user B registry unchanged', () => pm.expect(RegistryBAfterCashOut).to.deep.equal(RegistryBStored));",
                  "    const BalanceBAfterCashOut = await Read('/user/chips', TokenB, 'user B balance after user A cash-out'); Check('user A cash-out leaves user B balance unchanged', () => pm.expect(BalanceBAfterCashOut.ChipBalance).to.eql(BalanceBStored));",
                  "",
                  "    const ReadyB = await ResolveBlockingState(BAfterACashOut, TokenB, 'user B continuation', true);",
                  "    const ContinuedB = await BurnOne(ReadyB, TokenB, 'user B continuation after user A cash-out');",
                  "    const PersistedContinuationB = await Read('/gambit/sessions/active', TokenB, 'user B continued state after user A cash-out'); Check('user B remains playable and persists its continuation after user A ends', () => pm.expect(PersistedContinuationB).to.deep.equal(ContinuedB));",
                  "    const ActiveAAfterBContinuation = await Read('/gambit/sessions/active', TokenA, 'user A state after user B continuation'); Check('user B continuation cannot recreate or alter user A ended session', () => pm.expect(ActiveAAfterBContinuation).to.eql(null));",
                  "    const RegistryAAfterBContinuation = await Read('/sessions/me/active', TokenA, 'user A registry after user B continuation'); Check('user B continuation leaves user A registry released', () => pm.expect(RegistryAAfterBContinuation).to.eql(null));",
                  "    const BalanceAAfterBContinuation = await Read('/user/chips', TokenA, 'user A balance after user B continuation'); Check('user B continuation leaves user A post-cash-out balance unchanged', () => pm.expect(BalanceAAfterBContinuation).to.deep.equal(BalanceAAfterCashOut));",
                  "    const CompletionB = await FinishAndCashOut(TokenB); Check('user B remaining session is finished and cashed out through the API', () => pm.expect(CompletionB.CashOut.code).to.eql(201));",
                  "    const ActiveBAfterCleanup = await Read('/gambit/sessions/active', TokenB, 'user B session after cleanup cash-out'); Check('user B cleanup leaves no active Gambit session', () => pm.expect(ActiveBAfterCleanup).to.eql(null));",
                  "  } catch (Error) { FlowError = Error; } finally {",
                  "    const CleanupErrors = [];",
                  "    for (const [Token, Label] of [[TokenA, 'user A'], [TokenB, 'user B']]) { try { const Deactivation = await CleanupUser(Token, Label); pm.test('TC-019 - ' + Label + ' is deactivated through API-only cleanup', () => pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' })); } catch (CleanupError) { CleanupErrors.push(CleanupError); } }",
                  "    if (CleanupErrors.length) pm.test('TC-019 - API-only final cleanup completes for both users', () => { throw CleanupErrors[0]; });",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((Error) => { pm.test('TC-019 - simultaneous Gambit isolation and cleanup flow completes', () => { throw Error; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc019TokenB}}"
              }
            ],
            "url": "{{baseUrl}}/gambit/sessions/active"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-020 - Two clients of the same user share the active Slot session state",
      "item": [
        {
          "name": "TC-020 - Two authenticated clients share the same active Slot session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-020 - ' + Name, function () {",
                  "    try { Assertion(); } catch (Error) { Failure = Error; throw Error; }",
                  "  });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a structurally valid Slot state', () => {",
                  "    pm.expect(Session).to.be.an('object');",
                  "    pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Session.StartedAt).to.be.a('string').and.not.empty;",
                  "    pm.expect(Session.EndedAt).to.eql(null);",
                  "    pm.expect(Session.CurrentRewardSnapshot).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4);",
                  "    Session.CurrentSpinResult.Reels.forEach((Reel, Index) => { pm.expect(Reel.ReelIndex).to.eql(Index); pm.expect(Reel.SymbolId).to.be.a('string').and.not.empty; });",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Max).to.be.a('number').and.above(0);",
                  "    pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.DeletedAt).to.eql(null);",
                  "  });",
                  "}",
                  "function AssertReroll(Before, Response, SelectedReel, ExpectedBalance, Label) {",
                  "  Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201));",
                  "  const After = Response.json().session;",
                  "  AssertSessionShape(After, Label + ' result');",
                  "  Check(Label + ' changes only deterministic reroll invariants', () => {",
                  "    pm.expect(After.SlotSessionId).to.eql(Before.SlotSessionId); pm.expect(After.UserId).to.eql(Before.UserId); pm.expect(After.SlotMachineId).to.eql(Before.SlotMachineId);",
                  "    for (const Field of ['Status', 'StartedAt', 'EndedAt', 'DeletedAt']) pm.expect(After[Field]).to.eql(Before[Field]);",
                  "    pm.expect(After.CurrentRerollsSpent.Rerolls.Max).to.eql(Before.CurrentRerollsSpent.Rerolls.Max);",
                  "    pm.expect(After.CurrentRerollsSpent.Rerolls.Used).to.eql(Before.CurrentRerollsSpent.Rerolls.Used + 1);",
                  "    Before.CurrentSpinResult.Reels.forEach((BeforeReel) => { const AfterReel = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex); pm.expect(AfterReel).to.exist; if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel); });",
                  "    pm.expect(After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel).SymbolId).to.be.a('string').and.not.empty;",
                  "    pm.expect(Response.json().currentBalance).to.eql(ExpectedBalance);",
                  "  });",
                  "  return After;",
                  "}",
                  "async function CleanupUser(Token) {",
                  "  if (!Token) return;",
                  "  const Active = await CallApi('GET', '/sessions/active', undefined, Token);",
                  "  if (Active.code !== 200) throw new Error('Cleanup active-session lookup returned ' + Active.code + '.');",
                  "  if (Active.text().trim() !== '' && Active.json() !== null) { const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('Cleanup cash-out returned ' + CashOut.code + '.'); }",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error('Cleanup user deactivation returned ' + Deactivation.code + '.');",
                  "}",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc020TokenClientA', 'tc020TokenClientB', 'tc020CleanupToken', 'tc020UserId', 'tc020SnapshotA', 'tc020RegistryA', 'tc020BalanceBeforeSharedActions', 'tc020RerollCost']) pm.variables.unset(Key);",
                  "  const MachinesResponse = await CallApi('GET', '/slot/machine');",
                  "  Check('Slot Machine list is available through the API', () => { pm.expect(MachinesResponse.code).to.eql(200); pm.expect(MachinesResponse.json()).to.be.an('array'); });",
                  "  const Machine = MachinesResponse.json().find((Candidate) => Candidate.Name === 'Slot 1');",
                  "  Check('official Slot 1 fixture is available before the temporary user is registered', () => { if (!Machine) throw new Error('TC-020 depende da fixture oficial \"Slot 1\", ainda não disponível neste ambiente.'); });",
                  "  Check('official Slot 1 fixture is active and has valid numeric configuration', () => {",
                  "    pm.expect(Machine.SlotMachineId).to.be.a('number').and.above(0); pm.expect(Machine.Active).to.eql(true);",
                  "    for (const Field of ['MinimumSpinValue', 'MinimumChipsRequired', 'MinimumRerollValue']) pm.expect(typeof Machine[Field] === 'number' && Number.isFinite(Machine[Field]) && Machine[Field] >= 0).to.eql(true);",
                  "  });",
                  "  const InitialBalance = 200;",
                  "  const RequiredBalance = Math.max(Machine.MinimumChipsRequired, Machine.MinimumSpinValue + (3 * Machine.MinimumRerollValue));",
                  "  Check('initial balance covers creation, prepared state and both shared actions', () => { if (InitialBalance < RequiredBalance) throw new Error('TC-020 requires ' + RequiredBalance + ' chips but registration provides ' + InitialBalance + '.'); });",
                  "",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "  const Credentials = { Name: 'TC020 Shared Client User', Email: 'tc020.' + Suffix + '@example.test', Nickname: 'tc020' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "  const Registration = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance });",
                  "  Check('single temporary user is registered through the API with 200 chips', () => { pm.expect(Registration.code).to.eql(201); pm.expect(Registration.json().User.Email).to.eql(Credentials.Email); pm.expect(Registration.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "  const UserId = Registration.json().User.UserId; pm.variables.set('tc020UserId', UserId); pm.variables.set('tc020CleanupToken', Registration.json().Token);",
                  "",
                  "  const LoginA = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "  Check('Client A authenticates the temporary user and receives a valid JWT', () => { pm.expect(LoginA.code).to.eql(200); pm.expect(LoginA.json().User.UserId).to.eql(UserId); pm.expect(LoginA.json().Token).to.be.a('string').and.not.empty; pm.expect(LoginA.json().Token.split('.')).to.have.length(3); });",
                  "  const TokenA = LoginA.json().Token; pm.variables.set('tc020TokenClientA', TokenA); pm.variables.set('tc020CleanupToken', TokenA);",
                  "  const InitialBalanceA = await Read('/user/chips', TokenA, 'Client A initial balance');",
                  "  Check('Client A sees the persisted initial balance', () => pm.expect(InitialBalanceA.ChipBalance).to.eql(InitialBalance));",
                  "",
                  "  const Creation = await CallApi('POST', '/slot-machines/' + Machine.SlotMachineId + '/sessions', {}, TokenA);",
                  "  Check('Client A creates the single active session on official Slot 1', () => pm.expect(Creation.code).to.eql(201));",
                  "  const Created = Creation.json().session; AssertSessionShape(Created, 'created shared session');",
                  "  Check('created session belongs to the shared user and official Slot 1', () => { pm.expect(Created.UserId).to.eql(UserId); pm.expect(Created.SlotMachineId).to.eql(Machine.SlotMachineId); pm.expect(Creation.json().currentBalance).to.eql(InitialBalance - Machine.MinimumSpinValue); });",
                  "  const PreparedResponse = await CallApi('POST', '/sessions/active/reroll/0', undefined, TokenA);",
                  "  const Prepared = AssertReroll(Created, PreparedResponse, 0, InitialBalance - Machine.MinimumSpinValue - Machine.MinimumRerollValue, 'Client A prepared-state reroll');",
                  "  const PersistedPrepared = await Read('/sessions/active', TokenA, 'Client A prepared active session');",
                  "  Check('Client A prepared state is persisted before Client B login', () => pm.expect(PersistedPrepared).to.deep.equal(Prepared));",
                  "",
                  "  const LoginB = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "  Check('Client B independently authenticates the same user and receives a valid JWT', () => { pm.expect(LoginB.code).to.eql(200); pm.expect(LoginB.json().User.UserId).to.eql(UserId); pm.expect(LoginB.json().User.Email).to.eql(Credentials.Email); pm.expect(LoginB.json().Token).to.be.a('string').and.not.empty; pm.expect(LoginB.json().Token.split('.')).to.have.length(3); });",
                  "  const TokenB = LoginB.json().Token; pm.variables.set('tc020TokenClientB', TokenB);",
                  "  Check('Client A and Client B contexts represent the same UserId without requiring token inequality', () => { pm.expect(LoginA.json().User.UserId).to.eql(LoginB.json().User.UserId); pm.expect(pm.variables.get('tc020TokenClientA')).to.be.a('string').and.not.empty; pm.expect(pm.variables.get('tc020TokenClientB')).to.be.a('string').and.not.empty; });",
                  "",
                  "  const SnapshotA = await Read('/sessions/active', TokenA, 'Client A shared-session snapshot after Client B login');",
                  "  const RegistryA = await Read('/sessions/me/active', TokenA, 'Client A shared registry');",
                  "  const BalanceA = await Read('/user/chips', TokenA, 'Client A shared balance before synchronized actions');",
                  "  Check('Client A snapshot remains the single prepared session', () => { pm.expect(SnapshotA).to.deep.equal(Prepared); pm.expect(RegistryA.UserId).to.eql(UserId); pm.expect(RegistryA.GameType).to.eql('Slot'); pm.expect(RegistryA.ReferenceId).to.eql(Prepared.SlotSessionId); pm.expect(BalanceA.ChipBalance).to.eql(PreparedResponse.json().currentBalance); });",
                  "  pm.variables.set('tc020SnapshotA', JSON.stringify(SnapshotA)); pm.variables.set('tc020RegistryA', JSON.stringify(RegistryA)); pm.variables.set('tc020BalanceBeforeSharedActions', BalanceA.ChipBalance); pm.variables.set('tc020RerollCost', Machine.MinimumRerollValue);",
                  "})().catch(async (Error) => {",
                  "  const CleanupToken = pm.variables.get('tc020CleanupToken');",
                  "  if (CleanupToken) { try { await CleanupUser(CleanupToken); pm.test('TC-020 - API cleanup after preparation failure deactivates the temporary user', () => pm.expect(true).to.eql(true)); } catch (CleanupError) { pm.test('TC-020 - API cleanup after preparation failure completes', () => { throw CleanupError; }); } }",
                  "  pm.test('TC-020 - preparation completes', () => { throw Error; }); pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-020 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a structurally valid Slot state', () => {",
                  "    pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty; pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0); pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Session.StartedAt).to.be.a('string').and.not.empty; pm.expect(Session.EndedAt).to.eql(null); pm.expect(Session.DeletedAt).to.eql(null); pm.expect(Session.CurrentRewardSnapshot).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4); pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0);",
                  "  });",
                  "}",
                  "function AssertReroll(Before, Response, SelectedReel, ExpectedBalance, Label) {",
                  "  Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201)); const After = Response.json().session; AssertSessionShape(After, Label + ' result');",
                  "  Check(Label + ' changes deterministic reroll invariants', () => {",
                  "    pm.expect(After.SlotSessionId).to.eql(Before.SlotSessionId); pm.expect(After.UserId).to.eql(Before.UserId); pm.expect(After.SlotMachineId).to.eql(Before.SlotMachineId); pm.expect(After.CurrentRerollsSpent.Rerolls.Used).to.eql(Before.CurrentRerollsSpent.Rerolls.Used + 1);",
                  "    for (const Field of ['Status', 'StartedAt', 'EndedAt', 'DeletedAt']) pm.expect(After[Field]).to.eql(Before[Field]); pm.expect(After.CurrentRerollsSpent.Rerolls.Max).to.eql(Before.CurrentRerollsSpent.Rerolls.Max);",
                  "    Before.CurrentSpinResult.Reels.forEach((BeforeReel) => { const AfterReel = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex); pm.expect(AfterReel).to.exist; if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel); });",
                  "    pm.expect(After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel).SymbolId).to.be.a('string').and.not.empty; pm.expect(Response.json().currentBalance).to.eql(ExpectedBalance);",
                  "  }); return After;",
                  "}",
                  "async function CleanupUser(Token) {",
                  "  const Active = await CallApi('GET', '/sessions/active', undefined, Token); if (Active.code !== 200) throw new Error('Cleanup active-session lookup returned ' + Active.code + '.');",
                  "  if (Active.text().trim() !== '' && Active.json() !== null) { const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('Cleanup cash-out returned ' + CashOut.code + '.'); }",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token); if (Deactivation.code !== 200) throw new Error('Cleanup user deactivation returned ' + Deactivation.code + '.'); return Deactivation;",
                  "}",
                  "",
                  "(async function () {",
                  "  const TokenA = pm.variables.get('tc020TokenClientA'); const TokenB = pm.variables.get('tc020TokenClientB'); const UserId = pm.variables.get('tc020UserId');",
                  "  const SnapshotA = JSON.parse(pm.variables.get('tc020SnapshotA')); const RegistryInitial = JSON.parse(pm.variables.get('tc020RegistryA')); const InitialSharedBalance = Number(pm.variables.get('tc020BalanceBeforeSharedActions')); const RerollCost = Number(pm.variables.get('tc020RerollCost'));",
                  "  let FlowError;",
                  "  try {",
                  "    Check('main request uses the independently stored Client B context', () => { pm.expect(TokenA).to.be.a('string').and.not.empty; pm.expect(TokenB).to.be.a('string').and.not.empty; pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + TokenB); });",
                  "    Check('Client B retrieves the active session successfully', () => pm.response.to.have.status(200));",
                  "    const SnapshotB = pm.response.json(); AssertSessionShape(SnapshotB, 'Client B initial shared-session view');",
                  "    Check('both clients recover exactly the same persisted Slot session', () => { pm.expect(SnapshotB).to.deep.equal(SnapshotA); pm.expect(SnapshotB.UserId).to.eql(UserId); pm.expect(SnapshotB.SlotSessionId).to.eql(SnapshotA.SlotSessionId); pm.expect(SnapshotB.SlotMachineId).to.eql(SnapshotA.SlotMachineId); });",
                  "    Check('both clients initially share reward, reels, rerolls and lifecycle state', () => { pm.expect(SnapshotB.CurrentRewardSnapshot).to.eql(SnapshotA.CurrentRewardSnapshot); pm.expect(SnapshotB.CurrentSpinResult.Reels).to.deep.equal(SnapshotA.CurrentSpinResult.Reels); pm.expect(SnapshotB.CurrentRerollsSpent).to.deep.equal(SnapshotA.CurrentRerollsSpent); for (const Field of ['Status', 'StartedAt', 'EndedAt', 'DeletedAt']) pm.expect(SnapshotB[Field]).to.eql(SnapshotA[Field]); });",
                  "    const RegistryBInitial = await Read('/sessions/me/active', TokenB, 'Client B initial registry'); Check('both clients see the same single registry entry', () => { pm.expect(RegistryBInitial).to.deep.equal(RegistryInitial); pm.expect(RegistryBInitial.ReferenceId).to.eql(SnapshotA.SlotSessionId); });",
                  "    const BalanceBInitial = await Read('/user/chips', TokenB, 'Client B initial shared balance'); Check('both clients see the same balance before shared actions', () => pm.expect(BalanceBInitial.ChipBalance).to.eql(InitialSharedBalance));",
                  "",
                  "    const ActionAResponse = await CallApi('POST', '/sessions/active/reroll/1', undefined, TokenA);",
                  "    const StateAfterA = AssertReroll(SnapshotA, ActionAResponse, 1, InitialSharedBalance - RerollCost, 'Client A shared-state reroll');",
                  "    const PersistedThroughA = await Read('/sessions/active', TokenA, 'Client A state after its action'); Check('Client A action is persisted in the single active session', () => pm.expect(PersistedThroughA).to.deep.equal(StateAfterA));",
                  "    const ObservedThroughB = await Read('/sessions/active', TokenB, 'Client B refreshed state after Client A action');",
                  "    Check('Client B refresh observes the new state persisted by Client A', () => { pm.expect(ObservedThroughB).to.deep.equal(PersistedThroughA); pm.expect(ObservedThroughB).not.to.deep.equal(SnapshotB); pm.expect(ObservedThroughB.CurrentRerollsSpent.Rerolls.Used).to.eql(SnapshotB.CurrentRerollsSpent.Rerolls.Used + 1); });",
                  "    const BalanceAAfterA = await Read('/user/chips', TokenA, 'Client A balance after Client A action'); const BalanceBAfterA = await Read('/user/chips', TokenB, 'Client B balance after Client A action');",
                  "    Check('both clients see the same balance debit produced by Client A', () => { pm.expect(BalanceAAfterA.ChipBalance).to.eql(InitialSharedBalance - RerollCost); pm.expect(BalanceBAfterA).to.deep.equal(BalanceAAfterA); });",
                  "",
                  "    const SnapshotABeforeB = await Read('/sessions/active', TokenA, 'Client A snapshot before Client B action'); Check('Client A snapshot matches the state observed through Client B', () => pm.expect(SnapshotABeforeB).to.deep.equal(ObservedThroughB));",
                  "    const ActionBResponse = await CallApi('POST', '/sessions/active/reroll/2', undefined, TokenB);",
                  "    const StateAfterB = AssertReroll(SnapshotABeforeB, ActionBResponse, 2, BalanceAAfterA.ChipBalance - RerollCost, 'Client B shared-state reroll');",
                  "    const PersistedThroughB = await Read('/sessions/active', TokenB, 'Client B state after its action'); Check('Client B action is persisted in the single active session', () => pm.expect(PersistedThroughB).to.deep.equal(StateAfterB));",
                  "    const ObservedThroughA = await Read('/sessions/active', TokenA, 'Client A refreshed state after Client B action');",
                  "    Check('Client A refresh observes the new state persisted by Client B', () => { pm.expect(ObservedThroughA).to.deep.equal(PersistedThroughB); pm.expect(ObservedThroughA).not.to.deep.equal(SnapshotABeforeB); pm.expect(ObservedThroughA.CurrentRerollsSpent.Rerolls.Used).to.eql(SnapshotABeforeB.CurrentRerollsSpent.Rerolls.Used + 1); });",
                  "    const BalanceAAfterB = await Read('/user/chips', TokenA, 'Client A balance after Client B action'); const BalanceBAfterB = await Read('/user/chips', TokenB, 'Client B balance after Client B action');",
                  "    Check('both clients see the same balance debit produced by Client B', () => { pm.expect(BalanceAAfterB.ChipBalance).to.eql(BalanceAAfterA.ChipBalance - RerollCost); pm.expect(BalanceBAfterB).to.deep.equal(BalanceAAfterB); });",
                  "    const RegistryAAfter = await Read('/sessions/me/active', TokenA, 'Client A registry after both actions'); const RegistryBAfter = await Read('/sessions/me/active', TokenB, 'Client B registry after both actions');",
                  "    Check('both clients still reference one and the same active session', () => { pm.expect(RegistryAAfter).to.deep.equal(RegistryInitial); pm.expect(RegistryBAfter).to.deep.equal(RegistryInitial); pm.expect(RegistryAAfter.ReferenceId).to.eql(ObservedThroughA.SlotSessionId); pm.expect(RegistryBAfter.ReferenceId).to.eql(ObservedThroughA.SlotSessionId); });",
                  "  } catch (Error) { FlowError = Error; } finally {",
                  "    try { const Deactivation = await CleanupUser(TokenA); pm.test('TC-020 - shared session is cashed out and temporary user is deactivated through API-only cleanup', () => pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' })); } catch (CleanupError) { pm.test('TC-020 - API-only final cleanup completes', () => { throw CleanupError; }); }",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((Error) => { pm.test('TC-020 - bidirectional shared-state and cleanup flow completes', () => { throw Error; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc020TokenClientB}}"
              }
            ],
            "url": "{{baseUrl}}/sessions/active"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-021 - Two clients of the same user share the active Gambit session state",
      "item": [
        {
          "name": "TC-021 - Two authenticated clients share the same active Gambit session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-021 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function SessionView(Payload) { if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload; const View = Object.assign({}, Payload); delete View.PeekResult; return View; }",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a complete valid Gambit view', () => {",
                  "    pm.expect(Session).to.be.an('object'); pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0); pm.expect(Session.GambitTableId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount)); pm.expect(Session.AccumulatedPoints).to.be.a('number'); pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true);",
                  "    pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']); pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true); pm.expect(Session.Grid).to.be.an('object'); pm.expect(Session.Grid.Revealed).to.be.an('array'); pm.expect(Session.Grid.Unrevealed).to.be.an('array'); pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25);",
                  "    const Positions = []; Session.Grid.Revealed.forEach((Card) => { pm.expect(Card.Position).to.be.a('number').and.within(0, 24); pm.expect(Card.Points).to.be.a('number'); pm.expect(Card.Effect === null || typeof Card.Effect === 'string').to.eql(true); pm.expect(Card.Locked).to.be.a('boolean'); Positions.push(Card.Position); });",
                  "    Session.Grid.Unrevealed.forEach((Card) => { pm.expect(Card.Position).to.be.a('number').and.within(0, 24); pm.expect(Card.Locked).to.be.a('boolean'); pm.expect(Card).not.to.have.property('Points'); pm.expect(Card).not.to.have.property('Effect'); Positions.push(Card.Position); });",
                  "    pm.expect(new Set(Positions).size).to.eql(25); pm.expect(Session.Grid).to.have.property('PendingEvent'); pm.expect(Session.Grid).to.have.property('PendingInteraction');",
                  "  });",
                  "}",
                  "async function ResolveBlockingState(Session, Token) {",
                  "  let Current = SessionView(Session); for (let Steps = 0; Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent); Steps += 1) {",
                  "    if (Steps >= 20) throw new Error('TC-021 cleanup: pending Gambit state did not converge after 20 API resolutions.'); let Response;",
                  "    if (Current.Grid.PendingInteraction) { const Required = Current.Grid.PendingInteraction.RequiredSelections; const Candidates = Current.Grid.Unrevealed.filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0).filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index); if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error('TC-021 cleanup: pending interaction has no valid selection payload.'); Response = await CallApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: Candidates.slice(0, Required).map((Card) => Card.Position) }, Token); }",
                  "    else { const Pending = Current.Grid.PendingEvent; const GoodIndex = Array.isArray(Pending.GoodOptions) ? Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; const BadIndex = Array.isArray(Pending.BadOptions) ? Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error('TC-021 cleanup: pending event has no valid option payload.'); Response = await CallApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: GoodIndex, BadIndex: BadIndex }, Token); }",
                  "    if (Response.code !== 201) throw new Error('TC-021 cleanup pending-state endpoint returned ' + Response.code + '.'); Current = SessionView(Response.json());",
                  "  } return Current;",
                  "}",
                  "async function FinishAndCashOut(Token) {",
                  "  const Lookup = await CallApi('GET', '/gambit/sessions/active', undefined, Token); if (Lookup.code !== 200) throw new Error('TC-021 cleanup lookup returned ' + Lookup.code + '.'); let Current = Lookup.text().trim() === '' ? null : SessionView(Lookup.json());",
                  "  for (let Steps = 0; Current && Steps < 100; Steps += 1) {",
                  "    if (Current.Status === 'Finished') { const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('TC-021 cleanup cash-out returned ' + CashOut.code + '.'); return; }",
                  "    if (Current.Status !== 'InProgress') return; if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) { Current = await ResolveBlockingState(Current, Token); continue; }",
                  "    const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false); if (!Target) throw new Error('TC-021 cleanup found no burnable card.'); const Burned = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); if (Burned.code !== 201) throw new Error('TC-021 cleanup burn returned ' + Burned.code + '.'); Current = SessionView(Burned.json());",
                  "  } if (Current) throw new Error('TC-021 cleanup did not finish within 100 API actions.');",
                  "}",
                  "async function CleanupUser(Token) { if (!Token) return; await FinishAndCashOut(Token); const Deactivation = await CallApi('DELETE', '/user', undefined, Token); if (Deactivation.code !== 200) throw new Error('TC-021 cleanup user deactivation returned ' + Deactivation.code + '.'); }",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc021TokenClientA', 'tc021TokenClientB', 'tc021CleanupToken', 'tc021UserId', 'tc021SnapshotA', 'tc021RegistryA', 'tc021Balance', 'tc021SessionCost']) pm.variables.unset(Key);",
                  "  const TablesResponse = await CallApi('GET', '/gambit-table'); Check('Gambit Table list is available through the API', () => { pm.expect(TablesResponse.code).to.eql(200); pm.expect(TablesResponse.json()).to.be.an('array'); });",
                  "  const Table = TablesResponse.json().find((Candidate) => Candidate.Name === 'Gambit 1'); Check('official Gambit 1 fixture is available before the temporary user is registered', () => { if (!Table) throw new Error('TC-021 depende da fixture oficial \"Gambit 1\", ainda não disponível neste ambiente.'); });",
                  "  Check('official Gambit 1 fixture is active and has valid numeric configuration', () => { pm.expect(Table.GambitTableId).to.be.a('number').and.above(0); pm.expect(Table.Active).to.eql(true); for (const Field of ['MinimumChipsRequired', 'CardPrice', 'TableMultiplier', 'MinimumCardsPurchased', 'MaxCardsPurchased']) pm.expect(typeof Table[Field] === 'number' && Number.isFinite(Table[Field]) && Table[Field] >= 0).to.eql(true); pm.expect(Table.MinimumCardsPurchased).to.be.at.least(1); pm.expect(Table.MaxCardsPurchased).to.be.at.least(Table.MinimumCardsPurchased); });",
                  "  const InitialBalance = 200; const CardsPurchased = Table.MinimumCardsPurchased; const SessionCost = CardsPurchased * Table.CardPrice; const RequiredBalance = Math.max(Table.MinimumChipsRequired, SessionCost);",
                  "  Check('initial balance covers Gambit 1 access and the minimum card purchase', () => { if (InitialBalance < RequiredBalance) throw new Error('TC-021 requires ' + RequiredBalance + ' chips but registration provides ' + InitialBalance + '.'); });",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, ''); const Credentials = { Name: 'TC021 Shared Gambit User', Email: 'tc021.' + Suffix + '@example.test', Nickname: 'tc021' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "  const Registration = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance });",
                  "  Check('single temporary user is registered through the API with 200 chips', () => { pm.expect(Registration.code).to.eql(201); pm.expect(Registration.json().User.Email).to.eql(Credentials.Email); pm.expect(Registration.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "  const UserId = Registration.json().User.UserId; pm.variables.set('tc021UserId', UserId); pm.variables.set('tc021CleanupToken', Registration.json().Token);",
                  "  const LoginA = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password }); Check('Client A authenticates the temporary user and receives a valid JWT', () => { pm.expect(LoginA.code).to.eql(200); pm.expect(LoginA.json().User.UserId).to.eql(UserId); pm.expect(LoginA.json().Token).to.be.a('string').and.not.empty; pm.expect(LoginA.json().Token.split('.')).to.have.length(3); });",
                  "  const TokenA = LoginA.json().Token; pm.variables.set('tc021TokenClientA', TokenA); pm.variables.set('tc021CleanupToken', TokenA);",
                  "  const InitialChips = await Read('/user/chips', TokenA, 'Client A initial balance'); Check('Client A sees the persisted initial balance', () => pm.expect(InitialChips.ChipBalance).to.eql(InitialBalance));",
                  "  const Creation = await CallApi('POST', '/gambit-tables/' + Table.GambitTableId + '/sessions', { CardsPurchased: CardsPurchased }, TokenA); Check('Client A creates the single active session on official Gambit 1', () => pm.expect(Creation.code).to.eql(201));",
                  "  const Created = SessionView(Creation.json().session); AssertSessionShape(Created, 'created shared session'); Check('created session has the expected identity, card quantity and single debit', () => { pm.expect(Created.UserId).to.eql(UserId); pm.expect(Created.GambitTableId).to.eql(Table.GambitTableId); pm.expect(Created.CardsPurchased).to.eql(CardsPurchased); pm.expect(Created.Status).to.eql('InProgress'); pm.expect(Creation.json().currentBalance).to.eql(InitialBalance - SessionCost); });",
                  "  const RegistryA = await Read('/sessions/me/active', TokenA, 'Client A shared registry'); Check('Client A registry references the single Gambit session', () => { pm.expect(RegistryA.UserId).to.eql(UserId); pm.expect(RegistryA.GameType).to.eql('Gambit'); pm.expect(RegistryA.ReferenceId).to.eql(Created.GambitSessionId); });",
                  "  const LoginB = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password }); Check('Client B independently authenticates the same user and receives a valid JWT', () => { pm.expect(LoginB.code).to.eql(200); pm.expect(LoginB.json().User.UserId).to.eql(UserId); pm.expect(LoginB.json().User.Email).to.eql(Credentials.Email); pm.expect(LoginB.json().Token).to.be.a('string').and.not.empty; pm.expect(LoginB.json().Token.split('.')).to.have.length(3); });",
                  "  const TokenB = LoginB.json().Token; pm.variables.set('tc021TokenClientB', TokenB); Check('Client A and Client B contexts represent the same UserId without requiring token inequality', () => { pm.expect(LoginA.json().User.UserId).to.eql(LoginB.json().User.UserId); pm.expect(pm.variables.get('tc021TokenClientA')).to.be.a('string').and.not.empty; pm.expect(pm.variables.get('tc021TokenClientB')).to.be.a('string').and.not.empty; });",
                  "  const SnapshotA = await Read('/gambit/sessions/active', TokenA, 'Client A initial shared-session snapshot'); const BalanceA = await Read('/user/chips', TokenA, 'Client A shared balance after creation');",
                  "  Check('Client A snapshot remains the single created session with one purchase debit', () => { pm.expect(SnapshotA).to.deep.equal(Created); pm.expect(BalanceA.ChipBalance).to.eql(InitialBalance - SessionCost); });",
                  "  pm.variables.set('tc021SnapshotA', JSON.stringify(SnapshotA)); pm.variables.set('tc021RegistryA', JSON.stringify(RegistryA)); pm.variables.set('tc021Balance', BalanceA.ChipBalance); pm.variables.set('tc021SessionCost', SessionCost);",
                  "})().catch(async (Error) => {",
                  "  const CleanupToken = pm.variables.get('tc021CleanupToken'); if (CleanupToken) { try { await CleanupUser(CleanupToken); pm.test('TC-021 - API cleanup after preparation failure deactivates the temporary user', () => pm.expect(true).to.eql(true)); } catch (CleanupError) { pm.test('TC-021 - API cleanup after preparation failure completes', () => { throw CleanupError; }); } }",
                  "  pm.test('TC-021 - preparation completes', () => { throw Error; }); pm.execution.skipRequest();",
                  "});"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token; const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-021 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function SessionView(Payload) { if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload; const View = Object.assign({}, Payload); delete View.PeekResult; return View; }",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a complete valid Gambit view', () => {",
                  "    pm.expect(Session).to.be.an('object'); pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0); pm.expect(Session.GambitTableId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty;",
                  "    for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0); pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount));",
                  "    pm.expect(Session.AccumulatedPoints).to.be.a('number'); pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true); pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']); pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true);",
                  "    pm.expect(Session.Grid).to.be.an('object'); pm.expect(Session.Grid.Revealed).to.be.an('array'); pm.expect(Session.Grid.Unrevealed).to.be.an('array'); pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25); pm.expect(Session.Grid).to.have.property('PendingEvent'); pm.expect(Session.Grid).to.have.property('PendingInteraction');",
                  "  });",
                  "}",
                  "async function ResolveBlockingState(Session, Token, Label, RecordAssertions) {",
                  "  let Current = SessionView(Session); let Steps = 0; while (Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent)) {",
                  "    Steps += 1; if (Steps > 20) throw new Error(Label + ': pending Gambit state did not converge after 20 API resolutions.'); let Response;",
                  "    if (Current.Grid.PendingInteraction) { const Required = Current.Grid.PendingInteraction.RequiredSelections; const Candidates = Current.Grid.Unrevealed.filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0).filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index); if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error(Label + ': pending interaction cannot be resolved with valid distinct unrevealed positions.'); Response = await CallApi('POST', '/gambit/sessions/active/resolve-effect', { Positions: Candidates.slice(0, Required).map((Card) => Card.Position) }, Token); }",
                  "    else { const Pending = Current.Grid.PendingEvent; const GoodIndex = Array.isArray(Pending.GoodOptions) ? Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; const BadIndex = Array.isArray(Pending.BadOptions) ? Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error(Label + ': pending event does not expose valid selectable options.'); Response = await CallApi('POST', '/gambit/sessions/active/resolve-event', { GoodIndex: GoodIndex, BadIndex: BadIndex }, Token); }",
                  "    if (RecordAssertions) Check(Label + ' resolves pending state through Client B #' + Steps, () => pm.expect(Response.code).to.eql(201)); else if (Response.code !== 201) throw new Error(Label + ': pending-state endpoint returned ' + Response.code + '.'); Current = SessionView(Response.json());",
                  "  } return Current;",
                  "}",
                  "async function BurnOne(Session, Token, Label) {",
                  "  Check(Label + ' starts without a blocking state', () => { pm.expect(Session.Status).to.eql('InProgress'); pm.expect(Session.Grid.PendingEvent).to.eql(null); pm.expect(Session.Grid.PendingInteraction).to.eql(null); }); const Target = Session.Grid.Unrevealed.find((Card) => Card.Locked === false); Check(Label + ' has a valid card available to burn', () => pm.expect(Target).to.exist);",
                  "  const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); Check(Label + ' burn succeeds through the real API', () => pm.expect(Response.code).to.eql(201)); const Burned = SessionView(Response.json()); AssertSessionShape(Burned, Label + ' result');",
                  "  const RevealedCard = Burned.Grid.Revealed.find((Card) => Card.Position === Target.Position); const BoardSize = Session.Grid.Revealed.length + Session.Grid.Unrevealed.length; let ExpectedBurnSlots = Session.BurnSlotsAvailable; if (Session.NextEffect !== 'ANULACAO_TOTAL' && RevealedCard) { if (RevealedCard.Effect === 'QUANTO_MAIS_MELHOR') ExpectedBurnSlots = Math.min(BoardSize, Session.BurnSlotsAvailable + 1); else if (RevealedCard.Effect === 'QUANTO_MENOS_MELHOR') ExpectedBurnSlots = Math.max(Burned.ManualFlipsCount, Session.BurnSlotsAvailable - 1); }",
                  "  Check(Label + ' changes deterministic burn invariants', () => { pm.expect(Burned.GambitSessionId).to.eql(Session.GambitSessionId); pm.expect(Burned.UserId).to.eql(Session.UserId); pm.expect(Burned.GambitTableId).to.eql(Session.GambitTableId); pm.expect(Burned.CardsPurchased).to.eql(Session.CardsPurchased); pm.expect(RevealedCard).to.exist; pm.expect(Burned.BurnSlotsAvailable).to.eql(ExpectedBurnSlots); pm.expect(Burned.ManualFlipsCount).to.eql(Session.ManualFlipsCount + 1); pm.expect(Burned.BurnsRemaining).to.eql(Math.max(0, Burned.BurnSlotsAvailable - Burned.ManualFlipsCount)); pm.expect(Burned.Grid.Revealed.length).to.eql(Session.Grid.Revealed.length + 1); pm.expect(Burned.Grid.Unrevealed.length).to.eql(Session.Grid.Unrevealed.length - 1); pm.expect(RevealedCard.Position).to.eql(Target.Position); }); return Burned;",
                  "}",
                  "async function FinishAndCashOut(Token) {",
                  "  const Lookup = await CallApi('GET', '/gambit/sessions/active', undefined, Token); if (Lookup.code !== 200) throw new Error('TC-021 cleanup lookup returned ' + Lookup.code + '.'); let Current = Lookup.text().trim() === '' ? null : SessionView(Lookup.json());",
                  "  for (let Steps = 0; Current && Steps < 100; Steps += 1) { if (Current.Status === 'Finished') { const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('TC-021 cleanup cash-out returned ' + CashOut.code + '.'); return CashOut; } if (Current.Status !== 'InProgress') return null; if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) { Current = await ResolveBlockingState(Current, Token, 'cleanup', false); continue; } const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false); if (!Target) throw new Error('TC-021 cleanup found no burnable card.'); const Burned = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); if (Burned.code !== 201) throw new Error('TC-021 cleanup burn returned ' + Burned.code + '.'); Current = SessionView(Burned.json()); }",
                  "  if (Current) throw new Error('TC-021 cleanup did not finish within 100 API actions.'); return null;",
                  "}",
                  "",
                  "(async function () {",
                  "  const TokenA = pm.variables.get('tc021TokenClientA'); const TokenB = pm.variables.get('tc021TokenClientB'); const UserId = pm.variables.get('tc021UserId'); const SnapshotA = JSON.parse(pm.variables.get('tc021SnapshotA')); const RegistryInitial = JSON.parse(pm.variables.get('tc021RegistryA')); const SharedBalance = Number(pm.variables.get('tc021Balance'));",
                  "  let FlowError; try {",
                  "    Check('main request uses the independently stored Client B context', () => { pm.expect(TokenA).to.be.a('string').and.not.empty; pm.expect(TokenB).to.be.a('string').and.not.empty; pm.expect(pm.variables.replaceIn(pm.request.headers.get('Authorization'))).to.eql('Bearer ' + TokenB); }); Check('Client B retrieves the active Gambit session successfully', () => pm.response.to.have.status(200));",
                  "    const SnapshotB = pm.response.json(); AssertSessionShape(SnapshotB, 'Client B initial shared-session view'); Check('both clients initially recover the same complete Gambit state', () => { pm.expect(SnapshotB).to.deep.equal(SnapshotA); pm.expect(SnapshotB.UserId).to.eql(UserId); pm.expect(SnapshotB.GambitSessionId).to.eql(SnapshotA.GambitSessionId); pm.expect(SnapshotB.GambitTableId).to.eql(SnapshotA.GambitTableId); });",
                  "    Check('both clients initially share counters, points, result and the complete Grid', () => { for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining', 'AccumulatedPoints', 'NextEffect', 'Status', 'Result']) pm.expect(SnapshotB[Field]).to.deep.equal(SnapshotA[Field]); pm.expect(SnapshotB.Grid).to.deep.equal(SnapshotA.Grid); });",
                  "    const RegistryBInitial = await Read('/sessions/me/active', TokenB, 'Client B initial registry'); Check('both clients see the same single Gambit registry entry', () => { pm.expect(RegistryBInitial).to.deep.equal(RegistryInitial); pm.expect(RegistryBInitial.UserId).to.eql(UserId); pm.expect(RegistryBInitial.GameType).to.eql('Gambit'); pm.expect(RegistryBInitial.ReferenceId).to.eql(SnapshotA.GambitSessionId); });",
                  "    const BalanceBInitial = await Read('/user/chips', TokenB, 'Client B initial shared balance'); Check('both clients see the same balance after one session purchase', () => pm.expect(BalanceBInitial.ChipBalance).to.eql(SharedBalance));",
                  "",
                  "    const ActionA = await BurnOne(SnapshotA, TokenA, 'Client A shared-state action'); const PersistedThroughA = await Read('/gambit/sessions/active', TokenA, 'Client A state after its action'); Check('Client A action including pending state is persisted', () => pm.expect(PersistedThroughA).to.deep.equal(ActionA));",
                  "    const ObservedThroughB = await Read('/gambit/sessions/active', TokenB, 'Client B refreshed state after Client A action'); Check('Client B observes the exact new state including pending event or interaction produced by Client A', () => { pm.expect(ObservedThroughB).to.deep.equal(PersistedThroughA); pm.expect(ObservedThroughB.GambitSessionId).to.eql(SnapshotB.GambitSessionId); pm.expect(ObservedThroughB.ManualFlipsCount).to.eql(SnapshotB.ManualFlipsCount + 1); pm.expect(ObservedThroughB.Grid.PendingEvent).to.deep.equal(PersistedThroughA.Grid.PendingEvent); pm.expect(ObservedThroughB.Grid.PendingInteraction).to.deep.equal(PersistedThroughA.Grid.PendingInteraction); pm.expect(ObservedThroughB.NextEffect).to.deep.equal(PersistedThroughA.NextEffect); });",
                  "    const RegistryBAfterA = await Read('/sessions/me/active', TokenB, 'Client B registry after Client A action'); Check('Client A action preserves the single shared registry', () => pm.expect(RegistryBAfterA).to.deep.equal(RegistryInitial));",
                  "    const BalanceAAfterA = await Read('/user/chips', TokenA, 'Client A balance after its burn'); const BalanceBAfterA = await Read('/user/chips', TokenB, 'Client B balance after Client A burn'); Check('both clients see that burn has no additional chip cost', () => { pm.expect(BalanceAAfterA.ChipBalance).to.eql(SharedBalance); pm.expect(BalanceBAfterA).to.deep.equal(BalanceAAfterA); });",
                  "",
                  "    const HadPendingFromA = Boolean(ObservedThroughB.Grid.PendingEvent || ObservedThroughB.Grid.PendingInteraction); const ResolvedByB = await ResolveBlockingState(ObservedThroughB, TokenB, 'Client B pending-state continuation', true);",
                  "    const PersistedResolutionThroughB = await Read('/gambit/sessions/active', TokenB, 'Client B state after pending-state handling'); Check('Client B pending-state handling is persisted when applicable', () => pm.expect(PersistedResolutionThroughB).to.deep.equal(ResolvedByB));",
                  "    const ObservedResolutionThroughA = await Read('/gambit/sessions/active', TokenA, 'Client A refreshed state after Client B pending-state handling'); Check('Client A observes Client B resolution or the unchanged non-pending state', () => { pm.expect(ObservedResolutionThroughA).to.deep.equal(PersistedResolutionThroughB); pm.expect(ObservedResolutionThroughA.GambitSessionId).to.eql(SnapshotA.GambitSessionId); if (HadPendingFromA) { pm.expect(ObservedResolutionThroughA).not.to.deep.equal(ObservedThroughB); pm.expect(ObservedResolutionThroughA.Grid.PendingEvent).to.eql(null); pm.expect(ObservedResolutionThroughA.Grid.PendingInteraction).to.eql(null); } else pm.expect(ObservedResolutionThroughA).to.deep.equal(ObservedThroughB); });",
                  "    const BalanceAAfterResolution = await Read('/user/chips', TokenA, 'Client A balance after Client B pending-state handling'); const BalanceBAfterResolution = await Read('/user/chips', TokenB, 'Client B balance after pending-state handling'); Check('both clients see that pending-state resolution has no additional chip cost', () => { pm.expect(BalanceAAfterResolution.ChipBalance).to.eql(SharedBalance); pm.expect(BalanceBAfterResolution).to.deep.equal(BalanceAAfterResolution); });",
                  "",
                  "    const ActionB = await BurnOne(ObservedResolutionThroughA, TokenB, 'Client B shared-state action'); const PersistedThroughB = await Read('/gambit/sessions/active', TokenB, 'Client B state after its action'); Check('Client B action including any new pending state is persisted', () => pm.expect(PersistedThroughB).to.deep.equal(ActionB));",
                  "    const ObservedThroughA = await Read('/gambit/sessions/active', TokenA, 'Client A refreshed state after Client B action'); Check('Client A observes the exact new state persisted by Client B', () => { pm.expect(ObservedThroughA).to.deep.equal(PersistedThroughB); pm.expect(ObservedThroughA.GambitSessionId).to.eql(SnapshotA.GambitSessionId); pm.expect(ObservedThroughA.ManualFlipsCount).to.eql(ObservedResolutionThroughA.ManualFlipsCount + 1); pm.expect(ObservedThroughA.Grid.PendingEvent).to.deep.equal(PersistedThroughB.Grid.PendingEvent); pm.expect(ObservedThroughA.Grid.PendingInteraction).to.deep.equal(PersistedThroughB.Grid.PendingInteraction); });",
                  "    const BalanceAAfterB = await Read('/user/chips', TokenA, 'Client A balance after Client B burn'); const BalanceBAfterB = await Read('/user/chips', TokenB, 'Client B balance after its burn'); Check('both clients retain one shared balance after Client B action', () => { pm.expect(BalanceAAfterB.ChipBalance).to.eql(SharedBalance); pm.expect(BalanceBAfterB).to.deep.equal(BalanceAAfterB); });",
                  "    const RegistryAAfter = await Read('/sessions/me/active', TokenA, 'Client A registry after both actions'); const RegistryBAfter = await Read('/sessions/me/active', TokenB, 'Client B registry after both actions'); Check('both clients still reference one and the same active Gambit session', () => { pm.expect(RegistryAAfter).to.deep.equal(RegistryInitial); pm.expect(RegistryBAfter).to.deep.equal(RegistryInitial); pm.expect(RegistryAAfter.ReferenceId).to.eql(ObservedThroughA.GambitSessionId); pm.expect(RegistryBAfter.ReferenceId).to.eql(ObservedThroughA.GambitSessionId); });",
                  "  } catch (Error) { FlowError = Error; } finally {",
                  "    try { const CashOut = await FinishAndCashOut(TokenA); pm.test('TC-021 - shared Gambit session is finished and cashed out through API-only cleanup', () => pm.expect(CashOut === null || CashOut.code === 201).to.eql(true)); const Deactivation = await CallApi('DELETE', '/user', undefined, TokenA); pm.test('TC-021 - temporary user is deactivated through the authenticated API', () => { pm.expect(Deactivation.code).to.eql(200); pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' }); }); } catch (CleanupError) { pm.test('TC-021 - API-only final cleanup completes', () => { throw CleanupError; }); }",
                  "  } if (FlowError) throw FlowError;",
                  "})().catch((Error) => { pm.test('TC-021 - bidirectional shared Gambit state and cleanup flow completes', () => { throw Error; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{tc021TokenClientB}}"
              }
            ],
            "url": "{{baseUrl}}/gambit/sessions/active"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-022 - Failed reauthentication does not affect the active Slot session",
      "item": [
        {
          "name": "TC-022 - Invalid reauthentication preserves the active Slot session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token; const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response)));",
                  "}",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-022 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function AssertSessionShape(Session, Label) {",
                  "  Check(Label + ' exposes a structurally valid Slot state', () => {",
                  "    pm.expect(Session).to.be.an('object'); pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty; pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0); pm.expect(Session.Status).to.eql('InProgress');",
                  "    pm.expect(Session.StartedAt).to.be.a('string').and.not.empty; pm.expect(Session.EndedAt).to.eql(null); if (Object.prototype.hasOwnProperty.call(Session, 'LastInteractionAt')) pm.expect(Session.LastInteractionAt).to.be.a('string').and.not.empty; pm.expect(Session.DeletedAt).to.eql(null); pm.expect(Session.CurrentRewardSnapshot).to.be.a('number').and.at.least(0);",
                  "    pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4); Session.CurrentSpinResult.Reels.forEach((Reel, Index) => { pm.expect(Reel.ReelIndex).to.eql(Index); pm.expect(Reel.SymbolId).to.be.a('string').and.not.empty; }); pm.expect(Session.CurrentRerollsSpent.Rerolls.Max).to.be.a('number').and.above(0); pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0);",
                  "  });",
                  "}",
                  "function AssertReroll(Before, Response, SelectedReel, ExpectedBalance, Label) {",
                  "  Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201)); const After = Response.json().session; AssertSessionShape(After, Label + ' result');",
                  "  Check(Label + ' changes only deterministic reroll invariants', () => { pm.expect(After.SlotSessionId).to.eql(Before.SlotSessionId); pm.expect(After.UserId).to.eql(Before.UserId); pm.expect(After.SlotMachineId).to.eql(Before.SlotMachineId); for (const Field of ['Status', 'StartedAt', 'EndedAt', 'DeletedAt']) pm.expect(After[Field]).to.eql(Before[Field]); pm.expect(After.CurrentRerollsSpent.Rerolls.Max).to.eql(Before.CurrentRerollsSpent.Rerolls.Max); pm.expect(After.CurrentRerollsSpent.Rerolls.Used).to.eql(Before.CurrentRerollsSpent.Rerolls.Used + 1); Before.CurrentSpinResult.Reels.forEach((BeforeReel) => { const AfterReel = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex); pm.expect(AfterReel).to.exist; if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel); }); pm.expect(After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel).SymbolId).to.be.a('string').and.not.empty; pm.expect(Response.json().currentBalance).to.eql(ExpectedBalance); }); return After;",
                  "}",
                  "async function CleanupUser(Token) { if (!Token) return; const Active = await CallApi('GET', '/sessions/active', undefined, Token); if (Active.code !== 200) throw new Error('TC-022 cleanup active-session lookup returned ' + Active.code + '.'); if (Active.text().trim() !== '' && Active.json() !== null) { const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('TC-022 cleanup cash-out returned ' + CashOut.code + '.'); } const Deactivation = await CallApi('DELETE', '/user', undefined, Token); if (Deactivation.code !== 200) throw new Error('TC-022 cleanup user deactivation returned ' + Deactivation.code + '.'); }",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc022Email', 'tc022CorrectPassword', 'tc022WrongPassword', 'tc022ValidToken', 'tc022CleanupToken', 'tc022UserId', 'tc022Snapshot', 'tc022Registry', 'tc022Balance', 'tc022RerollCost']) pm.variables.unset(Key);",
                  "  const MachinesResponse = await CallApi('GET', '/slot/machine'); Check('Slot Machine list is available through the API', () => { pm.expect(MachinesResponse.code).to.eql(200); pm.expect(MachinesResponse.json()).to.be.an('array'); });",
                  "  const Machine = MachinesResponse.json().find((Candidate) => Candidate.Name === 'Slot 1'); Check('official Slot 1 fixture is available before the temporary user is registered', () => { if (!Machine) throw new Error('TC-022 depende da fixture oficial \"Slot 1\", ainda não disponível neste ambiente.'); });",
                  "  Check('official Slot 1 fixture is active and has valid numeric configuration', () => { pm.expect(Machine.SlotMachineId).to.be.a('number').and.above(0); pm.expect(Machine.Active).to.eql(true); for (const Field of ['MinimumSpinValue', 'MinimumChipsRequired', 'MinimumRerollValue']) pm.expect(typeof Machine[Field] === 'number' && Number.isFinite(Machine[Field]) && Machine[Field] >= 0).to.eql(true); });",
                  "  const InitialBalance = 200; const RequiredBalance = Math.max(Machine.MinimumChipsRequired, Machine.MinimumSpinValue + (2 * Machine.MinimumRerollValue)); Check('initial balance covers creation and both rerolls', () => { if (InitialBalance < RequiredBalance) throw new Error('TC-022 requires ' + RequiredBalance + ' chips but registration provides ' + InitialBalance + '.'); });",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, ''); const CorrectPassword = pm.variables.replaceIn('{{$guid}}') + 'Aa1!'; const WrongPassword = 'Wrong-' + pm.variables.replaceIn('{{$guid}}') + 'Bb2!'; const Credentials = { Name: 'TC022 Invalid Reauth User', Email: 'tc022.' + Suffix + '@example.test', Nickname: 'tc022' + Suffix, Password: CorrectPassword };",
                  "  Check('incorrect password is deliberately distinct from the valid password', () => pm.expect(WrongPassword).not.to.eql(CorrectPassword));",
                  "  const Registration = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance }); Check('temporary user is registered through the API with 200 chips', () => { pm.expect(Registration.code).to.eql(201); pm.expect(Registration.json().User.Email).to.eql(Credentials.Email); pm.expect(Registration.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "  const UserId = Registration.json().User.UserId; pm.variables.set('tc022CleanupToken', Registration.json().Token);",
                  "  const Login = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: CorrectPassword }); Check('initial login succeeds and returns the original valid JWT', () => { pm.expect(Login.code).to.eql(200); pm.expect(Login.json().User.UserId).to.eql(UserId); pm.expect(Login.json().User.Email).to.eql(Credentials.Email); pm.expect(Login.json().Token).to.be.a('string').and.not.empty; pm.expect(Login.json().Token.split('.')).to.have.length(3); });",
                  "  const ValidToken = Login.json().Token; pm.variables.set('tc022CleanupToken', ValidToken); const InitialChips = await Read('/user/chips', ValidToken, 'initial balance through the valid token'); Check('valid context sees the persisted initial balance', () => pm.expect(InitialChips.ChipBalance).to.eql(InitialBalance));",
                  "  const Creation = await CallApi('POST', '/slot-machines/' + Machine.SlotMachineId + '/sessions', {}, ValidToken); Check('real session is created on official Slot 1', () => pm.expect(Creation.code).to.eql(201)); const Created = Creation.json().session; AssertSessionShape(Created, 'created session'); Check('created session belongs to the temporary user and official Slot 1', () => { pm.expect(Created.UserId).to.eql(UserId); pm.expect(Created.SlotMachineId).to.eql(Machine.SlotMachineId); pm.expect(Creation.json().currentBalance).to.eql(InitialBalance - Machine.MinimumSpinValue); });",
                  "  const PreparedResponse = await CallApi('POST', '/sessions/active/reroll/0', undefined, ValidToken); const Prepared = AssertReroll(Created, PreparedResponse, 0, InitialBalance - Machine.MinimumSpinValue - Machine.MinimumRerollValue, 'pre-failure state-producing reroll');",
                  "  const Snapshot = await Read('/sessions/active', ValidToken, 'active session before invalid reauthentication'); Check('non-initial state is persisted before invalid reauthentication', () => { pm.expect(Snapshot).to.deep.equal(Prepared); pm.expect(Snapshot.CurrentRerollsSpent.Rerolls.Used).to.eql(Created.CurrentRerollsSpent.Rerolls.Used + 1); });",
                  "  const Registry = await Read('/sessions/me/active', ValidToken, 'registry before invalid reauthentication'); Check('registry before invalid reauthentication references the prepared Slot session', () => { pm.expect(Registry.UserId).to.eql(UserId); pm.expect(Registry.GameType).to.eql('Slot'); pm.expect(Registry.ReferenceId).to.eql(Snapshot.SlotSessionId); });",
                  "  const Balance = await Read('/user/chips', ValidToken, 'balance before invalid reauthentication'); Check('balance before invalid reauthentication contains only legitimate Slot costs', () => pm.expect(Balance.ChipBalance).to.eql(PreparedResponse.json().currentBalance));",
                  "  pm.variables.set('tc022Email', Credentials.Email); pm.variables.set('tc022CorrectPassword', CorrectPassword); pm.variables.set('tc022WrongPassword', WrongPassword); pm.variables.set('tc022ValidToken', ValidToken); pm.variables.set('tc022UserId', UserId); pm.variables.set('tc022Snapshot', JSON.stringify(Snapshot)); pm.variables.set('tc022Registry', JSON.stringify(Registry)); pm.variables.set('tc022Balance', Balance.ChipBalance); pm.variables.set('tc022RerollCost', Machine.MinimumRerollValue);",
                  "})().catch(async (Error) => { const CleanupToken = pm.variables.get('tc022CleanupToken'); if (CleanupToken) { try { await CleanupUser(CleanupToken); pm.test('TC-022 - API cleanup after preparation failure deactivates the temporary user', () => pm.expect(true).to.eql(true)); } catch (CleanupError) { pm.test('TC-022 - API cleanup after preparation failure completes', () => { throw CleanupError; }); } } pm.test('TC-022 - preparation completes', () => { throw Error; }); pm.execution.skipRequest(); });"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) { const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token; const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) }; return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response))); }",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-022 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function AssertSessionShape(Session, Label) { Check(Label + ' exposes a structurally valid Slot state', () => { pm.expect(Session).to.be.an('object'); pm.expect(Session.SlotSessionId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty; pm.expect(Session.SlotMachineId).to.be.a('number').and.above(0); pm.expect(Session.Status).to.eql('InProgress'); pm.expect(Session.StartedAt).to.be.a('string').and.not.empty; pm.expect(Session.EndedAt).to.eql(null); if (Object.prototype.hasOwnProperty.call(Session, 'LastInteractionAt')) pm.expect(Session.LastInteractionAt).to.be.a('string').and.not.empty; pm.expect(Session.DeletedAt).to.eql(null); pm.expect(Session.CurrentRewardSnapshot).to.be.a('number').and.at.least(0); pm.expect(Session.CurrentSpinResult.Reels).to.be.an('array').with.length(4); Session.CurrentSpinResult.Reels.forEach((Reel, Index) => { pm.expect(Reel.ReelIndex).to.eql(Index); pm.expect(Reel.SymbolId).to.be.a('string').and.not.empty; }); pm.expect(Session.CurrentRerollsSpent.Rerolls.Max).to.be.a('number').and.above(0); pm.expect(Session.CurrentRerollsSpent.Rerolls.Used).to.be.a('number').and.at.least(0); }); }",
                  "function AssertReroll(Before, Response, SelectedReel, ExpectedBalance, Label) { Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201)); const After = Response.json().session; AssertSessionShape(After, Label + ' result'); Check(Label + ' changes only deterministic reroll invariants', () => { pm.expect(After.SlotSessionId).to.eql(Before.SlotSessionId); pm.expect(After.UserId).to.eql(Before.UserId); pm.expect(After.SlotMachineId).to.eql(Before.SlotMachineId); for (const Field of ['Status', 'StartedAt', 'EndedAt', 'DeletedAt']) pm.expect(After[Field]).to.eql(Before[Field]); pm.expect(After.CurrentRerollsSpent.Rerolls.Max).to.eql(Before.CurrentRerollsSpent.Rerolls.Max); pm.expect(After.CurrentRerollsSpent.Rerolls.Used).to.eql(Before.CurrentRerollsSpent.Rerolls.Used + 1); Before.CurrentSpinResult.Reels.forEach((BeforeReel) => { const AfterReel = After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === BeforeReel.ReelIndex); pm.expect(AfterReel).to.exist; if (BeforeReel.ReelIndex !== SelectedReel) pm.expect(AfterReel).to.deep.equal(BeforeReel); }); pm.expect(After.CurrentSpinResult.Reels.find((Reel) => Reel.ReelIndex === SelectedReel).SymbolId).to.be.a('string').and.not.empty; pm.expect(Response.json().currentBalance).to.eql(ExpectedBalance); }); return After; }",
                  "async function CleanupUser(Token) { const Active = await CallApi('GET', '/sessions/active', undefined, Token); if (Active.code !== 200) throw new Error('TC-022 cleanup active-session lookup returned ' + Active.code + '.'); if (Active.text().trim() !== '' && Active.json() !== null) { const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('TC-022 cleanup cash-out returned ' + CashOut.code + '.'); } const Deactivation = await CallApi('DELETE', '/user', undefined, Token); if (Deactivation.code !== 200) throw new Error('TC-022 cleanup user deactivation returned ' + Deactivation.code + '.'); return Deactivation; }",
                  "",
                  "(async function () {",
                  "  const ValidToken = pm.variables.get('tc022ValidToken'); const UserId = pm.variables.get('tc022UserId'); const Snapshot = JSON.parse(pm.variables.get('tc022Snapshot')); const RegistryBefore = JSON.parse(pm.variables.get('tc022Registry')); const BalanceBefore = Number(pm.variables.get('tc022Balance')); const RerollCost = Number(pm.variables.get('tc022RerollCost')); let FlowError;",
                  "  try {",
                  "    const AttemptedCredentials = JSON.parse(pm.variables.replaceIn(pm.request.body.raw)); Check('invalid reauthentication sends the correct email with a deliberately incorrect password', () => { pm.expect(AttemptedCredentials.Email).to.eql(pm.variables.get('tc022Email')); pm.expect(AttemptedCredentials.Password).to.eql(pm.variables.get('tc022WrongPassword')); pm.expect(AttemptedCredentials.Password).not.to.eql(pm.variables.get('tc022CorrectPassword')); });",
                  "    Check('invalid reauthentication is rejected with the implemented authentication contract', () => { pm.response.to.have.status(401); const Error = pm.response.json(); pm.expect(Error).to.deep.include({ message: 'Invalid credentials', error: 'Unauthorized', statusCode: 401 }); });",
                  "    Check('rejected reauthentication does not return a valid authenticated context', () => { const Error = pm.response.json(); pm.expect(Error).not.to.have.property('Token'); pm.expect(Error).not.to.have.property('User'); });",
                  "    Check('original valid token remains available for all protected checks', () => { pm.expect(ValidToken).to.be.a('string').and.not.empty; pm.expect(ValidToken.split('.')).to.have.length(3); });",
                  "    const SessionAfterFailure = await Read('/sessions/active', ValidToken, 'session through the original token after invalid reauthentication'); AssertSessionShape(SessionAfterFailure, 'session preserved after invalid reauthentication');",
                  "    Check('invalid reauthentication leaves the complete persisted Slot state unchanged', () => { pm.expect(SessionAfterFailure).to.deep.equal(Snapshot); pm.expect(SessionAfterFailure.SlotSessionId).to.eql(Snapshot.SlotSessionId); pm.expect(SessionAfterFailure.UserId).to.eql(UserId); pm.expect(SessionAfterFailure.SlotMachineId).to.eql(Snapshot.SlotMachineId); pm.expect(SessionAfterFailure.Status).to.eql('InProgress'); pm.expect(SessionAfterFailure.CurrentRewardSnapshot).to.eql(Snapshot.CurrentRewardSnapshot); pm.expect(SessionAfterFailure.CurrentSpinResult.Reels).to.deep.equal(Snapshot.CurrentSpinResult.Reels); pm.expect(SessionAfterFailure.CurrentRerollsSpent).to.deep.equal(Snapshot.CurrentRerollsSpent); for (const Field of ['StartedAt', 'EndedAt', 'LastInteractionAt', 'DeletedAt']) if (Object.prototype.hasOwnProperty.call(Snapshot, Field)) pm.expect(SessionAfterFailure[Field]).to.deep.equal(Snapshot[Field]); });",
                  "    const RegistryAfterFailure = await Read('/sessions/me/active', ValidToken, 'registry through the original token after invalid reauthentication'); Check('invalid reauthentication leaves the active-session registry unchanged', () => { pm.expect(RegistryAfterFailure).to.deep.equal(RegistryBefore); pm.expect(RegistryAfterFailure.UserId).to.eql(UserId); pm.expect(RegistryAfterFailure.GameType).to.eql('Slot'); pm.expect(RegistryAfterFailure.ReferenceId).to.eql(Snapshot.SlotSessionId); });",
                  "    const BalanceAfterFailure = await Read('/user/chips', ValidToken, 'balance through the original token after invalid reauthentication'); Check('invalid reauthentication leaves the chip balance unchanged', () => pm.expect(BalanceAfterFailure.ChipBalance).to.eql(BalanceBefore));",
                  "    const ContinuationResponse = await CallApi('POST', '/sessions/active/reroll/1', undefined, ValidToken); const Continued = AssertReroll(SessionAfterFailure, ContinuationResponse, 1, BalanceBefore - RerollCost, 'continuation through the original valid token');",
                  "    const PersistedContinuation = await Read('/sessions/active', ValidToken, 'continued session persisted state'); Check('new action remains persisted in the same original session', () => { pm.expect(PersistedContinuation).to.deep.equal(Continued); pm.expect(PersistedContinuation.SlotSessionId).to.eql(Snapshot.SlotSessionId); pm.expect(PersistedContinuation.CurrentRerollsSpent.Rerolls.Used).to.eql(Snapshot.CurrentRerollsSpent.Rerolls.Used + 1); });",
                  "    const BalanceAfterContinuation = await Read('/user/chips', ValidToken, 'balance after valid continuation'); Check('valid continuation alone debits the configured reroll cost', () => pm.expect(BalanceAfterContinuation.ChipBalance).to.eql(BalanceBefore - RerollCost));",
                  "    const RegistryAfterContinuation = await Read('/sessions/me/active', ValidToken, 'registry after valid continuation'); Check('valid continuation keeps the registry tied to the same session', () => pm.expect(RegistryAfterContinuation).to.deep.equal(RegistryBefore));",
                  "  } catch (Error) { FlowError = Error; } finally { try { const Deactivation = await CleanupUser(ValidToken); pm.test('TC-022 - active session is cashed out and temporary user is deactivated through API-only cleanup', () => pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' })); } catch (CleanupError) { pm.test('TC-022 - API-only final cleanup completes', () => { throw CleanupError; }); } } if (FlowError) throw FlowError;",
                  "})().catch((Error) => { pm.test('TC-022 - invalid reauthentication isolation and cleanup flow completes', () => { throw Error; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"Email\": \"{{tc022Email}}\",\n  \"Password\": \"{{tc022WrongPassword}}\"\n}"
            },
            "url": "{{baseUrl}}/auth/login"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-023 - Invalid token action does not affect the active Gambit session",
      "item": [
        {
          "name": "TC-023 - Invalid JWT action preserves and allows recovery of Gambit session",
          "event": [
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) { const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token; const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) }; return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response))); }",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-023 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function SessionView(Payload) { if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload; const View = Object.assign({}, Payload); delete View.PeekResult; return View; }",
                  "function AssertSessionShape(Session, Label) { Check(Label + ' exposes a complete valid Gambit view', () => { pm.expect(Session).to.be.an('object'); pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0); pm.expect(Session.GambitTableId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty; for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0); pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount)); pm.expect(Session.AccumulatedPoints).to.be.a('number'); pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true); pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']); pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true); pm.expect(Session.Grid).to.be.an('object'); pm.expect(Session.Grid.Revealed).to.be.an('array'); pm.expect(Session.Grid.Unrevealed).to.be.an('array'); pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25); pm.expect(Session.Grid).to.have.property('PendingEvent'); pm.expect(Session.Grid).to.have.property('PendingInteraction'); }); }",
                  "function ValidPendingAction(Session) {",
                  "  if (Session.Grid.PendingInteraction) { const Required = Session.Grid.PendingInteraction.RequiredSelections; const Candidates = Session.Grid.Unrevealed.filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0).filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index); if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error('TC-023 cannot build a valid ResolveEffectDto from the pending interaction.'); return { Kind: 'resolve-effect', Path: '/gambit/sessions/active/resolve-effect', Body: { Positions: Candidates.slice(0, Required).map((Card) => Card.Position) } }; }",
                  "  if (Session.Grid.PendingEvent) { const Pending = Session.Grid.PendingEvent; const GoodIndex = Array.isArray(Pending.GoodOptions) ? Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; const BadIndex = Array.isArray(Pending.BadOptions) ? Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error('TC-023 cannot build a valid ResolveEventDto from the pending event.'); return { Kind: 'resolve-event', Path: '/gambit/sessions/active/resolve-event', Body: { GoodIndex: GoodIndex, BadIndex: BadIndex } }; }",
                  "  const Target = Session.Grid.Unrevealed.find((Card) => Card.Locked === false); if (!Target) throw new Error('TC-023 cannot find a valid burn position for the protected action.'); return { Kind: 'burn', Path: '/gambit/sessions/active/burn/' + Target.Position, Body: {} };",
                  "}",
                  "async function ResolveBlockingState(Session, Token) { let Current = SessionView(Session); for (let Steps = 0; Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent); Steps += 1) { if (Steps >= 20) throw new Error('TC-023 cleanup pending state did not converge.'); const Action = ValidPendingAction(Current); const Response = await CallApi('POST', Action.Path, Action.Body, Token); if (Response.code !== 201) throw new Error('TC-023 cleanup pending-state endpoint returned ' + Response.code + '.'); Current = SessionView(Response.json()); } return Current; }",
                  "async function FinishAndCashOut(Token) { const Lookup = await CallApi('GET', '/gambit/sessions/active', undefined, Token); if (Lookup.code !== 200) throw new Error('TC-023 cleanup lookup returned ' + Lookup.code + '.'); let Current = Lookup.text().trim() === '' ? null : SessionView(Lookup.json()); for (let Steps = 0; Current && Steps < 100; Steps += 1) { if (Current.Status === 'Finished') { const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('TC-023 cleanup cash-out returned ' + CashOut.code + '.'); return; } if (Current.Status !== 'InProgress') return; if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) { Current = await ResolveBlockingState(Current, Token); continue; } const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false); if (!Target) throw new Error('TC-023 cleanup found no burnable card.'); const Burned = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); if (Burned.code !== 201) throw new Error('TC-023 cleanup burn returned ' + Burned.code + '.'); Current = SessionView(Burned.json()); } if (Current) throw new Error('TC-023 cleanup did not finish within 100 API actions.'); }",
                  "async function CleanupUser(Token) { if (!Token) return; await FinishAndCashOut(Token); const Deactivation = await CallApi('DELETE', '/user', undefined, Token); if (Deactivation.code !== 200) throw new Error('TC-023 cleanup user deactivation returned ' + Deactivation.code + '.'); }",
                  "",
                  "(async function () {",
                  "  for (const Key of ['tc023Email', 'tc023Password', 'tc023ValidToken', 'tc023InvalidToken', 'tc023RecoveredToken', 'tc023CleanupToken', 'tc023UserId', 'tc023Snapshot', 'tc023Registry', 'tc023Balance', 'tc023AttackPath', 'tc023AttackBody', 'tc023AttackKind']) pm.variables.unset(Key);",
                  "  const TablesResponse = await CallApi('GET', '/gambit-table'); Check('Gambit Table list is available through the API', () => { pm.expect(TablesResponse.code).to.eql(200); pm.expect(TablesResponse.json()).to.be.an('array'); }); const Table = TablesResponse.json().find((Candidate) => Candidate.Name === 'Gambit 1'); Check('official Gambit 1 fixture is available before the temporary user is registered', () => { if (!Table) throw new Error('TC-023 depende da fixture oficial \"Gambit 1\", ainda não disponível neste ambiente.'); });",
                  "  Check('official Gambit 1 fixture is active and has valid numeric configuration', () => { pm.expect(Table.GambitTableId).to.be.a('number').and.above(0); pm.expect(Table.Active).to.eql(true); for (const Field of ['MinimumChipsRequired', 'CardPrice', 'TableMultiplier', 'MinimumCardsPurchased', 'MaxCardsPurchased']) pm.expect(typeof Table[Field] === 'number' && Number.isFinite(Table[Field]) && Table[Field] >= 0).to.eql(true); pm.expect(Table.MinimumCardsPurchased).to.be.at.least(1); pm.expect(Table.MaxCardsPurchased).to.be.at.least(Table.MinimumCardsPurchased); });",
                  "  const InitialBalance = 200; const CardsPurchased = Table.MinimumCardsPurchased; const SessionCost = CardsPurchased * Table.CardPrice; const RequiredBalance = Math.max(Table.MinimumChipsRequired, SessionCost); Check('initial balance covers Gambit 1 access and minimum card purchase', () => { if (InitialBalance < RequiredBalance) throw new Error('TC-023 requires ' + RequiredBalance + ' chips but registration provides ' + InitialBalance + '.'); });",
                  "  const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, ''); const Credentials = { Name: 'TC023 Invalid JWT User', Email: 'tc023.' + Suffix + '@example.test', Nickname: 'tc023' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' }; const Registration = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance }); Check('temporary user is created through the API with 200 chips', () => { pm.expect(Registration.code).to.eql(201); pm.expect(Registration.json().User.Email).to.eql(Credentials.Email); pm.expect(Registration.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "  const UserId = Registration.json().User.UserId; pm.variables.set('tc023CleanupToken', Registration.json().Token); const Login = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password }); Check('initial valid login authenticates the temporary user', () => { pm.expect(Login.code).to.eql(200); pm.expect(Login.json().User.UserId).to.eql(UserId); pm.expect(Login.json().Token).to.be.a('string').and.not.empty; pm.expect(Login.json().Token.split('.')).to.have.length(3); }); const ValidToken = Login.json().Token; pm.variables.set('tc023CleanupToken', ValidToken);",
                  "  const InitialChips = await Read('/user/chips', ValidToken, 'initial balance through the valid token'); Check('valid user identity starts with the persisted 200-chip balance', () => pm.expect(InitialChips.ChipBalance).to.eql(InitialBalance));",
                  "  const Creation = await CallApi('POST', '/gambit-tables/' + Table.GambitTableId + '/sessions', { CardsPurchased: CardsPurchased }, ValidToken); Check('real session is created on official Gambit 1', () => pm.expect(Creation.code).to.eql(201)); const Created = SessionView(Creation.json().session); AssertSessionShape(Created, 'created session'); Check('created session has the expected identity, card quantity and dynamic debit', () => { pm.expect(Created.UserId).to.eql(UserId); pm.expect(Created.GambitTableId).to.eql(Table.GambitTableId); pm.expect(Created.CardsPurchased).to.eql(CardsPurchased); pm.expect(Created.Status).to.eql('InProgress'); pm.expect(Creation.json().currentBalance).to.eql(InitialBalance - SessionCost); });",
                  "  const Registry = await Read('/sessions/me/active', ValidToken, 'registry before the invalid-token attempt'); Check('registry references the created Gambit session', () => { pm.expect(Registry.UserId).to.eql(UserId); pm.expect(Registry.GameType).to.eql('Gambit'); pm.expect(Registry.ReferenceId).to.eql(Created.GambitSessionId); });",
                  "  const FirstTarget = Created.Grid.Unrevealed.find((Card) => Card.Locked === false); Check('valid preparation action uses an available unlocked board position', () => pm.expect(FirstTarget).to.exist); const FirstBurnResponse = await CallApi('POST', '/gambit/sessions/active/burn/' + FirstTarget.Position, undefined, ValidToken); Check('valid preparation burn succeeds through the real API', () => pm.expect(FirstBurnResponse.code).to.eql(201)); const FirstBurn = SessionView(FirstBurnResponse.json()); AssertSessionShape(FirstBurn, 'non-initial session'); Check('valid preparation burn produces deterministic non-initial state', () => { pm.expect(FirstBurn.GambitSessionId).to.eql(Created.GambitSessionId); pm.expect(FirstBurn.ManualFlipsCount).to.eql(Created.ManualFlipsCount + 1); pm.expect(FirstBurn.Grid.Revealed.length).to.eql(Created.Grid.Revealed.length + 1); pm.expect(FirstBurn.Grid.Unrevealed.length).to.eql(Created.Grid.Unrevealed.length - 1); pm.expect(FirstBurn.Grid.Revealed.some((Card) => Card.Position === FirstTarget.Position)).to.eql(true); });",
                  "  const Snapshot = await Read('/gambit/sessions/active', ValidToken, 'complete session snapshot before invalid-token action'); Check('complete non-initial state including pending data is persisted before the attack', () => pm.expect(Snapshot).to.deep.equal(FirstBurn)); const Balance = await Read('/user/chips', ValidToken, 'balance before invalid-token action'); Check('pre-attack balance contains only the session purchase debit', () => pm.expect(Balance.ChipBalance).to.eql(InitialBalance - SessionCost));",
                  "  const Attack = ValidPendingAction(Snapshot); Check('protected attack is a mutable action valid for the current Gambit state', () => { pm.expect(Attack.Kind).to.be.oneOf(['burn', 'resolve-event', 'resolve-effect']); pm.expect(Attack.Path.startsWith('/gambit/sessions/active/')).to.eql(true); pm.expect(Attack.Body).to.be.an('object'); });",
                  "  const Parts = ValidToken.split('.'); const Replacement = Parts[2].charAt(0) === 'A' ? 'B' : 'A'; const InvalidToken = Parts[0] + '.' + Parts[1] + '.' + Replacement + Parts[2].slice(1); Check('tampered JWT preserves Bearer structure but differs from the valid token', () => { pm.expect(InvalidToken.split('.')).to.have.length(3); pm.expect(InvalidToken).not.to.eql(ValidToken); pm.expect(InvalidToken.split('.')[0]).to.eql(Parts[0]); pm.expect(InvalidToken.split('.')[1]).to.eql(Parts[1]); });",
                  "  pm.variables.set('tc023Email', Credentials.Email); pm.variables.set('tc023Password', Credentials.Password); pm.variables.set('tc023ValidToken', ValidToken); pm.variables.set('tc023InvalidToken', InvalidToken); pm.variables.set('tc023UserId', UserId); pm.variables.set('tc023Snapshot', JSON.stringify(Snapshot)); pm.variables.set('tc023Registry', JSON.stringify(Registry)); pm.variables.set('tc023Balance', Balance.ChipBalance); pm.variables.set('tc023AttackPath', Attack.Path); pm.variables.set('tc023AttackBody', JSON.stringify(Attack.Body)); pm.variables.set('tc023AttackKind', Attack.Kind);",
                  "})().catch(async (Error) => { const CleanupToken = pm.variables.get('tc023CleanupToken'); if (CleanupToken) { try { await CleanupUser(CleanupToken); pm.test('TC-023 - API cleanup after preparation failure deactivates the temporary user', () => pm.expect(true).to.eql(true)); } catch (CleanupError) { pm.test('TC-023 - API cleanup after preparation failure completes', () => { throw CleanupError; }); } } pm.test('TC-023 - preparation completes', () => { throw Error; }); pm.execution.skipRequest(); });"
                ]
              }
            },
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) { const Header = { 'Content-Type': 'application/json' }; if (Token) Header.Authorization = 'Bearer ' + Token; const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header }; if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) }; return new Promise((Resolve, Reject) => pm.sendRequest(Request, (Error, Response) => Error ? Reject(Error) : Resolve(Response))); }",
                  "function Check(Name, Assertion) { let Failure; pm.test('TC-023 - ' + Name, function () { try { Assertion(); } catch (Error) { Failure = Error; throw Error; } }); if (Failure) throw Failure; }",
                  "async function Read(Path, Token, Label) { const Response = await CallApi('GET', Path, undefined, Token); Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200)); return Response.text().trim() === '' ? null : Response.json(); }",
                  "function SessionView(Payload) { if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload; const View = Object.assign({}, Payload); delete View.PeekResult; return View; }",
                  "function AssertSessionShape(Session, Label) { Check(Label + ' exposes a complete valid Gambit view', () => { pm.expect(Session).to.be.an('object'); pm.expect(Session.GambitSessionId).to.be.a('number').and.above(0); pm.expect(Session.GambitTableId).to.be.a('number').and.above(0); pm.expect(Session.UserId).to.be.a('string').and.not.empty; for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining']) pm.expect(Session[Field]).to.be.a('number').and.at.least(0); pm.expect(Session.BurnsRemaining).to.eql(Math.max(0, Session.BurnSlotsAvailable - Session.ManualFlipsCount)); pm.expect(Session.AccumulatedPoints).to.be.a('number'); pm.expect(Session.NextEffect === null || typeof Session.NextEffect === 'string').to.eql(true); pm.expect(Session.Status).to.be.oneOf(['InProgress', 'Finished']); pm.expect(Session.Result === null || typeof Session.Result === 'number').to.eql(true); pm.expect(Session.Grid.Revealed).to.be.an('array'); pm.expect(Session.Grid.Unrevealed).to.be.an('array'); pm.expect(Session.Grid.Revealed.length + Session.Grid.Unrevealed.length).to.eql(25); pm.expect(Session.Grid).to.have.property('PendingEvent'); pm.expect(Session.Grid).to.have.property('PendingInteraction'); }); }",
                  "function ValidPendingAction(Session) { if (Session.Grid.PendingInteraction) { const Required = Session.Grid.PendingInteraction.RequiredSelections; const Candidates = Session.Grid.Unrevealed.filter((Card) => Number.isInteger(Card.Position) && Card.Position >= 0).filter((Card, Index, Cards) => Cards.findIndex((Candidate) => Candidate.Position === Card.Position) === Index); if (!Number.isInteger(Required) || Required < 1 || Required > 3 || Candidates.length < Required) throw new Error('TC-023 cannot build a valid ResolveEffectDto.'); return { Path: '/gambit/sessions/active/resolve-effect', Body: { Positions: Candidates.slice(0, Required).map((Card) => Card.Position) } }; } if (Session.Grid.PendingEvent) { const Pending = Session.Grid.PendingEvent; const GoodIndex = Array.isArray(Pending.GoodOptions) ? Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; const BadIndex = Array.isArray(Pending.BadOptions) ? Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0) : -1; if (GoodIndex < 0 || GoodIndex > 2 || BadIndex < 0 || BadIndex > 2) throw new Error('TC-023 cannot build a valid ResolveEventDto.'); return { Path: '/gambit/sessions/active/resolve-event', Body: { GoodIndex: GoodIndex, BadIndex: BadIndex } }; } return null; }",
                  "async function ResolveBlockingState(Session, Token, Label, RecordAssertions) { let Current = SessionView(Session); let Steps = 0; while (Current && Current.Status === 'InProgress' && (Current.Grid.PendingInteraction || Current.Grid.PendingEvent)) { Steps += 1; if (Steps > 20) throw new Error(Label + ': pending state did not converge.'); const Action = ValidPendingAction(Current); const Response = await CallApi('POST', Action.Path, Action.Body, Token); if (RecordAssertions) Check(Label + ' resolves pending state through the recovered token #' + Steps, () => pm.expect(Response.code).to.eql(201)); else if (Response.code !== 201) throw new Error(Label + ': pending endpoint returned ' + Response.code + '.'); Current = SessionView(Response.json()); } return Current; }",
                  "async function BurnOne(Session, Token, Label) { Check(Label + ' starts from an actionable state', () => { pm.expect(Session.Status).to.eql('InProgress'); pm.expect(Session.Grid.PendingEvent).to.eql(null); pm.expect(Session.Grid.PendingInteraction).to.eql(null); }); const Target = Session.Grid.Unrevealed.find((Card) => Card.Locked === false); Check(Label + ' selects an available unlocked position', () => pm.expect(Target).to.exist); const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); Check(Label + ' succeeds through the real API', () => pm.expect(Response.code).to.eql(201)); const Burned = SessionView(Response.json()); AssertSessionShape(Burned, Label + ' result'); Check(Label + ' changes deterministic burn invariants in the same session', () => { pm.expect(Burned.GambitSessionId).to.eql(Session.GambitSessionId); pm.expect(Burned.UserId).to.eql(Session.UserId); pm.expect(Burned.GambitTableId).to.eql(Session.GambitTableId); pm.expect(Burned.ManualFlipsCount).to.eql(Session.ManualFlipsCount + 1); pm.expect(Burned.Grid.Revealed.length).to.eql(Session.Grid.Revealed.length + 1); pm.expect(Burned.Grid.Unrevealed.length).to.eql(Session.Grid.Unrevealed.length - 1); pm.expect(Burned.Grid.Revealed.some((Card) => Card.Position === Target.Position)).to.eql(true); }); return Burned; }",
                  "async function FinishAndCashOut(Token) { const Lookup = await CallApi('GET', '/gambit/sessions/active', undefined, Token); if (Lookup.code !== 200) throw new Error('TC-023 cleanup lookup returned ' + Lookup.code + '.'); let Current = Lookup.text().trim() === '' ? null : SessionView(Lookup.json()); for (let Steps = 0; Current && Steps < 100; Steps += 1) { if (Current.Status === 'Finished') { const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token); if (CashOut.code !== 201) throw new Error('TC-023 cleanup cash-out returned ' + CashOut.code + '.'); return CashOut; } if (Current.Status !== 'InProgress') return null; if (Current.Grid.PendingInteraction || Current.Grid.PendingEvent) { Current = await ResolveBlockingState(Current, Token, 'cleanup', false); continue; } const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false); if (!Target) throw new Error('TC-023 cleanup found no burnable card.'); const Response = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token); if (Response.code !== 201) throw new Error('TC-023 cleanup burn returned ' + Response.code + '.'); Current = SessionView(Response.json()); } if (Current) throw new Error('TC-023 cleanup did not finish within 100 API actions.'); return null; }",
                  "",
                  "(async function () {",
                  "  const ValidToken = pm.variables.get('tc023ValidToken'); const InvalidToken = pm.variables.get('tc023InvalidToken'); const UserId = pm.variables.get('tc023UserId'); const Snapshot = JSON.parse(pm.variables.get('tc023Snapshot')); const RegistryBefore = JSON.parse(pm.variables.get('tc023Registry')); const BalanceBefore = Number(pm.variables.get('tc023Balance')); let CleanupToken = ValidToken; let FlowError;",
                  "  try {",
                  "    Check('mutable request uses only the tampered JWT and the prepared valid action', () => { const SentAuthorization = pm.variables.replaceIn(pm.request.headers.get('Authorization')); pm.expect(SentAuthorization).to.eql('Bearer ' + InvalidToken); pm.expect(SentAuthorization).not.to.eql('Bearer ' + ValidToken); pm.expect(pm.variables.replaceIn(pm.request.url.toString())).to.include(pm.variables.get('tc023AttackPath')); pm.expect(pm.variables.get('tc023AttackKind')).to.be.oneOf(['burn', 'resolve-event', 'resolve-effect']); });",
                  "    Check('invalid JWT action is rejected by the implemented guard contract', () => { pm.response.to.have.status(401); pm.expect(pm.response.json()).to.deep.include({ message: 'Unauthorized', statusCode: 401 }); }); Check('rejected action returns no successful Gambit state', () => { const Error = pm.response.json(); pm.expect(Error).not.to.have.property('GambitSessionId'); pm.expect(Error).not.to.have.property('Grid'); });",
                  "    const StateAfterAttack = await Read('/gambit/sessions/active', ValidToken, 'session through the original token after rejected action'); AssertSessionShape(StateAfterAttack, 'session preserved after rejected action'); Check('invalid JWT action leaves the complete persisted Gambit state deeply unchanged', () => { pm.expect(StateAfterAttack).to.deep.equal(Snapshot); pm.expect(StateAfterAttack.GambitSessionId).to.eql(Snapshot.GambitSessionId); pm.expect(StateAfterAttack.UserId).to.eql(UserId); pm.expect(StateAfterAttack.GambitTableId).to.eql(Snapshot.GambitTableId); for (const Field of ['CardsPurchased', 'BurnSlotsAvailable', 'ManualFlipsCount', 'BurnsRemaining', 'AccumulatedPoints', 'NextEffect', 'Status', 'Result']) pm.expect(StateAfterAttack[Field]).to.deep.equal(Snapshot[Field]); pm.expect(StateAfterAttack.Grid).to.deep.equal(Snapshot.Grid); });",
                  "    const RegistryAfterAttack = await Read('/sessions/me/active', ValidToken, 'registry after rejected action'); Check('invalid JWT action leaves registry deeply unchanged', () => { pm.expect(RegistryAfterAttack).to.deep.equal(RegistryBefore); pm.expect(RegistryAfterAttack.UserId).to.eql(UserId); pm.expect(RegistryAfterAttack.GameType).to.eql('Gambit'); pm.expect(RegistryAfterAttack.ReferenceId).to.eql(Snapshot.GambitSessionId); }); const BalanceAfterAttack = await Read('/user/chips', ValidToken, 'balance after rejected action'); Check('invalid JWT action leaves balance unchanged', () => pm.expect(BalanceAfterAttack.ChipBalance).to.eql(BalanceBefore));",
                  "    const RecoveredLogin = await CallApi('POST', '/auth/login', { Email: pm.variables.get('tc023Email'), Password: pm.variables.get('tc023Password') }); Check('new valid login succeeds for the original user', () => { pm.expect(RecoveredLogin.code).to.eql(200); pm.expect(RecoveredLogin.json().User.UserId).to.eql(UserId); pm.expect(RecoveredLogin.json().User.Email).to.eql(pm.variables.get('tc023Email')); pm.expect(RecoveredLogin.json().Token).to.be.a('string').and.not.empty; pm.expect(RecoveredLogin.json().Token.split('.')).to.have.length(3); }); const RecoveredToken = RecoveredLogin.json().Token; pm.variables.set('tc023RecoveredToken', RecoveredToken); CleanupToken = RecoveredToken;",
                  "    const Recovered = await Read('/gambit/sessions/active', RecoveredToken, 'session through the newly authenticated context'); AssertSessionShape(Recovered, 'session recovered with the new valid token'); Check('new authentication recovers exactly the same complete session snapshot', () => pm.expect(Recovered).to.deep.equal(Snapshot)); const RecoveredRegistry = await Read('/sessions/me/active', RecoveredToken, 'registry through the new valid token'); Check('new authentication recovers the same registry', () => pm.expect(RecoveredRegistry).to.deep.equal(RegistryBefore)); const RecoveredBalance = await Read('/user/chips', RecoveredToken, 'balance through the new valid token'); Check('new authentication recovers the same chip balance', () => pm.expect(RecoveredBalance.ChipBalance).to.eql(BalanceBefore));",
                  "    const Ready = await ResolveBlockingState(Recovered, RecoveredToken, 'recovered pending state', true); const ReadyPersisted = await Read('/gambit/sessions/active', RecoveredToken, 'state after legitimate pending-state handling'); Check('legitimate pending-state handling is persisted on the same recovered session', () => { pm.expect(ReadyPersisted).to.deep.equal(Ready); pm.expect(ReadyPersisted.GambitSessionId).to.eql(Snapshot.GambitSessionId); }); const Continued = await BurnOne(ReadyPersisted, RecoveredToken, 'continuation with the newly authenticated token'); const PersistedContinuation = await Read('/gambit/sessions/active', RecoveredToken, 'continued Gambit state'); Check('new legitimate action is persisted in the recovered session', () => { pm.expect(PersistedContinuation).to.deep.equal(Continued); pm.expect(PersistedContinuation.GambitSessionId).to.eql(Snapshot.GambitSessionId); });",
                  "    const BalanceAfterContinuation = await Read('/user/chips', RecoveredToken, 'balance after recovered continuation'); Check('Gambit resolution and burn do not debit chips unexpectedly', () => pm.expect(BalanceAfterContinuation.ChipBalance).to.eql(BalanceBefore)); const RegistryAfterContinuation = await Read('/sessions/me/active', RecoveredToken, 'registry after recovered continuation'); Check('continued play keeps the registry tied to the same Gambit session', () => pm.expect(RegistryAfterContinuation).to.deep.equal(RegistryBefore));",
                  "  } catch (Error) { FlowError = Error; } finally { try { const CashOut = await FinishAndCashOut(CleanupToken); pm.test('TC-023 - recovered Gambit session is finished and cashed out through API-only cleanup', () => pm.expect(CashOut === null || CashOut.code === 201).to.eql(true)); const Deactivation = await CallApi('DELETE', '/user', undefined, CleanupToken); pm.test('TC-023 - temporary user is deactivated through the authenticated API', () => { pm.expect(Deactivation.code).to.eql(200); pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' }); }); } catch (CleanupError) { pm.test('TC-023 - API-only final cleanup completes', () => { throw CleanupError; }); } } if (FlowError) throw FlowError;",
                  "})().catch((Error) => { pm.test('TC-023 - invalid-token isolation, valid recovery and cleanup flow completes', () => { throw Error; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{tc023InvalidToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{{tc023AttackBody}}"
            },
            "url": "{{baseUrl}}{{tc023AttackPath}}"
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-024 - Concurrent Slot cash-out rejects duplicate payout",
      "item": [
        {
          "name": "TC-024 - Concurrent Slot cash-out rejects duplicate payout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((ResolveRequest, RejectRequest) => pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? RejectRequest(ErrorObject) : ResolveRequest(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-024 - ' + Name, function () { try { Assertion(); } catch (ErrorObject) { Failure = ErrorObject; throw ErrorObject; } });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "async function ConcurrentCashOuts(Path, Token) {",
                  "  const Request = { method: 'POST', url: pm.environment.get('baseUrl') + Path, header: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + Token } };",
                  "  return Promise.all([0, 1].map(() => new Promise((ResolveRequest, RejectRequest) => {",
                  "    pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? RejectRequest(ErrorObject) : ResolveRequest(Response));",
                  "  })));",
                  "}",
                  "async function CleanupUser(Token) {",
                  "  if (!Token) return;",
                  "  const Active = await CallApi('GET', '/sessions/active', undefined, Token);",
                  "  if (Active.code !== 200) throw new Error('TC-024 cleanup active-session lookup returned ' + Active.code + '.');",
                  "  if (Active.text().trim() !== '' && Active.json() !== null) {",
                  "    const CashOut = await CallApi('POST', '/sessions/active/cash-out', undefined, Token);",
                  "    if (CashOut.code !== 201) throw new Error('TC-024 cleanup cash-out returned ' + CashOut.code + '.');",
                  "  }",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error('TC-024 cleanup user deactivation returned ' + Deactivation.code + '.');",
                  "  return Deactivation;",
                  "}",
                  "",
                  "(async function () {",
                  "  let CleanupToken;",
                  "  let FlowError;",
                  "  try {",
                  "    Check('Slot Machine list is available through the real API', () => { pm.response.to.have.status(200); pm.expect(pm.response.json()).to.be.an('array'); });",
                  "    const Machine = pm.response.json().find((Candidate) => Candidate.Name === 'Slot 1');",
                  "    Check('official Slot 1 fixture is active and usable', () => { pm.expect(Machine).to.exist; pm.expect(Machine.SlotMachineId).to.be.a('number').and.above(0); pm.expect(Machine.Active).to.eql(true); });",
                  "    const InitialBalance = Math.max(200, Machine.MinimumChipsRequired, Machine.MinimumSpinValue + 100);",
                  "    const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "    const Credentials = { Name: 'TC024 Concurrent Slot User', Email: 'tc024.' + Suffix + '@example.test', Nickname: 'tc024' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "    const Registration = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance });",
                  "    if (Registration.code === 201) CleanupToken = Registration.json().Token;",
                  "    Check('temporary user is created through the real API', () => { pm.expect(Registration.code).to.eql(201); pm.expect(Registration.json().User.Email).to.eql(Credentials.Email); pm.expect(Registration.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "    const Login = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "    Check('temporary user is authenticated through the real API', () => { pm.expect(Login.code).to.eql(200); pm.expect(Login.json().User.UserId).to.eql(Registration.json().User.UserId); pm.expect(Login.json().Token).to.be.a('string').and.not.empty; });",
                  "    const Token = Login.json().Token;",
                  "    CleanupToken = Token;",
                  "    const Creation = await CallApi('POST', '/slot-machines/' + Machine.SlotMachineId + '/sessions', undefined, Token);",
                  "    Check('real Slot session starts for the authenticated user', () => { pm.expect(Creation.code).to.eql(201); pm.expect(Creation.json().session.SlotMachineId).to.eql(Machine.SlotMachineId); pm.expect(Creation.json().session.Status).to.eql('InProgress'); });",
                  "    const Session = await Read('/sessions/active', Token, 'active Slot session before cash-out');",
                  "    const SlotSessionId = Session.SlotSessionId;",
                  "    const RewardSnapshot = Session.CurrentRewardSnapshot;",
                  "    Check('cash-out snapshot captures the original Slot session and reward', () => { pm.expect(SlotSessionId).to.be.a('number').and.above(0); pm.expect(SlotSessionId).to.eql(Creation.json().session.SlotSessionId); pm.expect(RewardSnapshot).to.be.a('number').and.at.least(0); });",
                  "    const BalanceBefore = (await Read('/user/chips', Token, 'balance immediately before concurrent cash-out')).ChipBalance;",
                  "    Check('balance before cash-out reflects the real spin debit', () => pm.expect(BalanceBefore).to.eql(InitialBalance - Machine.MinimumSpinValue));",
                  "",
                  "    const CashOutResponses = await ConcurrentCashOuts('/sessions/active/cash-out', Token);",
                  "    const Successful = CashOutResponses.filter((Response) => Response.code === 201);",
                  "    const Rejected = CashOutResponses.filter((Response) => Response.code === 400);",
                  "    Check('exactly one concurrent Slot cash-out succeeds', () => pm.expect(Successful).to.have.length(1));",
                  "    Check('duplicate concurrent Slot cash-out returns 400', () => { pm.expect(Rejected).to.have.length(1); pm.expect(Rejected[0].code).to.eql(400); });",
                  "    Check('both overlapping Slot cash-out responses are fully accounted for', () => pm.expect(Successful.length + Rejected.length).to.eql(2));",
                  "    const ExpectedFinalBalance = BalanceBefore + RewardSnapshot;",
                  "    Check('successful Slot cash-out reports a single reward credit', () => { pm.expect(Successful[0].json().message).to.eql('Cash out successful'); pm.expect(Successful[0].json().finalBalance).to.eql(ExpectedFinalBalance); });",
                  "    const FinalBalance = (await Read('/user/chips', Token, 'balance after concurrent Slot cash-out')).ChipBalance;",
                  "    Check('final balance equals the pre-cash-out balance plus one reward snapshot', () => pm.expect(FinalBalance).to.eql(ExpectedFinalBalance));",
                  "    const ActiveAfter = await Read('/sessions/active', Token, 'active Slot session after concurrent cash-out');",
                  "    Check('no active Slot session remains after concurrent cash-out', () => pm.expect(ActiveAfter).to.eql(null));",
                  "    const RegistryAfter = await Read('/sessions/me/active', Token, 'active-session registry after concurrent Slot cash-out');",
                  "    Check('active-session registry is released for the original Slot session', () => pm.expect(RegistryAfter).to.eql(null));",
                  "    const Repeated = await CallApi('POST', '/sessions/active/cash-out', undefined, Token);",
                  "    Check('the original Slot session cannot produce another payout', () => { pm.expect(SlotSessionId).to.be.a('number').and.above(0); pm.expect(Repeated.code).to.eql(400); });",
                  "    const BalanceAfterRepeatedAttempt = (await Read('/user/chips', Token, 'balance after repeated Slot cash-out attempt')).ChipBalance;",
                  "    Check('repeated Slot cash-out attempt does not change the once-credited balance', () => pm.expect(BalanceAfterRepeatedAttempt).to.eql(ExpectedFinalBalance));",
                  "  } catch (ErrorObject) {",
                  "    FlowError = ErrorObject;",
                  "  } finally {",
                  "    if (CleanupToken) {",
                  "      try {",
                  "        const Deactivation = await CleanupUser(CleanupToken);",
                  "        pm.test('TC-024 - temporary user is deactivated through API-only cleanup', () => pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' }));",
                  "      } catch (CleanupError) {",
                  "        pm.test('TC-024 - API-only cleanup completes', () => { throw CleanupError; });",
                  "      }",
                  "    }",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((ErrorObject) => { pm.test('TC-024 - concurrent Slot cash-out flow completes', () => { throw ErrorObject; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/slot/machine",
              "host": ["{{baseUrl}}"],
              "path": ["slot", "machine"]
            }
          },
          "response": []
        }
      ]
    },
    {
      "name": "TC-025 - Concurrent Gambit cash-out rejects duplicate payout",
      "item": [
        {
          "name": "TC-025 - Concurrent Gambit cash-out rejects duplicate payout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "async function CallApi(Method, Path, Body, Token) {",
                  "  const Header = { 'Content-Type': 'application/json' };",
                  "  if (Token) Header.Authorization = 'Bearer ' + Token;",
                  "  const Request = { method: Method, url: pm.environment.get('baseUrl') + Path, header: Header };",
                  "  if (Body !== undefined) Request.body = { mode: 'raw', raw: JSON.stringify(Body) };",
                  "  return new Promise((ResolveRequest, RejectRequest) => pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? RejectRequest(ErrorObject) : ResolveRequest(Response)));",
                  "}",
                  "function Check(Name, Assertion) {",
                  "  let Failure;",
                  "  pm.test('TC-025 - ' + Name, function () { try { Assertion(); } catch (ErrorObject) { Failure = ErrorObject; throw ErrorObject; } });",
                  "  if (Failure) throw Failure;",
                  "}",
                  "async function Read(Path, Token, Label) {",
                  "  const Response = await CallApi('GET', Path, undefined, Token);",
                  "  Check(Label + ' returns 200', () => pm.expect(Response.code).to.eql(200));",
                  "  return Response.text().trim() === '' ? null : Response.json();",
                  "}",
                  "function SessionView(Payload) {",
                  "  if (!Payload || !Object.prototype.hasOwnProperty.call(Payload, 'PeekResult')) return Payload;",
                  "  const View = Object.assign({}, Payload);",
                  "  delete View.PeekResult;",
                  "  return View;",
                  "}",
                  "function PendingAction(Session) {",
                  "  if (Session.Grid.PendingInteraction) {",
                  "    const Required = Session.Grid.PendingInteraction.RequiredSelections;",
                  "    const Positions = Session.Grid.Unrevealed.filter((Card) => Card.Locked === false).slice(0, Required).map((Card) => Card.Position);",
                  "    if (Positions.length !== Required) throw new Error('TC-025 cannot build the required pending interaction selection.');",
                  "    return { Path: '/gambit/sessions/active/resolve-effect', Body: { Positions } };",
                  "  }",
                  "  if (Session.Grid.PendingEvent) {",
                  "    const Pending = Session.Grid.PendingEvent;",
                  "    const GoodIndex = Pending.GoodOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0);",
                  "    const BadIndex = Pending.BadOptions.findIndex((Option) => typeof Option === 'string' && Option.length > 0);",
                  "    if (GoodIndex < 0 || BadIndex < 0) throw new Error('TC-025 cannot build the required pending event selection.');",
                  "    return { Path: '/gambit/sessions/active/resolve-event', Body: { GoodIndex, BadIndex } };",
                  "  }",
                  "  return null;",
                  "}",
                  "async function FinishSession(Session, Token) {",
                  "  let Current = SessionView(Session);",
                  "  let Actions = 0;",
                  "  while (Current.Status === 'InProgress' && Actions < 100) {",
                  "    Actions += 1;",
                  "    const Pending = PendingAction(Current);",
                  "    if (Pending) {",
                  "      const Resolved = await CallApi('POST', Pending.Path, Pending.Body, Token);",
                  "      if (Resolved.code !== 201) throw new Error('TC-025 pending-state resolution returned ' + Resolved.code + ': ' + Resolved.text());",
                  "      Current = SessionView(Resolved.json());",
                  "      continue;",
                  "    }",
                  "    const Target = Current.Grid.Unrevealed.find((Card) => Card.Locked === false);",
                  "    if (!Target) throw new Error('TC-025 found no burnable card before the session finished.');",
                  "    const Burned = await CallApi('POST', '/gambit/sessions/active/burn/' + Target.Position, undefined, Token);",
                  "    if (Burned.code !== 201) throw new Error('TC-025 burn returned ' + Burned.code + ': ' + Burned.text());",
                  "    Current = SessionView(Burned.json());",
                  "  }",
                  "  if (Current.Status !== 'Finished') throw new Error('TC-025 did not finish within 100 real API actions.');",
                  "  return { Session: Current, Actions };",
                  "}",
                  "async function ConcurrentCashOuts(Path, Token) {",
                  "  const Request = { method: 'POST', url: pm.environment.get('baseUrl') + Path, header: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + Token } };",
                  "  return Promise.all([0, 1].map(() => new Promise((ResolveRequest, RejectRequest) => {",
                  "    pm.sendRequest(Request, (ErrorObject, Response) => ErrorObject ? RejectRequest(ErrorObject) : ResolveRequest(Response));",
                  "  })));",
                  "}",
                  "async function CleanupUser(Token) {",
                  "  if (!Token) return;",
                  "  const Lookup = await CallApi('GET', '/gambit/sessions/active', undefined, Token);",
                  "  if (Lookup.code !== 200) throw new Error('TC-025 cleanup session lookup returned ' + Lookup.code + '.');",
                  "  let Current = Lookup.text().trim() === '' ? null : SessionView(Lookup.json());",
                  "  if (Current && Current.Status === 'InProgress') Current = (await FinishSession(Current, Token)).Session;",
                  "  if (Current && Current.Status === 'Finished') {",
                  "    const CashOut = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token);",
                  "    if (CashOut.code !== 201) throw new Error('TC-025 cleanup cash-out returned ' + CashOut.code + '.');",
                  "  }",
                  "  const Deactivation = await CallApi('DELETE', '/user', undefined, Token);",
                  "  if (Deactivation.code !== 200) throw new Error('TC-025 cleanup user deactivation returned ' + Deactivation.code + '.');",
                  "  return Deactivation;",
                  "}",
                  "",
                  "(async function () {",
                  "  let CleanupToken;",
                  "  let FlowError;",
                  "  try {",
                  "    Check('Gambit Table list is available through the real API', () => { pm.response.to.have.status(200); pm.expect(pm.response.json()).to.be.an('array'); });",
                  "    const Table = pm.response.json().find((Candidate) => Candidate.Name === 'Gambit 1');",
                  "    Check('official Gambit 1 fixture is active and usable', () => { pm.expect(Table).to.exist; pm.expect(Table.GambitTableId).to.be.a('number').and.above(0); pm.expect(Table.Active).to.eql(true); });",
                  "    const CardsPurchased = Math.min(12, Table.MaxCardsPurchased);",
                  "    Check('real Gambit play buys a valid card count', () => { pm.expect(CardsPurchased).to.be.at.least(Table.MinimumCardsPurchased); pm.expect(CardsPurchased).to.be.at.most(Table.MaxCardsPurchased); });",
                  "    const SessionCost = CardsPurchased * Table.CardPrice;",
                  "    const InitialBalance = Math.max(Table.MinimumChipsRequired, SessionCost) + 100;",
                  "    const Suffix = pm.variables.replaceIn('{{$guid}}').replace(/-/g, '');",
                  "    const Credentials = { Name: 'TC025 Concurrent Gambit User', Email: 'tc025.' + Suffix + '@example.test', Nickname: 'tc025' + Suffix, Password: pm.variables.replaceIn('{{$guid}}') + 'Aa1!' };",
                  "    const Registration = await CallApi('POST', '/auth/register', { Name: Credentials.Name, BirthDate: '1995-01-01', Nickname: Credentials.Nickname, Email: Credentials.Email, Password: Credentials.Password, ChipBalance: InitialBalance });",
                  "    if (Registration.code === 201) CleanupToken = Registration.json().Token;",
                  "    Check('temporary user is created through the real API', () => { pm.expect(Registration.code).to.eql(201); pm.expect(Registration.json().User.Email).to.eql(Credentials.Email); pm.expect(Registration.json().User.ChipBalance).to.eql(InitialBalance); });",
                  "    const Login = await CallApi('POST', '/auth/login', { Email: Credentials.Email, Password: Credentials.Password });",
                  "    Check('temporary user is authenticated through the real API', () => { pm.expect(Login.code).to.eql(200); pm.expect(Login.json().User.UserId).to.eql(Registration.json().User.UserId); pm.expect(Login.json().Token).to.be.a('string').and.not.empty; });",
                  "    const Token = Login.json().Token;",
                  "    CleanupToken = Token;",
                  "    const Creation = await CallApi('POST', '/gambit-tables/' + Table.GambitTableId + '/sessions', { CardsPurchased }, Token);",
                  "    Check('real Gambit session starts for the authenticated user', () => { pm.expect(Creation.code).to.eql(201); pm.expect(Creation.json().session.GambitTableId).to.eql(Table.GambitTableId); pm.expect(Creation.json().session.Status).to.eql('InProgress'); });",
                  "    const Completed = await FinishSession(Creation.json().session, Token);",
                  "    const Finished = await Read('/gambit/sessions/active', Token, 'finished Gambit session before cash-out');",
                  "    const GambitSessionId = Finished.GambitSessionId;",
                  "    const ResultSnapshot = Finished.Result;",
                  "    const ExpectedReward = Math.max(0, ResultSnapshot || 0);",
                  "    Check('real burns and pending-state handlers finish the original Gambit session', () => { pm.expect(Completed.Actions).to.be.above(0); pm.expect(Finished.Status).to.eql('Finished'); pm.expect(GambitSessionId).to.be.a('number').and.above(0); pm.expect(GambitSessionId).to.eql(Creation.json().session.GambitSessionId); pm.expect(Finished).to.deep.equal(Completed.Session); pm.expect(ResultSnapshot).to.be.a('number').and.at.least(0); });",
                  "    const BalanceBefore = (await Read('/user/chips', Token, 'balance immediately before concurrent Gambit cash-out')).ChipBalance;",
                  "    Check('balance before cash-out reflects only the real card purchase debit', () => pm.expect(BalanceBefore).to.eql(InitialBalance - SessionCost));",
                  "",
                  "    const CashOutResponses = await ConcurrentCashOuts('/gambit/sessions/active/cash-out', Token);",
                  "    const Successful = CashOutResponses.filter((Response) => Response.code === 201);",
                  "    const Rejected = CashOutResponses.filter((Response) => Response.code === 400);",
                  "    Check('exactly one concurrent Gambit cash-out succeeds', () => pm.expect(Successful).to.have.length(1));",
                  "    Check('duplicate concurrent Gambit cash-out returns 400', () => { pm.expect(Rejected).to.have.length(1); pm.expect(Rejected[0].code).to.eql(400); });",
                  "    Check('both overlapping Gambit cash-out responses are fully accounted for', () => pm.expect(Successful.length + Rejected.length).to.eql(2));",
                  "    const ExpectedFinalBalance = BalanceBefore + ExpectedReward;",
                  "    Check('successful Gambit cash-out reports the finished session reward once', () => { pm.expect(Successful[0].json().message).to.eql('Cash out successful'); pm.expect(Successful[0].json().reward).to.eql(ExpectedReward); pm.expect(Successful[0].json().finalBalance).to.eql(ExpectedFinalBalance); });",
                  "    const FinalBalance = (await Read('/user/chips', Token, 'balance after concurrent Gambit cash-out')).ChipBalance;",
                  "    Check('final balance equals the pre-cash-out balance plus one Gambit reward', () => pm.expect(FinalBalance).to.eql(ExpectedFinalBalance));",
                  "    const ActiveAfter = await Read('/gambit/sessions/active', Token, 'active Gambit session after concurrent cash-out');",
                  "    Check('no active Gambit session remains after concurrent cash-out', () => pm.expect(ActiveAfter).to.eql(null));",
                  "    const RegistryAfter = await Read('/sessions/me/active', Token, 'active-session registry after concurrent Gambit cash-out');",
                  "    Check('active-session registry is released for the original Gambit session', () => pm.expect(RegistryAfter).to.eql(null));",
                  "    const Repeated = await CallApi('POST', '/gambit/sessions/active/cash-out', undefined, Token);",
                  "    Check('the original Gambit session cannot produce another payout', () => { pm.expect(GambitSessionId).to.be.a('number').and.above(0); pm.expect(Repeated.code).to.eql(400); });",
                  "    const BalanceAfterRepeatedAttempt = (await Read('/user/chips', Token, 'balance after repeated Gambit cash-out attempt')).ChipBalance;",
                  "    Check('repeated Gambit cash-out attempt does not change the once-credited balance', () => pm.expect(BalanceAfterRepeatedAttempt).to.eql(ExpectedFinalBalance));",
                  "  } catch (ErrorObject) {",
                  "    FlowError = ErrorObject;",
                  "  } finally {",
                  "    if (CleanupToken) {",
                  "      try {",
                  "        const Deactivation = await CleanupUser(CleanupToken);",
                  "        pm.test('TC-025 - temporary user is deactivated through API-only cleanup', () => pm.expect(Deactivation.json()).to.deep.equal({ message: 'Account deleted successfully' }));",
                  "      } catch (CleanupError) {",
                  "        pm.test('TC-025 - API-only cleanup completes', () => { throw CleanupError; });",
                  "      }",
                  "    }",
                  "  }",
                  "  if (FlowError) throw FlowError;",
                  "})().catch((ErrorObject) => { pm.test('TC-025 - concurrent Gambit cash-out flow completes', () => { throw ErrorObject; }); });"
                ]
              }
            }
          ],
          "request": {
            "auth": {
              "type": "noauth"
            },
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/gambit-table",
              "host": ["{{baseUrl}}"],
              "path": ["gambit-table"]
            }
          },
          "response": []
        }
      ]
    }
  ]
}
